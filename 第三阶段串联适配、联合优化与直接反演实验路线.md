# 第三阶段：串联适配、联合优化与直接反演实验路线

> 更新日期：2026-09-01  
> 当前状态：Stage 1和Stage 2已分别完成预训练，C1/C2均未修复级联退化。`S3-D0-H`完整38轨oracle-support为`RMSE=4.1538 mm/h、r=0.3728`，相对匹配F0的`3.9180/0.4713`明显退化，证明冻结W1.25特征不能由17参数线性头充分读取降水。为区分“冻结表示不可线性读取”和“直接多头路线无效”，现已实现最后一次受控`S3-D0-D-RainPrimary`：从D0-H最佳雨量头初始化，冻结stem/encoder，解冻完整decoder与三头，以最终降水为主任务、Stage 2物理任务为train-only梯度平衡后的弱约束。

## 1. 先给出冻结策略结论

不应在“冻结Stage 1”和“冻结Stage 2”之间二选一。三种冻结状态都需要有实验证据，但它们的作用和执行顺序不同：

| 状态 | Stage 2 | Stage 1 | 实验作用 | 是否需要新训练 |
|---|---|---|---|---|
| `S3-F0-Frozen` | 冻结 | 冻结 | 测量独立预训练后的原始级联误差 | 否，已完成 |
| `S3-C1-O-S1Adapt` | 冻结 | 训练 | 检验Stage 1能否适应Stage 2输出的dBZ分布和纹理 | 是，第一个单边实验 |
| `S3-C2-O-S2TaskAware` | 训练 | 冻结 | 检验最终降水损失能否引导Stage 2生成对封版Stage 1更有用的反射率 | 是，第二个单边实验 |
| `S3-C3-PartialJoint` | 局部训练 | 训练 | 在可归因的单边实验后做有限联合优化 | 有条件才训练 |

因此，两个单边冻结实验都建议做一遍，但要严格按下列顺序执行：

```text
F0两端全冻结基线（已完成）
  ↓
C0无训练接口审计
  ↓
C1-O：冻结Stage 2，训练Stage 1
  ↓ 分析完整val
C2-O：冻结Stage 1，训练Stage 2
  ↓ 分析完整val并比较两个适配方向
只让达到门槛的较好方向进入可部署P版
  ↓
直接GR→rain多头对照D0
  ↓
若单边适配有效，再做局部联合解冻C3
```

不建议一开始同时训练两个3D U-Net。那样无法判断改善来自哪一端，也更容易出现中间dBZ物理语义漂移、显存压力和优化不稳定。

## 2. 符号、封版模型与当前输入契约

将Stage 2记为 $F_{\theta}$，Stage 1记为 $G_{\phi}$：

\[
X_{GR}
\xrightarrow{F_{\theta}}
(\widehat Z_{DPR},\widehat p_{support})
\xrightarrow{G_{\phi}}
\widehat R_{DPR}.
\]

当前两个预训练起点为：

- Stage 2：`outputs/stage2_ablations/four_channel_distance_intensity_w1p25/best_dbz.pt`，完整val选出的W1.25反射率检查点；
- Stage 1：`outputs/ablations/stage1_i_g002_t3d/best.pt`，`I+G-0.02+T3D`封版检查点，best epoch 22。

Stage 2的最终输入始终只由部署时可获得的GR信息构成：

```text
x_gr: (B,4,64,64,60)

channel 0 = sparse_dbz_standardized
channel 1 = gr_value_mask
channel 2 = nearest_gr_distance_scaled
channel 3 = height_scaled
```

Stage 2输出：

```text
support_logits:       (B,1,64,64,60)
support_probability:  (B,1,64,64,60) = sigmoid(support_logits)
predicted_dpr_std_s2: (B,1,64,64,60)
```

Stage 1的输入契约是：

```text
x_stage1: (B,3,64,64,60)

channel 0 = DPR dBZ在Stage 1统计下的逐高度标准化值
channel 1 = DPR反射率support信息
channel 2 = height_scaled
```

Stage 2和Stage 1不得直接共用标准化数值。串联接口必须显式执行：

```text
predicted_dpr_std_s2
  -> 使用Stage 2 target统计反标准化
  -> predicted_dpr_physical_dbz
  -> 使用Stage 1 input统计逐高度标准化
  -> predicted_dpr_std_s1
```

上述反标准化和重新标准化都是仿射运算，在C2/C3中必须保持可导。

## 3. F0两端全冻结实测结论

现有结果来自完整38条验证轨道和1,195,966个可靠正降水体素：

| Stage 1输入 | RMSE (mm/h) | Pearson r | CCC | dR/dz r |
|---|---:|---:|---:|---:|
| 真实DPR dBZ + 真实DPR mask | 2.2262 | 0.8637 | 0.8522 | 0.7023 |
| 师兄插值GR + 封版Stage 1 | 4.0753 | 0.4280 | 0.3638 | 0.0456 |
| D预测dBZ + 真实mask | 4.0004 | 0.4419 | 0.3669 | 0.0615 |
| D预测dBZ + 预测mask | 4.0755 | 0.4246 | 0.3589 | 0.0582 |
| W1.25预测dBZ + 真实mask | 3.9180 | 0.4713 | 0.3888 | 0.0688 |
| W1.25预测dBZ + 预测mask | 3.9884 | 0.4538 | 0.3796 | 0.0654 |

这些结果说明：

1. `true DPR -> Stage 1`在新串联评价中与原Stage 1完整val结果几乎完全一致，可基本排除高度轴、通道顺序、归一化和滑窗重建接错；
2. W1.25是当前最好的Stage 2串联起点，但只比D和师兄插值略好，与真实DPR输入上限仍相差很大；
3. W1.25使用真实mask时r已由0.8637降到0.4713，换为预测mask只进一步降到0.4538；
4. 因此主要瓶颈是Stage 2反射率值场、强回波尾部和三维结构与真实DPR域不一致，预测mask是次要但稳定存在的附加损失；
5. 当前不应再独立搜索W权重或support硬阈值，而应转向接口适配和最终降水任务。

原始串联指标和逐轨结果见[comparison.csv](./outputs/stage2_stage1_cascade/validation_d_vs_w1p25/comparison.csv)与[metrics.json](./outputs/stage2_stage1_cascade/validation_d_vs_w1p25/metrics.json)。

## 4. S3-C0：无训练接口审计

C0不产生新模型，用于在耗费GPU训练前补齐错误归因。

### 4.1 补齐dBZ×support的严格2×2评价

现有评价还缺少“真实DPR dBZ + Stage 2预测support”。需对每个Stage 2候选统一计算：

| dBZ值场 | support | 含义 |
|---|---|---|
| 真实DPR | 真实DPR | Stage 1理想输入上限 |
| Stage 2预测 | 真实DPR | 隔离dBZ值场误差 |
| 真实DPR | Stage 2预测 | 隔离support误差 |
| Stage 2预测 | Stage 2预测 | 完整部署串联 |

只有这样才能形成值场与support的完整析因实验。

当前实现将四个单元固定命名为`TT/PT/TP/PP`，并自动输出`factorial_2x2.json`和`factorial_2x2.csv`。其中第一个字母表示dBZ值场是真实`T`还是Stage 2预测`P`，第二个字母表示support是真实`T`还是预测`P`。文件同时给出：真实support下的值场效应、真实值场下的support效应、预测值场下的support附加效应，以及`PP-PT-TP+TT`交互项。

必须注意一个不可回避的反事实边界：当Stage 2产生support假阳性时，该位置不存在“真实DPR dBZ”。代码不借用Stage 2 dBZ，而填入Stage 1训练集该高度的dBZ均值，使其进入Stage 1标准化后恰好为`0`，同时保留预测support通道为`1`。因此TP是“真实已观测dBZ + 假阳性位置中性占位”的受控support诊断，不能被解释为自然界中存在未观测的真实dBZ；该说明也写入审计JSON。

### 4.2 修正串联可视化

所有方法必须使用同一经纬度范围，不再对各自的output support独立缩放。图中同时显示：

- 公共QC轨道底图；
- 真实DPR support轮廓；
- Stage 2预测support轮廓；
- 降水质心、覆盖面积和主要连通雨团；
- 相同高度和相同颜色范围的水平切片。

修正后的绘图从`qc_label_mask`沿高度求并集得到二维公共QC轨道底图，所有水平面板强制使用同一`lon_min/lon_max/lat_min/lat_max`；浅灰色显示公共QC footprint，青色虚线显示真实DPR support，橙色实线显示当前方法实际送入Stage 1的support。A–B剖面也叠加同一组support边界。旧版轨道缓存可通过DPR-oracle输入support自动恢复真实support，因此无需重新推理即可先重绘旧结果；新C0缓存则会显式保存`true_dpr_support`、Stage 2 support概率、阈值后二值support和物理dBZ。

### 4.3 补充结构和可观测性分组

最终降水误差需继续按以下分组：

- `Q11/Q01/gap_proxy/outside_proxy`；
- 最近GR直接观测距离和局部观测密度；
- 降水质心偏移、水平扩散尺度、覆盖比和连通域；
- 水平exact指标与邻域放宽指标；
- 逐高度分位数、平均垂直廓线、强降水尾部和dR/dz。

可增加一个只在train拟合的“逐高度仿射dBZ校准”诊断，再将校准结果送入冻结Stage 1。如果它只改善Bias而r和dR/dz几乎不变，就可以进一步确认主要损失来自空间和垂直结构，而不是简单的均值/方差偏差。校准参数不得在val或test上拟合。

## 5. S3-C1-O-S1Adapt：冻结Stage 2，训练Stage 1

### 5.1 这个实验回答什么

C1-O回答：

> Stage 2的预测dBZ中是否仍然保留了足够的降水信息，只是封版Stage 1没有见过这种平滑、压缩且带结构误差的输入分布？

这是第一个实验，因为它：

- 保留Stage 2已学到的物理dBZ语义；
- 不会因为下游降水损失而先改变Stage 2输出；
- 能最直接区分“下游输入域不匹配”和“Stage 2信息已不可恢复”；
- Stage 2可在`no_grad`下运行，显存和训练稳定性优于反向穿过两个网络。

### 5.2 严格oracle接口

C1-O使用真实DPR support，仅隔离dBZ接口问题：

```text
Stage 2固定输出 predicted_dpr_std_s2: (B,1,64,64,60)
  -> 反标准化到物理dBZ
  -> 重新按Stage 1统计标准化
  -> 在M_dpr=0处填0

x_stage1_oracle: (B,3,64,64,60)
  = [predicted_dpr_std_s1, true_dpr_support, height_scaled]

Stage 1:
  -> predicted_rain: (B,1,64,64,60)
```

这里的真实DPR support是诊断手段，部署时不可获得，所以C1-O不是最终模型。

### 5.3 训练规则

- Stage 2设为`.eval()`、`requires_grad=False`，其前向使用`torch.no_grad()`；
- Stage 1从T3D epoch 22封版权重初始化；
- 首轮只优化降水主损失`I + 0.02G`，不更换数据划分、Sampler、随机种子和Patch几何；
- T3D类型头结构可保留，但首个接口实验暂不累加类型损失，避免将额外多任务优化混入冻结方向判断；
- 建议Stage 1学习率从`1e-5`开始，训练15至20轮，低学习率衰减至`1e-6`，patience为8；
- 验证时仍对Stage 2漏掉或预测错误的dBZ进行评分，不得用预测结果缩小loss/metric mask。

### 5.4 继续门槛

C1-O必须相对匹配的F0 oracle-mask基线（RMSE 3.9180，r 0.4713）比较：

- RMSE至少下降3%，即低于约`3.800 mm/h`；或
- Pearson r至少提高`0.01`，即高于约`0.481`；
- 逐轨配对bootstrap方向稳定，不是少数轨道提高总体结果；
- `5–10/10–30/>=30 mm/h`和dR/dz不得出现超过约3%的新退化。

上述只是“继续实验”的最低门槛，不等于最终模型已达标。如果仅提高0.01而与DPR oracle仍差距巨大，必须继续与直接GR→rain模型比较。

## 6. S3-C2-O-S2TaskAware：冻结Stage 1，训练Stage 2

### 6.1 这个实验回答什么

C2-O回答：

> 在保持Stage 1的DPR→rain映射不变时，最终降水损失能否引导Stage 2优先修正对降水最关键的强回波、局部排序和垂直结构？

它建议也做一遍，因为C1与C2回答的问题不同：

- C1测试下游适应能力，但无法恢复Stage 2已丢失的信息；
- C2可以用最终降水目标重新分配Stage 2的学习重点，但有把中间反射率变成“只能欺骗固定Stage 1的伪 dBZ”的风险。

### 6.2 损失与物理锚定

不允许C2只使用最终降水损失。必须保留Stage 2原有的dBZ与support监督：

\[
L_{C2}
=L_{dbz}^{W1.25}
+L_{support}
+\lambda_R\left(L_I+0.02L_G\right).
\]

其中：

- `L_dbz^W1.25`继续使用当前物理dBZ强度权重；
- `L_support`继续使用真实DPR support标签和原BCE口径；
- `L_I+0.02L_G`是冻结Stage 1输出的最终降水损失；
- `lambda_R`不在val搜索；正式训练前只用train batch做损失尺度/梯度范数审计，使下游rain梯度不在首轮压过原Stage 2物理监督，选定后固定。

首轮可先只解冻Stage 2反射率头和最后一个解码块，较原W1.25训练使用更低学习率。如果这一受限变体完全无法改善，再考虑是否扩大解冻范围，不得直接全量解冻。

### 6.3 梯度路径的重要实现要求

Stage 1的参数要冻结且设为`.eval()`，但不能把Stage 1前向包在`torch.no_grad()`中。正确梯度路径是：

```text
rain loss
  -> frozen Stage 1 operations
  -> differentiable Stage2->Stage1 normalization bridge
  -> Stage 2 predicted dBZ
  -> trainable Stage 2 parameters
```

`requires_grad=False`只阻止Stage 1参数更新，不应阻断损失对Stage 1输入和Stage 2输出的梯度。

C2-O同样使用真实DPR support送入Stage 1，用于和C1-O在相同support条件下比较。support头仍接受原监督，但不在下游梯度中使用`probability >= 0.8`硬阈值。

### 6.4 额外通过条件

C2-O除了满足与C1-O相同的最终降水门槛，还必须满足：

- Stage 2全目标dBZ RMSE不得相对W1.25恶化超过约3%；
- Stage 2 dBZ Pearson r不得出现超过约0.01的明显下降；
- support CSI/Recall/FAR不得通过放弃中间物理任务来换取最终降水的表面改善；
- `>=35 dBZ`、`>=30 mm/h`和dR/dz必须同时报告。

如果最终降水提高但dBZ指标大幅恶化，该模型不能再被解释为“GR→DPR反射率→降水”，只能作为隐表示端到端模型，并必须与D0直接模型公平比较。

## 7. 两个单边实验如何决定后续方向

### 7.1 两者都未达到门槛

这说明：

- Stage 2的确已经丢失了最终降水所需的事件级结构信息；
- 下游适配无法恢复，上游也无法在物理锚定下修正；
- 不应继续训练两个完整U-Net的联合串联。

此时直接跳转`S3-D0-DirectMultiHead`。

### 7.2 只有C1-O明显有效

说明Stage 2预测仍保留足够信息，主要是Stage 1输入域不匹配。下一步进入`S3-C1-P-S1Adapt`，使用部署时可获得的预测support接口，不必先改动Stage 2。

### 7.3 只有C2-O明显有效

说明固定Stage 1仍可用，但Stage 2必须感知最终降水目标。下一步进入`S3-C2-P-S2TaskAware`，将下游接口改为可导的预测support表示。

### 7.4 两者都有效

使用完整val的最终降水、Stage 2物理dBZ、强尾部、dR/dz和逐轨bootstrap选出Pareto较好的方向，只让一个主方向进入P版。不自动把两个O版都扩展为新一轮全部组合实验。

## 8. P版：从oracle诊断过渡到可部署接口

### 8.1 O版与P版的边界

- `O`表示oracle support：Stage 1输入真实DPR mask，仅用于科学归因；
- `P`表示predicted support：输入必须只使用GR与Stage 2预测，可作为最终部署候选。

P版不得在loss或metric中将标签mask与预测support取交集。否则Stage 2漏掉的降水体素会直接从评价中消失，造成虚假改善。

### 8.2 先保留硬mask口径作为严格对照

为了保持单因素，对应O版达标后，首个P版可先使用当前仅由val选定的`0.8`硬阈值。漏掉位置在最终评价中必须记为降水0，不能被删除。

### 8.3 soft-support是后续独立实验

硬阈值不可导，也会使false negative永久无法被下游修复。可在硬P版完成后，另行定义一个soft-support单因素实验：

```text
z_soft = support_probability * predicted_dpr_std_s1

x_stage1_soft: (B,3,64,64,60)
  = [z_soft, support_probability, height_scaled]
```

该实验不得悄悄沿用“只在可靠正降水上学习强度”的旧任务口径。如果允许模型在预测support外输出降水，就必须同时引入有效零降水监督，并在完整QC标签域报告降水发生、Recall、FAR、CSI与强度指标。这是新任务，必须作为显式消融记录。

## 9. S3-D0-DirectMultiHead：直接GR→rain对照

D0不是串联失败后的临时备选，而是阶段三必须建立的正式对照：

```text
x_gr: (B,4,64,64,60)
  -> 共享高度保持3D U-Net骨干
       |- rain头：最终三维降水（主任务）
       |- dBZ头：DPR反射率（辅助任务）
       `- support头：DPR反射率支持域（辅助任务）
```

该设计的作用是：

- 最终rain头可直接使用GR骨干的高维特征；
- dBZ/support仍作为物理辅助监督，不丢失Stage 2已学到的知识和可解释性；
- 降水不再完全受制于单个确dBZ场和mask的信息瓶颈；
- 可与两个独立U-Net的参数量、训练成本和最终性能公平比较。

即使C1/C2取得改善，D0仍必须训练，因为项目的最终目标是GR→rain，而不是必须显式经过唯一dBZ中间场。

### 9.1 已实现的三个受控解冻范围

截至2026-09-01，D0代码、真实数据契约和CPU端到端烟雾已经完成。三个实验共用四通道输入、完整Stage 2四类Sampler、三个标签侧任务和相同随机种子：

| 实验 | 可训练部分 | 轮数/LR | 目的 | 当前状态 |
|---|---|---|---|---|
| `D0-H` | 新增`1×1×1 rain_head`，17参数 | 5轮，`1e-4→1e-5` | 测试冻结W1.25高维特征是否已含可线性读取的降水信息 | 已完成，未过门槛 |
| 旧`D0-D-AnchorPrimary` | 完整decoder+三头，encoder冻结 | 20轮，`1e-5→1e-6` | 物理锚点为主、rain为辅 | 保留复现，不启动 |
| `D0-D-RainPrimary` | 完整decoder+三头，stem/encoder冻结，共589011参数 | 20轮，`1e-5→1e-6` | 最终rain为主、support/dBZ为弱物理约束 | 已实现并通过真实CPU烟雾，待三卡正式训练 |

D0-H没有共享的可训练参数，因此不对互不相连的head梯度做伪比较。RainPrimary从D0-H `best_rain.pt`（epoch 2）恢复完整权重，避免丢弃已学rain head；梯度范数只计算三个任务共同作用的decoder参数，明确排除任务专属head。

### 9.2 损失、门控与整轨评价

三个输出均保持`(B,1,64,64,60)`。旧AnchorPrimary损失为：

\[
L_{D0}=L_{support}+L_{dbz}^{W1.25}+\lambda_R(L_I+0.02L_G).
\]

本轮RainPrimary严格改为：

\[
L_{D0\text{-}RP}=(L_I+0.02L_G)+\lambda_{phys}
  (L_{support}+L_{dbz}^{W1.25}).
\]

`lambda_phys`只由train batch审计选择，使共享decoder上加权后的Stage 2物理梯度范数约为未加权rain梯度的`0.25`；validation/test不参与选择。单卡真实CPU烟雾审计得到`lambda_phys≈0.1072`，该数值仅验证实现，正式三卡会用每卡最多4个有效batch重新聚合。

其中support、dBZ和rain继续使用各自原有mask及分母，不能互相取交集。训练时rain head输出稠密`log1p(mm/h)`，不在网络内部乘hard support；整轨拼接后才从同一个连续rain场形成两个口径：

- `d0_oracle_support`：用真实DPR support门控，仅诊断GR特征的强度上限，不可部署；
- `d0_predicted_support`：用完整val选择的D0 support阈值门控，是最终可部署口径。

这样能够把“直接rain强度能力”和“support恢复误差”分开，不会让不可导阈值阻断训练，也不会通过真实标签过滤模型漏报。

### 9.3 D0-H结论与RainPrimary例外实验

D0-H未通过预注册门槛：oracle-support RMSE比F0恶化约`6.0%`，`r`下降约`0.099`；逐轨RMSE只在`6/38`轨改善。真实support与预测support差距较小，退化主要来自强度表示，尤其是`5–10`、`10–30`和`>=30 mm/h`以及对流样本。dR/dz RMSE降低伴随梯度相关性、符号一致率和幅值比下降，属于输出变平滑而非垂直物理改善。

因此旧AnchorPrimary D0-D不启动。但H只检验冻结特征的线性可读性，尚未检验共享decoder能否受rain主任务驱动而重组空间表示。`D0-D-RainPrimary`作为一次明确记录的门槛外最终实验；若其仍不能至少超过F0的`RMSE=3.9180、r=0.4713`，则停止D0结构/权重微调，转向原始GR体扫、真实几何和缺失信息上限。

## 10. S3-C3-PartialJoint：局部联合解冻

C3只在至少一个单边P版达到门槛时启动。建议：

1. 从较好的单边适配检查点初始化；
2. 先只解冻Stage 2反射率头和最后一个解码块，Stage 1使用较高但仍很小的学习率；
3. 参考量级为Stage 1 `5e-6–1e-5`、Stage 2 `1e-6`，正式值在train-only梯度审计后固定；
4. 最终rain损失为主目标，但仍保留Stage 2 dBZ与support锚定；
5. 一次只扩大一个解冻区域，每一步后都做完整val整轨评价；
6. 如果最终rain改善仅来自中间dBZ语义坍塌，立即停止扩大解冻。

当C1-O和C2-O都未达标时，C3不得启动。

## 11. 统一评价口径和门槛

Stage 3开发期间只访问完整38条val轨道，不使用test选择冻结方向、support表示、损失权重、学习率或解冻范围。

每个正式模型必须报告：

1. 可靠正降水体素上的MAE/RMSE/Bias/r/CCC；
2. 包含有效零降水的完整QC标签域指标；
3. `<1/1–5/5–10/10–30/>=30 mm/h`强度分箱；
4. 逐高度、相对CFB、层云/对流和dR/dz；
5. `Q11/Q01/gap/outside`、最近GR距离和局部密度；
6. 降水质心、连通雨团、覆盖面积和邻域结构指标；
7. Stage 2中间dBZ、support、CFAD和垂直结构指标；
8. 逐轨结果与逐轨配对bootstrap置信区间。

三个实用层级的门槛分开：

- **继续试验门槛**：相对匹配F0基线，RMSE改善至少3%或r提高至少0.01，且关键分组无明显退化；
- **单边方向强通过门槛**：相对W1.25 oracle-mask基线至少关闭约20%的DPR-oracle差距，即约达到`RMSE <= 3.580 mm/h`或`r >= 0.550`，且逐轨bootstrap支持改善。只达到最低门槛而未达到该强门槛时，属于弱信号，可做重复种子或P版诊断，但不足以直接启动两端联合解冻；
- **Stage 3候选强门槛**：完整val的r建议至少达到0.60，稳定超过师兄插值和F0-W1.25，并明显恢复中强降水与dR/dz。

师兄PPT中约0.68的相关系数只能在分割、mask、高度范围和样本权重全部统一后比较，不得直接与当前pooled可靠正降水r混用。

## 12. 建议的代码实施顺序

当前C0只读评价、绘图与C1-O训练管线已经实现。代码状态如下：

```text
[已完成] scripts/evaluate_stage2_stage1_cascade.py   # C0严格2×2、主效应和交互项
[已完成] scripts/visualize_stage2_stage1_cascade.py  # 公共地理范围、QC底图和support轮廓

[已完成] src/precipitation_inversion/data/stage3_patch_dataset.py
[已完成] src/precipitation_inversion/models/stage3_cascade.py
[已完成] src/precipitation_inversion/losses/stage3_losses.py
[已完成] src/precipitation_inversion/training/stage3_engine.py
[已完成] scripts/train_stage3_cascade.py
[已完成] scripts/evaluate_stage3_cascade.py
[已完成] scripts/launch_stage3_c1_ddp.sh
[已完成] scripts/train_stage3_c2.py
[已完成] scripts/launch_stage3_c2_ddp.sh
[已完成] scripts/train_stage3_d0.py
[已完成] scripts/evaluate_stage3_direct.py
[已完成] scripts/launch_stage3_d0_ddp.sh
[已完成] src/precipitation_inversion/models/stage3_direct.py
[已完成] D0整轨三头重建、validation-only阈值选择及统一轨道可视化

[已完成] configs/stage3_c1_o_freeze_s2_train_s1.yaml
[已完成] configs/stage3_c2_o_freeze_s1_train_s2.yaml
[已完成] configs/stage3_d0_h_frozen_feature_probe.yaml
[已完成] configs/stage3_d0_d_decoder_multihead.yaml
[已完成] configs/stage3_d0_d_rain_primary.yaml

[已完成] tests/test_stage3_patch_dataset.py
[已完成] tests/test_stage3_cascade.py
[已完成] tests/test_stage3_losses.py
[已完成] tests/test_stage3_training_engine.py
[已完成] tests/test_stage3_evaluation.py
[已完成] tests/test_stage3_c2.py
[已完成] tests/test_stage3_d0.py
tests/test_stage3_visualization.py
```

`evaluate_stage2_stage1_cascade.py`继续作为F0/C0只读评价基准，不把Stage 3训练逻辑塞入同一文件。P版、D0和C3的配置仅在前置门槛达到后创建，避免未完成判定就扩张实验矩阵。

## 13. 检查点和存储策略

所有新Stage 3训练必须遵守：

- 始终保存`best.pt`与`last.pt`；
- 周期检查点每10个epoch保存一次，即`epoch_0009.pt`、`epoch_0019.pt`、`epoch_0029.pt`等；
- 不得每个epoch保存一个完整串联权重；
- C1只保存可训练Stage 1权重，并在metadata中记录冻结Stage 2的路径、哈希和配置；
- C2只保存可训练Stage 2权重，并记录冻结Stage 1的路径、哈希和配置；
- D0保存一个完整单网络状态（包含新增rain head），仍只保留各任务best、last和每10轮周期点；D0-H只有5轮，因此不会产生`epoch_000x.pt`；
- C3必须保存两端状态及完整输入契约，但仍只每10轮保存周期权重。

三卡DDP训练时，检查点、整轨评价和可视化只由rank 0写入，其他rank不得重复保存。

## 14. 数据上限与原始GR资料申请

当前W1.25在`outside_proxy`中的Stage 2 dBZ r约为0.428，support Recall约为0.232。这表明当前数据没有提供足够的事件级观测信息，单纯增大网络无法唯一恢复远离GR直接观测的真实降水结构。

建议在Stage 3最小适配实验进行的同时，向师兄询问：

- 原始极坐标GR体扫或未压缩的波束数据；
- 雷达站ID、站点经纬度和高度；
- 方位角、仰角、距离、波束宽度与真实覆盖标志；
- GR与DPR实际时间差；
- 同心圆压缩、多雷达合并和`dbz_gr_interp`生成细节；
- `NaN/-9999.9`等原始编码的确切含义。

## 15. 最终预注册路线

```text
S3-F0-Frozen                 已完成：确认独立硬串联严重退化
  ↓
S3-C0-2x2                    已完成：退化主要来自Stage 2值场
  ↓
S3-C1-O-S1Adapt              已完成且未过门槛：不进入C1-P
  ↓
S3-C2-O-S2TaskAware          已完成且未过门槛：停止C2-P
  ↓
S3-D0-H-DirectMultiHead      已完成且未过门槛：冻结特征线性头不足
  ↓ 一次有记录的门槛外机制验证
S3-D0-D-RainPrimary          已实现：rain主任务+弱物理约束，decoder+三头训练
  ↓
S3-C3-PartialJoint           C1/C2均失败，当前停止
  ↓
只锁定1至2个候选后才访问test，并在统一mask/split下与师兄方案比较
```

当前紧接着应正式训练`S3-D0-D-RainPrimary`并完成38轨val。主要判定使用oracle-support rain，predicted-support用于部署审计；同时核对Stage 2 support/dBZ没有灾难性退化。若RainPrimary仍未超过F0，则终止D0扩张，并把实验资源转移到当前压缩GR数据的可观测性与原始极坐标/几何资料。
