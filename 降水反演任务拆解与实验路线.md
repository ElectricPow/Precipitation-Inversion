# 降水反演任务拆解与实验路线

## 1. 最终目标

本项目的最终目标是构建一个降水反演模型：

> 输入地基雷达（Ground Radar，GR）在三维空间中的稀疏反射率以及推理时可获得的辅助变量，输出与星载雷达 DPR 降水产品尽可能接近的密集三维降水率图。

可形式化为：

\[
Z_{GR}^{sparse},\ M_{GR},\ A
\longrightarrow
\hat R_{DPR}^{dense}
\]

其中：

- \(Z_{GR}^{sparse}\) 是稀疏的地基雷达反射率；
- \(M_{GR}\) 是 GR 观测有效性掩码；
- \(A\) 是高度、温度、气压、比湿及雷达几何等可用辅助变量；
- \(\hat R_{DPR}^{dense}\) 是模型预测的密集降水率。

需要注意，DPR 降水产品本身也是卫星算法的反演结果，并非绝对无误差的真实降水。本项目的实际目标是学习和模仿 DPR 产品，并以它作为当前参考标签。

## 2. 问题中的三种核心困难

这个任务同时包含三类不同的问题，不应将它们混为一个简单的回归任务。

### 2.1 反射率到降水率的反演

需要学习：

\[
Z_{DPR}\longrightarrow R_{DPR}
\]

反射率与降水率之间不是唯一且恒定的对应关系，还会受降水类型、粒子谱、高度、温湿度、相态和衰减等因素影响。

### 2.2 GR 与 DPR 的观测域差异

需要学习：

\[
Z_{GR}\longrightarrow Z_{DPR}
\]

两种雷达在频段、扫描几何、灵敏度、噪声阈值、衰减、时空匹配和质量控制上都存在差异。这一映射应在时空配准且两类雷达都有效的位置上学习。

### 2.3 稀疏观测到密集空间场的恢复

需要学习：

\[
Z^{sparse}\longrightarrow Z^{dense}
\]

或者直接学习：

\[
Z_{GR}^{sparse}\longrightarrow R_{DPR}^{dense}
\]

仅将 GR 的数值分布转换为 DPR 风格，输出仍然是稀疏的。因此，“观测域转换”和“稀疏到密集”必须分别考虑。

## 3. 总体研究路线

保留三阶段思路，但对每个阶段的目标进行更严格的界定：

1. 学习 DPR 反射率到 DPR 降水率的反演，建立理想输入下的性能上限；
2. 学习 GR 与 DPR 的观测域差异，并研究稀疏信息恢复；
3. 串联预训练模块，与直接端到端模型对比，最后进行联合优化。

截至2026-09-01，前两阶段预训练和两端全冻结串联评价已完成，主线已进入Stage 3。详细冻结矩阵、可导接口、门槛和分支决策见[第三阶段串联适配、联合优化与直接反演实验路线](./第三阶段串联适配、联合优化与直接反演实验路线.md)。

## 4. 阶段一：DPR 反射率到 DPR 降水率

### 4.1 阶段目标

这一阶段不宜严格表述为“破译卫星官方算法”。DPR 官方反演可能还使用双频信息、衰减订正、粒子谱假设、降水类型和近地面质量标志等信息，当前 NC 中的变量只是官方产品的一部分。

本阶段的主要价值是：

- 判断 DPR 反射率中包含多少降水强度信息；
- 建立近似理想观测条件下的性能上限；
- 比较局部映射、垂直廓线和三维邻域信息的价值；
- 选择适合零膨胀和长尾降水分布的损失函数；
- 为最终模型预训练降水反演模块。

### 4.2 必须规避的 mask 泄漏

对已检查的示例文件：

- `dbz_dpr` 有效网格数为 95,263；
- `pre_dpr > 0` 的网格数也为 95,263；
- 两者的位置完全一致；
- `pre_dpr = 0` 的位置中，`dbz_dpr` 全部为缺测。

这意味着 `dbz_dpr` 的有效性掩码本身已经显露了是否存在降水。如果直接使用“填充后的 `dbz_dpr` + mask”预测所有网格的 `pre_dpr`，模型可能只是通过 mask 判断有雨和无雨，得到虚高的性能。

因此，本阶段建议拆成：

#### 4.2.1 降水发生识别

预测：

\[
P(R>r_0)
\]

由于当前 `dbz_dpr` mask 与正降水位置绑定，该实验主要用于理解数据产品，不能单独证明模型学会了公平的降水检测。

#### 4.2.2 正降水强度反演

仅在以下网格上学习：

\[
pre\_dpr>0,\qquad dbz\_dpr\ \text{有效}
\]

此实验回答：在 DPR 已经检测到降水的条件下，能否根据反射率估计降水强度。

### 4.3 模型实验顺序

按从简单到复杂的顺序进行：

1. 现有分层 Z–R 关系；
2. 每个网格独立预测的 MLP；
3. 输入整条垂直廓线的一维 CNN；
4. 引入三维邻域的小型 3D CNN 或 3D U-Net；
5. 在前述模型中加入高度、温度、气压和比湿等辅助变量。

简单模型是后续评价复杂模型的必要基线。如果 MLP 已经接近 3D U-Net，则说明该映射可能主要由局部数值关系决定。

## 5. 阶段二：GR–DPR 观测域转换与稀疏恢复

> 阶段二已根据全数据集统计、PPT 波束中心栅格化流程和原始存储编码形成详细方案，见[《第二阶段：GR 稀疏反射率到 DPR 反射率实验路线》](./第二阶段GR稀疏反射率到DPR反射率实验路线.md)。后续实施以该详细文档为准。

### 5.1 子任务 A：GR 到 DPR 的反射率校准

首先在 GR 和 DPR 都有有效反射率的配准位置上学习：

\[
\hat Z_{DPR}=f(Z_{GR},z,T,P,Q,G)
\]

其中 \(G\) 表示距离、方位角、仰角或雷达站点等几何信息。

不建议只进行无条件的全局直方图匹配。两类反射率的整体分布接近，并不代表同一时空位置的数值映射正确。

至少应将高度作为条件，并对不同高度、雷达距离和降水强度分组评价校准结果。

### 5.2 子任务 B：稀疏到密集的空间恢复

校准后的反射率仍然只存在于 GR 有直接观测的稀疏位置：

\[
Z_{GR}^{sparse}
\longrightarrow
\hat Z_{DPR}^{sparse}
\]

因此还需要研究：

\[
\hat Z_{DPR}^{sparse}
\longrightarrow
\hat Z_{DPR}^{dense}
\]

该过程不一定必须显式生成密集反射率，也可以让最终模型直接从稀疏 GR 预测密集降水率。两条路线应在实验中对比。

### 5.3 必须显式保留 GR mask

模型输入至少应包含：

```text
GR反射率填充数组
GR有效观测mask
高度或高度编码
```

不能在将缺测全部填成0后丢弃 mask，否则模型无法区分：

- 雷达没有直接观测；
- 低于预处理阈值的弱回波；
- 人工填充为0的反射率；
- 有效的无降水标签。

## 6. 阶段三：串联、直接预测与端到端优化

### 6.1 串联路线

串联模型可表示为：

\[
Z_{GR}^{sparse}
\xrightarrow{F}
\hat Z_{DPR}^{dense}
\xrightarrow{G}
\hat R_{DPR}^{dense}
\]

其中：

- \(F\) 负责观测域校准和稀疏信息恢复；
- \(G\) 负责反射率到降水率的反演。

建议训练流程：

1. 分别预训练 \(F\) 和 \(G\)；
2. 串联后首先冻结部分模块，检查级联误差；
3. 训练稳定后逐步解冻；
4. 使用较小学习率进行端到端微调。

最终不建议长期完全冻结两个模型，因为前一阶段的偏差可能被后一阶段放大。

### 6.2 当前冻结实验矩阵

完整val已证明两端全冻结的W1.25串联只达到`RMSE=3.9884 mm/h`和`r=0.4538`，而真实DPR输入Stage 1为`2.2262/0.8637`。因此冻结策略不再是模糊的“先冻结部分模块”，而是预注册为：

| 实验 | Stage 2 | Stage 1 | 目的 |
|---|---|---|---|
| `S3-F0-Frozen` | 冻结 | 冻结 | 已完成的级联失败基线，不重复训练 |
| `S3-C1-O-S1Adapt` | 冻结 | 训练 | 完整val未过门槛，不进入C1-P |
| `S3-C2-O-S2TaskAware` | 训练 | 冻结 | 已完成；结果与F0基本相同，未过门槛 |
| `S3-C3-PartialJoint` | 局部解冻 | 训练 | 仅在至少一个单边实验有效时进行 |

C1-O和C2-O先使用相同的真实DPR support隔离dBZ接口问题。两个单边实验都应执行，但必须先C1、后C2、每一步完整val分析；只让达标的较好方向进入预测support部署版。C2必须保留dBZ和support辅助损失，防止中间输出变成只能迎合Stage 1的“伪dBZ”。

### 6.3 直接端到端路线

应同时建立直接预测基线：

\[
Z_{GR}^{sparse},M_{GR},A
\longrightarrow
R_{DPR}^{dense}
\]

串联路线的优点是可解释性强，可以分别评估“GR–DPR校准”和“降水反演”。直接路线不会受中间反射率目标的强制约束，有可能得到更好的最终降水性能。

需要对比：

1. 传统插值 GR + 分层 Z–R；
2. 插值 GR + 神经网络降水反演；
3. GR→DPR→降水的串联模型；
4. 稀疏 GR→密集降水的直接模型；
5. 分阶段预训练后进行端到端微调的模型。

## 7. 针对零膨胀和长尾分布的建模方法

降水数据包含大量零值，且强降水样本很少。如果对所有网格直接使用普通 MSE，模型很容易通过预测平滑的小值降低平均误差，却丢失强降水核心。

### 7.1 两部分输出

建议将预测拆成两个头。

降水发生头：

\[
p=P(R>r_0)
\]

可使用 BCE、加权 BCE 或 Focal Loss。

正降水强度头：

\[
y=\log(1+R)
\]

仅在有效正降水标签上计算 Huber、加权 MAE 或分位数损失。

总损失可写为：

\[
L=\lambda_1L_{occurrence}
+\lambda_2L_{intensity}
+\lambda_3L_{reflectivity}
\]

其中 \(L_{reflectivity}\) 是可选的中间反射率监督。

### 7.2 分层采样与加权

可按降水强度分层：

```text
无降水
0–1 mm/h
1–5 mm/h
5–10 mm/h
10–20 mm/h
>20 mm/h
```

通过分层采样或损失加权提高强降水对训练的贡献，但需同时监测虚警率，避免过度强调强降水而造成大面积假降水。

## 8. 辅助变量与标签泄漏

可候选的辅助输入包括：

- 高度 `z`；
- 温度 `t`；
- 气压 `p`；
- 比湿 `q`；
- GR 有效观测 mask；
- 经纬度；
- 时间、季节或月份；
- 地基雷达距离、方位角和仰角；
- 可在推理时获取的地形或站点信息。

每个输入变量都必须回答：

> 在没有 DPR 标签的实际推理阶段，这个变量是否仍然可获得？

需要谨慎处理：

- `pre_dpr` 只能是标签；
- 如果 `typePrecip` 来自 DPR 反演，则不能作为最终模型输入；
- DPR 质量标志可用于筛选训练标签，但不应作为部署时不可得的输入；
- `cfb` 如果来自 DPR，主要应用于建立有效标签 mask，而非直接提供给最终模型。

来自卫星产品的派生变量可以用于“性能上限实验”，但必须与“可部署模型”的结果分开报告。

## 9. 数据划分和数据泄漏

数据划分的严格程度优先于模型复杂度。

不应随机拆分单个三维网格，也应尽量避免将同一条轨道中相邻切块随机分配到训练集和测试集。相邻网格、相邻切块和同一次卫星过境高度相关，随机拆分会导致结果虚高。

建议以完整 NC 文件、卫星轨道、日期或降水事件为单位划分：

```text
训练集：若干日期/轨道/事件
验证集：未出现过的日期/轨道/事件
测试集：另外的日期/轨道/事件
```

还可以额外设置：

- 同一雷达站、不同时间的测试；
- 从未参与训练的雷达站测试；
- 层云、对流和其他降水类型分组测试；
- 按高度、GR距离和有效观测稀疏度分组测试。

均值、标准差、分位数、归一化参数和分层采样权重只能使用训练集计算。

## 10. 评价指标

不能只使用全局 RMSE，否则一个过度平滑且大部分输出小值的模型也可能得到看似较好的分数。

### 10.1 降水发生

- POD / Recall；
- FAR；
- CSI；
- Precision、F1。

### 10.2 降水强度

- MAE；
- RMSE；
- Bias；
- 相关系数；
- `log1p` 降水空间中的 MAE/RMSE；
- 仅在真实正降水位置上的强度误差。

### 10.3 不同降水阈值

建议在以下阈值上分别评价：

```text
0.1、1、5、10、20、30、50 mm/h
```

这可以判断模型是否仅对弱降水表现良好，以及是否能保留少数强降水样本。

### 10.4 空间结构

- FSS；
- SSIM；
- 不同邻域尺度的降水覆盖率误差；
- 强降水中心的位置和强度误差；
- 水平切片和垂直廓线的定性对比。

### 10.5 分组评价

- 层云降水与对流降水；
- 弱、中等和强降水；
- 不同高度；
- 不同地基雷达距离；
- 不同 GR 有效观测密度。

## 11. 建议的实验实施顺序

### 步骤 0：建立可信的数据协议

- [ ] 生成全数据集文件索引和基本统计；
- [ ] 明确每个变量的缺测、填充和有效值规则；
- [ ] 建立 GR 输入 mask、DPR 标签 mask 和 `cfb` 杂波 mask；
- [ ] 确定按轨道/日期/事件隔离的训练、验证和测试集；
- [ ] 确认所有最终输入在实际推理时可获得。

### 步骤 1：建立基础反演基线

- [ ] 评估师兄现有的分层 Z–R 方法；
- [ ] 建立 DPR反射率→正降水强度的 MLP；
- [ ] 比较原始降水率、`log1p`降水率和不同损失；
- [ ] 建立统一的训练日志、评价表和可视化模板。

### 步骤 2：引入垂直和三维信息

- [ ] 训练垂直廓线一维 CNN；
- [ ] 训练小型 3D CNN/3D U-Net；
- [ ] 通过消融实验比较 `z/p/t/q` 等辅助变量；
- [ ] 确定阶段一的最佳降水反演模块。

### 步骤 3：学习 GR–DPR 观测差异

- [ ] 构建 GR 与 DPR 同时有效的配准样本；
- [ ] 评估不同高度、降水强度和距离下的系统偏差；
- [ ] 建立简单线性校准、分层校准和 MLP 基线；
- [ ] 完成条件 GR→DPR 反射率映射。

### 步骤 4：稀疏到密集

- [ ] 使用现有 `dbz_gr_interp` 建立插值基线；
- [ ] 建立显式输入 GR mask 的稀疏恢复模型；
- [ ] 分别比较“先恢复反射率”和“直接预测降水”；
- [ ] 从空间结构和强降水尾部两方面检查平滑问题。

### 步骤 5：串联与端到端对比

- [x] 串联 GR→DPR 与 DPR→降水模块，完成两端全冻结的完整val上限/oracle/部署评价；
- [ ] 完成`S3-C0-2x2`严格接口归因和统一空间可视化（代码与测试已完成，完整val待运行）；
- [x] 完成C1-O冻结Stage 2训练与完整val归因，结论为未过门槛；
- [x] 正式训练并评价C2-O冻结Stage 1任务感知Stage 2；完整val未过门槛；
- [x] 实现稀疏GR→密集降水的`S3-D0-DirectMultiHead`直接模型、训练/整轨评价/可视化代码；
- [ ] 正式训练并评价D0-H冻结特征探针；
- [ ] 仅在单边部署版有效后比较固定串联和局部联合解冻；
- [ ] 通过中间反射率监督构建多任务模型；
- [ ] 完成全部基线、消融和分组评价。

## 12. 当前优先级

当前不应继续独立搜索Stage 2强回波权重、support阈值或更大网络，而应优先完成：

1. 在三张空闲GPU上正式训练已实现的`S3-D0-H-DirectMultiHead`五轮冻结特征探针；
2. 完整val同时评价oracle/predicted support下的最终rain、辅助dBZ/support、强尾部、dR/dz和逐轨稳定性；
3. 只有D0-H相对F0达到`RMSE改善>=3%`或`r提高>=0.01`且关键分组不退化，才启动D0-D完整decoder+三头联合训练；
4. C1和C2均未过门槛，不扩展C1-P/C2-P，也不启动C3双U-Net联合解冻；
5. 若D0-H无信号，先审计GR共享特征与标签监督域，不通过增加网络复杂度掩盖数据可观测性问题。

Stage 3开发期间仅使用完整38条val轨道，不用test选择冻结方向、损失权重或解冻范围。
