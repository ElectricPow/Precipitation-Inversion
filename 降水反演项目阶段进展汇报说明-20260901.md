# 降水反演项目阶段进展汇报说明

> 汇报日期：2026-09-01  
> 汇报主题：从数据处理、两阶段 3D U-Net 到串联误差定位  
> 配套图片目录：[`outputs/progress_report_20260901/`](./outputs/progress_report_20260901/)  
> 图片生成脚本：[`scripts/build_progress_report_assets.py`](./scripts/build_progress_report_assets.py)  
> 说明：本文面向第一次接触机器学习与本数据集的听众。文中把“已经完成并有完整验证集证据的结论”和“正在运行/计划中的实验”严格分开。

## 1. 一页结论：目前做到哪里了

本项目的最终目标是：仅使用部署时能够获得的地基雷达（GR）稀疏三维反射率及其几何辅助信息，重建尽可能接近星载卫星 DPR 产品的三维降水率。

当前采用两阶段路线：

1. **Stage 1：DPR 反射率 → DPR 降水率。**这一阶段学习卫星产品内部“反射率到降水率”的映射。当前模型在完整 38 轨验证集的可靠正降水体素上达到 `Pearson r=0.8637`、`RMSE=2.2262 mm/h`，说明该映射已经学得较好。
2. **Stage 2：GR 稀疏反射率 → DPR 反射率及其支持域。**这一阶段同时涉及传感器数值域转换、空间偏移、稀疏填补、支持域恢复和强回波长尾。当前较好的第一版模型在 DPR 真实支持域上的反射率达到 `r=0.7020`、`RMSE=5.3085 dBZ`，支持域 `CSI=0.5763`。
3. **两阶段冻结串联后仍明显退化。**真实 DPR 反射率输入 Stage 1 时降水 `r=0.8637`；换成 Stage 2 预测反射率并使用真实支持域时降至 `r=0.4713`；再换成预测支持域时为 `r=0.4538`。因此，目前最主要的瓶颈是 Stage 2 恢复出的 dBZ 值场、强回波尾部和三维结构与 Stage 1 的训练输入域不一致，而不只是一个支持域阈值问题。
4. **当前正在执行 Stage 2-v2 的任务分解审计。**新的思路不再让一个共享主干和两个末端小头“一锅乱炖”地解决所有问题，而是先把直接观测区标定、有限配准、支持域恢复、邻近缺口补全、远离观测区不确定性和强回波恢复逐项拆开、逐项验收。

![总体任务路线](./outputs/progress_report_20260901/figures/01_overall_task_route.png)

## 2. 为什么要拆成两个阶段

原始输入和最终标签之间存在两个性质不同的问题：

- `GR sparse dBZ → DPR dBZ`主要解决“两个传感器如何对齐，以及稀疏空间如何恢复”；
- `DPR dBZ → DPR rain`主要解决“同一个卫星产品内部如何由反射率转换为降水率”。

如果直接从 GR 一步预测降水率，最终误差变大时很难判断究竟是雷达对齐错了、空间补全错了，还是反射率到降水的映射错了。拆成两阶段后，可以分别评价每一个中间任务，并用真实中间量做反事实替换来定位误差。

这条路线的代价是：Stage 1 训练时看到的是真实 DPR 反射率，而部署时看到的是 Stage 2 生成的“近似 DPR 反射率”。即使 Stage 2 的平均 dBZ 指标看起来尚可，只要它把强回波压平、把空间结构移动或生成了不自然的纹理，Stage 1 就可能把这些误差进一步放大。这正是当前已经被验证的主要问题。

## 3. 数据集如何划分

### 3.1 为什么不能随机切 Patch 再划分

同一条卫星轨道内相邻位置高度相关，同一天的多个轨道也可能属于同一个天气过程。如果先把轨道切成很多 Patch 再随机分到训练集和验证集，模型可能在训练时见过验证样本附近的云系，这会造成数据泄漏，使验证指标虚高。

因此项目先按日期组成互斥分组，再在日期组层面平衡文件数、正降水和强降水比例。一个日期组只能属于 train、validation 或 test 中的一个集合。

### 3.2 固定划分结果

| 集合 | 轨道文件数 | 日期组数 | Patch 数 | 用途 |
|---|---:|---:|---:|---|
| Train | 175 | 76 | 2,076 | 更新模型参数、拟合标准化统计 |
| Validation | 38 | 16 | 437 | 选检查点、选 support 阈值、比较消融实验 |
| Test | 41 | 16 | 459 | 最终封板后的一次性泛化评估，不用于调参 |
| 合计 | 254 | 108 | 2,972 | 所有轨道仅出现一次 |

当前 Stage 2-v2 的方案选择仍只使用 38 条 validation 轨道，不能用 test 结果挑模型。

### 3.3 数据为什么必须带 Mask

数据的整体稀疏和长尾特征很强：

- GR 稀疏反射率物理有效体素约占全部网格的 `1.90%`；
- DPR 反射率物理有效体素约占 `3.63%`；
- DPR 降水变量中，有限且非负的标签占约 `98.05%`，但其中 `96.30%` 是真实零降水。

所以“数值为 0”至少可能表示三件不同的事情：真实零降水、标准化后的均值位置，或为了组成张量而填入的缺测占位值。必须用独立 Mask 区分这些语义。

## 4. 一条长度不定的轨道如何变成模型 Batch

原始轨道变量的三维形状为：

```text
(nscan, nray, nz) = (不定长, 49, 60)
```

其中：

- `nscan`是卫星沿轨飞行方向的扫描数，不同轨道约为 85～638；
- `nray=49`是垂直于轨道方向的波束/像元数；
- `nz=60`是高度层，中心高度从 0.125 km 到 14.875 km，层间隔 0.25 km。

神经网络训练需要同一个 Batch 内形状一致，所以项目采用“**非重叠输出核心 + 重叠上下文**”方式：

1. 沿 scan 方向把轨道分成互不重叠的 32-scan 输出核心；
2. 每个核心左右各读取 16-scan 上下文，因此模型输入长度为 `16+32+16=64`；
3. 相邻 Patch 的上下文会重叠，但最终只保留各自中央 32-scan 核心，所以每个真实位置只输出一次；
4. 轨道首尾缺少上下文时填中性占位值，同时 `geometry_mask=0`，不会被当作真实位置；
5. ray 方向从 49 在高端补 15 列到 64，以适配四次水平下采样；高度 60 层完全不补、不池化；
6. 单个样本成为 `(C,64,64,60)`，DataLoader 再堆叠为 `(B,C,64,64,60)`。

![数据划分、Patch 和 Batch](./outputs/progress_report_20260901/figures/02_dataset_split_and_patch.png)

### 4.1 Padding 到底填什么

| 数据类型 | 轨道边界/ray Padding 值 | 为什么安全 |
|---|---:|---|
| 标准化 dBZ、标准化连续变量 | `0.0` | 0 是标准化空间的中性值；配套有效 Mask 为 0 |
| 高度通道 | `0.0` | 仅在伪几何位置使用；`geometry_mask=0` |
| 最近 GR 观测距离 | `1.0` | 表示截断尺度内最远，同时 `gr_value_mask=0` |
| 所有布尔 Mask | `False` | 明确排除伪位置、缺测或不可信监督 |
| 回归标签占位 | `0.0` | 对应 loss Mask 为 0，不进入损失 |

这里最重要的原则是：**Padding 里的 0 不是物理观测值，能否进入模型监督由 Mask 决定。**

### 4.2 标准化发生了什么

- dBZ 按训练集、逐高度层统计均值和标准差，只用物理有效体素拟合；validation/test 只能复用训练统计。
- 有效 dBZ 转换为 `(dBZ-mean[z])/std[z]`；缺测和 Padding 在转换后填 0。
- 高度线性缩放到 `[-1,1]`，让网络明确知道同一回波位于低层还是高层。
- Stage 1 的降水标签先做 `log1p(rain)`，减弱极少数强降水对梯度的支配；输出评价时再用 `expm1` 回到 mm/h。

三卡训练时当前每卡 `B=1`。每张卡独立计算一个 Patch 的前向和梯度，DDP 在每一步同步并平均三个进程的梯度，因此有效全局 Batch 为 3；这不是把同一个张量复制三遍。

## 5. Stage 1：DPR 反射率到降水率

### 5.1 最终选定输入

Stage 1 每个样本有 3 个输入通道：

```text
通道1  dbz_dpr_standardized  逐高度标准化的DPR反射率
通道2  dbz_dpr_valid         DPR反射率是否物理有效，1/0
通道3  height_scaled         高度，线性缩放到[-1,1]

单样本输入 (3,64,64,60)
Batch输入  (B,3,64,64,60)
```

CFB 以下的反射率在最终模型输入中没有被强行清零；但高置信主损失会排除 CFB 以下区域。此前“清零 CFB 以下输入”和“增加 CFB 距离通道”的受控消融都弱于原基线，因此没有进入最终输入契约。

### 5.2 高度保持的 3D U-Net

普通 3D U-Net 会同时压缩 scan、ray 和 height。考虑到高度坐标具有明确物理顺序，当前模型只在水平方向下采样，卷积/池化步长为 `(2,2,1)`：

```text
输入             (B,  3,64,64,60)
Encoder level 1  (B, 16,64,64,60)
Encoder level 2  (B, 32,32,32,60)
Encoder level 3  (B, 64,16,16,60)
Encoder level 4  (B,128, 8, 8,60)
Bottleneck       (B,256, 4, 4,60)
Decoder level 4  (B,128, 8, 8,60)
Decoder level 3  (B, 64,16,16,60)
Decoder level 2  (B, 32,32,32,60)
Decoder level 1  (B, 16,64,64,60)
Rain head        (B,  1,64,64,60)
```

同尺度的编码器特征通过 skip connection 拼接到解码器，使模型恢复细节时仍能利用浅层的局部空间信息。全模型约 202 万参数。

![Stage 1 网络结构](./outputs/progress_report_20260901/figures/03_stage1_architecture.png)

### 5.3 为什么还有一个 T3D 降水类型辅助头

`typePrecip`是卫星产品给出的降水类型标签。部署时无法把卫星标签当输入，但训练时可以把它作为辅助监督，要求模型从反射率三维结构中同时判断层云、对流和其他类型。

最终 T3D 头不做简单高度平均，而是从 `(B,16,64,64,60)` 特征出发，用不同水平/垂直感受野的 3D 卷积分支保留随高度变化的形态，再把高度由 60 压缩到 30、15，最后形成 `(B,3,64,64)` 的三类 logits。这样能够比简单高度池化更好地保留层云和对流廓线随高度弯曲、倾斜和水平偏移的信息。

### 5.4 Stage 1 的 Mask

主要高置信降水损失 Mask 满足：

```text
reliable_loss_mask =
    位于中央输出核心
  AND DPR反射率物理有效
  AND 位于CFB及以上（排除杂波高风险区）
  AND pre_dpr通过正降水质量控制
  AND pre_dpr为有限值
```

因此它不是“所有有限 pre_dpr”，而是可靠的正降水监督。类型分类另用二维 `type_loss_mask`，只选择中央核心内具有合法类别编码的剖面。Padding、halo 和非法类别都不参与分类损失。

### 5.5 Stage 1 损失函数

最终总损失为：

\[
L_{S1}=L_{rain}+0.02L_{dR/dz}+0.01L_{type}.
\]

- `L_rain`：在可靠 Mask 内对 `log1p(rain)` 使用 Smooth-L1。它在小误差附近像均方误差、在异常大误差处更接近绝对误差，训练比纯 MSE 稳定。
- 强度权重：按物理降水率 `<1、1–5、5–10、10–30、>=30 mm/h` 使用 `[1,1,1.5,2,3]` 权重，提升中强降水的梯度份额。
- `L_dR/dz`：先把预测恢复为物理 mm/h，再比较相邻可靠高度层的降水垂直梯度，约束三维廓线变化，而不是只拟合每个体素。
- `L_type`：在合法剖面上做三分类交叉熵。类别标签只在 loss 侧出现，不会泄漏到模型输入。

## 6. Stage 2：GR 稀疏反射率到 DPR 反射率

### 6.1 当前第一版选定输入

当前作为后续审计起点的是 W1.25 四通道模型：

```text
通道1  dbz_gr_sparse_standardized   标准化GR稀疏dBZ
通道2  gr_value_mask                该格点是否有GR物理有效值
通道3  gr_nearest_distance_scaled   同高度层内距最近GR观测的水平距离，截断并缩放到[0,1]
通道4  height_scaled                高度[-1,1]

单样本输入 (4,64,64,60)
Batch输入  (B,4,64,64,60)
```

距离通道完全由部署时可用的 GR Mask 计算，不使用 DPR 标签。它告诉模型当前位置是直接观测附近、仰角间隙附近，还是远离任何 GR 有效点。

### 6.2 共享主干和双输出头

Stage 2 使用与 Stage 1 相同的高度保持 3D U-Net 主干，通道仍为 `16/32/64/128/256`，输出共享特征 `(B,16,64,64,60)`。随后分为：

- `support_head`：输出 `(B,1,64,64,60)` logits，经 sigmoid 得到“该体素存在 DPR 有效回波”的概率；
- `dbz_head`：输出 `(B,1,64,64,60)` 的标准化 DPR dBZ。

全模型约 199 万参数。两个头结构上独立，但它们上游的编码器和解码器完全共享。

![Stage 2 网络结构](./outputs/progress_report_20260901/figures/04_stage2_architecture.png)

### 6.3 Stage 2 最重要的 Mask 到底何时为 1

| 名称 | 为 1 的中文条件 | 用途 |
|---|---|---|
| `geometry_mask` | 这个位置确实来自原轨道，而不是首尾或 ray Padding | 排除伪几何位置 |
| `core_mask` | 位于当前 Patch 唯一负责输出的中央 32-scan 核心，且属于真实 ray | 防止重叠上下文被重复监督/计数 |
| `gr_value_mask` | GR 原数组在此处是有限物理 dBZ，不是 NaN、Mask 或约定哨兵值；负 dBZ 仍可能有效 | 输入通道；区分真实 GR 数值与填 0 |
| `M_support` / `support_loss_mask` | 位于 core，且 `pre_dpr` 是有限、非负标签；真实零降水也在该可信判别域内 | support BCE 的监督范围 |
| `y_support` / `target_support` | 在 `M_support` 内，DPR dBZ 是物理有效值时为 1，否则为 0 | support 二分类标签 |
| `M_dbz` / `regression_mask` | 位于 core，且 DPR dBZ 是物理有效值 | dBZ 回归损失范围；必然对应 `y_support=1` |
| `Q11` | 同一体素 GR 和 DPR dBZ 都物理有效 | 直接重叠区的标定/配准审计 |
| `Q01` | DPR 有效、GR 同体素无有效值 | 空间补全区域 |
| `gap_proxy` | 属于 Q01，但师兄的 GR 插值支持能够到达 | 邻近缺口代理，只作训练分析/评价，不作部署输入 |
| `outside_proxy` | 属于 Q01，连 GR 插值支持也不可达 | 远离观测代理，只作训练分析/评价，不作部署输入 |

![主要 Mask 和监督域](./outputs/progress_report_20260901/figures/05_masks_and_supervision.png)

这里尤其需要区分：`M_support`是“这里的标签足够可信，可以问模型有没有 DPR 回波”，`y_support`才是问题的答案；`M_dbz`则表示“这里确实有 DPR 反射率，可以问数值是多少”。三者不能互换。

### 6.4 一个损失为什么能更新两个输出头

Stage 2 实际不是“只有一种损失”，而是先计算两个子损失，再相加成一个用于反向传播的标量：

\[
L_{S2}=1.0L_{support}+1.0L_{dbz}.
\]

- `L_support`：只在 `M_support=1` 的位置做 BCE，正类权重为 5，缓解回波体素少、背景多的问题；
- `L_dbz`：只在 `M_dbz=1` 的位置做 Smooth-L1；W1.25 按物理 dBZ 分段赋权，`<25、25–35、>=35 dBZ` 的权重为 `[1,1.1,1.25]`。

自动微分会沿计算图寻找每个损失依赖的参数：

- support 头只参与 `L_support`，所以只收到 support 梯度；
- dBZ 头只参与 `L_dbz`，所以只收到 dBZ 梯度；
- 共享 3D U-Net 同时参与两个输出，所以收到两个梯度之和。

如果两个任务需要相反的共享特征，它们的梯度会互相抵消；如果某个损失数值或梯度明显更大，也会压制另一个任务。这是第一版多任务可能早停、且各子任务完成度不足的重要原因之一。

![双头损失与梯度流](./outputs/progress_report_20260901/figures/06_multitask_loss_gradient_flow.png)

## 7. 如何读懂性能指标

| 指标 | 直观含义 | 越好方向 |
|---|---|---|
| MAE | 平均每个有效点差多少 | 越小越好 |
| RMSE | 对大误差惩罚更重，能暴露强降水/强回波失败 | 越小越好 |
| Bias | 平均偏高还是偏低；负值表示整体低估 | 越接近 0 越好 |
| Pearson r | 预测与真值是否一起升降；不保证绝对数值准确 | 越接近 1 越好 |
| CCC | 同时考虑相关性、均值和方差一致性 | 越接近 1 越好 |
| Recall/POD | 真实回波中找回了多少 | 越大越好 |
| Precision | 预测回波中有多少是真的 | 越大越好 |
| FAR | 预测回波中虚警的比例 | 越小越好 |
| CSI | 预测与真实回波支持域的交集/并集 | 越大越好 |
| FSS | 允许一定邻域偏移后，空间覆盖是否相似 | 越大越好 |

不能只看相关系数。例如把所有强回波系统性压低，空间排序可能还在，Pearson r 仍不算很差，但 RMSE、Bias、CCC 和最终降水都会恶化。

## 8. Stage 1 当前性能

最终选定模型为 `stage1_i_g002_t3d` 的 epoch 22 检查点。完整 38 轨 validation 在 `1,195,966` 个可靠正降水体素上的结果为：

| 指标 | 结果 |
|---|---:|
| Pearson r | 0.8637 |
| CCC | 0.8522 |
| MAE | 0.5121 mm/h |
| RMSE | 2.2262 mm/h |
| Bias | +0.0352 mm/h |
| typePrecip accuracy | 0.8355 |
| typePrecip balanced accuracy | 0.8448 |

按降水强度分层后，中强降水仍更困难：`5–10 mm/h` 的 `r=0.3433`，`10–30 mm/h` 的 `r=0.4484`；`>=30 mm/h` 虽然 `r=0.6037`，但 `Bias=-15.14 mm/h`，说明强尾部仍被明显低估。总体指标高，不能掩盖长尾问题。

![Stage 1 性能](./outputs/progress_report_20260901/figures/07_stage1_performance.png)

训练曲线可作为附录：

![Stage 1 训练曲线](./outputs/progress_report_20260901/training/stage1_training_overview.png)

### 8.1 Stage 1 代表轨道图

以下图片来自固定测试轨道的既有诊断，只用于展示空间形态、剖面、分布和高度误差；正式方案选择没有使用这些测试图调参。

![Stage 1 代表性湿轨道](./outputs/progress_report_20260901/orbits/stage1_representative_wet_track.png)

![Stage 1 强降水轨道](./outputs/progress_report_20260901/orbits/stage1_heavy_rain_track.png)

![Stage 1 六轨总体诊断](./outputs/progress_report_20260901/orbits/stage1_six_track_aggregate.png)

## 9. Stage 2 当前第一版性能

当前用于 Stage 2-v2 审计的起点是四通道距离模型 W1.25 的 `best_dbz.pt`，epoch 27。support 阈值 `0.80`由 validation 的 CSI 选择。

### 9.1 完整验证集总体结果

| 任务 | 指标 | 结果 |
|---|---|---:|
| DPR dBZ（真实目标支持域） | Pearson r | 0.7020 |
|  | CCC | 0.6816 |
|  | MAE | 3.8067 dBZ |
|  | RMSE | 5.3085 dBZ |
|  | Bias | -0.1489 dBZ |
| Support | CSI | 0.5763 |
|  | Recall | 0.6840 |
|  | Precision | 0.7854 |
|  | FAR | 0.2146 |
| 邻域结构 | FSS radius 1/2/4 | 0.8289 / 0.8569 / 0.8833 |

![Stage 2 性能](./outputs/progress_report_20260901/figures/08_stage2_performance.png)

### 9.2 为什么总体 r=0.702 仍不能说明 Stage 2 做好了

38 条 validation 共有 `1,496,179` 个 DPR 有效反射率目标体素：

| 区域 | 目标占比 | RMSE | Pearson r | 总 dBZ 平方误差贡献 | Support Recall |
|---|---:|---:|---:|---:|---:|
| Q11 / overlap | 32.49% | 3.940 | 0.841 | 17.90% | 0.946 |
| gap_proxy | 34.41% | 4.657 | 0.779 | 26.48% | 0.871 |
| outside_proxy | 33.10% | 6.881 | 0.428 | 55.61% | 0.232 |

`outside_proxy`只占约三分之一目标，却贡献超过一半 dBZ 平方误差，是最大的空间瓶颈。`DPR>=35 dBZ`约占 `8.13%`，但贡献约 `32.57%` 的 dBZ 平方误差，且 `Bias=-7.37 dBZ、r=0.171`，是最大的强度瓶颈。强回波集合与三个空间区域重叠，所以两类百分比不能直接相加。

![Stage 2 误差预算](./outputs/progress_report_20260901/figures/10_stage2_error_budget.png)

### 9.3 Stage 2 代表验证轨道图

图片显示目标/预测 dBZ 水平汇总、固定 ray 的高度剖面、支持域深度和公共支持域误差。它们用于解释完整验证指标，而不是替代完整 38 轨统计。

![Stage 2 验证轨道 1](./outputs/progress_report_20260901/orbits/stage2_orbit_01_2A.GPM.DPR.V9-20211125.20170503-S070958-E084232.018057.V07A.png)

![Stage 2 验证轨道 2](./outputs/progress_report_20260901/orbits/stage2_orbit_02_2A.GPM.DPR.V9-20211125.20170503-S162529-E175803.018063.V07A.png)

## 10. 串联后发生了什么：2×2 反事实审计

为了分别回答“support 错误有多大影响”和“dBZ 值场错误有多大影响”，固定同一个 Stage 1，在完整 38 轨 validation 上替换真假中间量：

| Stage 1 的输入 | 降水 Pearson r | RMSE (mm/h) | 解释 |
|---|---:|---:|---|
| 真实 DPR dBZ + 真实 support | 0.8637 | 2.2262 | Stage 1 理想输入上限 |
| 真实 DPR dBZ + 预测 support | 0.7083 | 3.1454 | 只引入 support 错误 |
| 预测 DPR dBZ + 真实 support | 0.4713 | 3.9180 | 只引入 dBZ 值场错误 |
| 预测 DPR dBZ + 预测 support | 0.4538 | 3.9884 | 完整可部署冻结串联 |
| 师兄插值 GR + Stage 1 | 0.4280 | 4.0753 | 当前同代码中的插值输入参考 |

关键证据是：真实 dBZ 加预测 support 仍有 `r=0.7083`，而预测 dBZ 加真实 support 只有 `r=0.4713`。所以当前首先应改善 Stage 2 反射率值场和补全方式，同时保留 support 改进；不能把主要问题归因于阈值选择。

![串联误差传播](./outputs/progress_report_20260901/figures/09_cascade_error_propagation.png)

### 10.1 串联轨道可视化

![长轨道方法总览](./outputs/progress_report_20260901/orbits/cascade_long_track_overview.png)

![较强降水轨道方法总览](./outputs/progress_report_20260901/orbits/cascade_wet_track_overview.png)

![W1.25 串联失败细节](./outputs/progress_report_20260901/orbits/cascade_w1p25_failure_detail.png)

从图上可以看到的“向中心集中、整体偏移、强值被压平”等现象必须和统计指标一起解释。单幅图可能受天气过程影响，完整验证集才是结论口径。

## 11. 到目前为止最重要的问题

### 11.1 Stage 2 把太多不同任务混在同一个共享主干里

第一版 Stage 2 同时学习：

```text
GR→DPR传感器数值域标定
+ 局部空间错位
+ DPR支持域恢复
+ 仰角间隙和空间空洞填补
+ 远离观测区的生成
+ 强回波长尾恢复
```

这些任务所需特征和梯度并不相同。两个 `1×1×1` 输出头只能在最后一层分开，前面的绝大多数参数仍被两个目标共同拉动。总体损失下降或早停，并不等于每个子任务都达到可用水平。

### 11.2 可观测区域之间难度差异极大

Q11、gap 和 outside 各约占三分之一，但 outside 的误差贡献达到 `55.6%`、support recall 只有 `0.232`。统一平均损失会让模型倾向输出条件均值，视觉上表现为空间收缩和平滑；对不可充分观测区域，模型还应表达置信度或分位数，而不应假装每个体素都能被确定恢复。

### 11.3 强回波被系统性压缩

Stage 2 的 `>=35 dBZ`负 Bias 为 `-7.37 dBZ`。反射率是对数单位，数 dBZ 的低估传入非线性的降水映射后可能形成很大的降水率差异。W1.25 只带来小幅总体改善，没有根治强尾部。

### 11.4 局部错位存在，但不是唯一答案

固定 `±2` 格局部搜索的 support CSI 从 exact `0.2916`提高到局部 oracle `0.3014`，绝对增益约 `0.0098`。这说明局部偏移真实存在，但当前理论增益仍属中等，不支持立即用复杂的无限制形变网络替代整个路线。

### 11.5 独立两阶段之间存在输入域偏移和误差叠加

Stage 1 只在真实 DPR dBZ 上训练，它对真实域表现很好，却没有义务适应 Stage 2 的偏平滑、偏移或支持域错误。此前冻结一侧、端到端多头、降水主任务等 Stage 3 尝试都没有超过冻结基线，说明不能靠简单联合微调自动修复一个尚未完成的 Stage 2。

### 11.6 与师兄约 `r=0.68` 的结果需统一评价口径

师兄 PPT 中最终模型给出的 Pearson 相关性约为 `0.68`，应作为最低质量预期；但其数据范围、有效 Mask、按轨还是按体素聚合、是否包含零降水等口径尚未完全统一，不能与当前 `r=0.4538`作严格统计差值。下一步需要在同一批轨道和同一评价 Mask 上复现/导出师兄方法，才能形成公平比较。工程上可把同口径 `r>=0.70`设为留有波动裕量的目标。

## 12. 已经做过的关键受控实验，应在汇报中简要提及

不建议把所有实验逐一铺满 PPT，但需要让导师知道当前结论不是只训练了一次得出的：

- Stage 1 比较了 CFB 以下输入清零、CFB 距离通道、输入有效性、强度加权、CFB 弱监督、物理 `dR/dz` 和降水类型辅助任务；最终选择 I+G+T3D。
- Stage 2 比较了 3/4/5 通道、GR 两类 Mask、最近观测距离、局部密度、Fill-45 采样、师兄插值输入以及 W1.5/W1.25 强回波权重；W1.25 是当前第一版的平衡起点，而不是最终封版。
- Stage 3 比较了全冻结串联、只适配 Stage 1、只适配 Stage 2、直接多头和降水主任务；均未解决根本退化，所以已停止盲目联训并返回 Stage 2 做任务分解。

汇报时最好把这些描述成“控制变量得到的排除证据”，而不是简单说某个模型失败。

## 13. 当前正在运行的工作与下一路线

正在运行的是 `S2-R0-DecompositionAudit` 的冻结 Stage 1 分区 Oracle。它不训练新模型，而是逐区域把 Stage 2 的错误中间量替换成真值，回答：

- 如果只修复 Q11、gap 或 outside 的 dBZ，最终降水能恢复多少？
- 如果只修复某一区域的 support，最终降水能恢复多少？
- 强回波真值替换能关闭多少最终 RMSE？

当前报告不能引用尚未跑满 38 轨的中间数字。完整 R0 结果将与已经完成的静态误差预算和局部位移审计合并，决定 R1～R4 的优先顺序。

后续候选路线是：

1. `R1`：人工遮挡 GR 自监督 + Oracle-DPRSparse，先测稀疏几何下的纯空间恢复上限，并比较普通卷积与 mask-aware/partial convolution；
2. `R2`：在 Q11 做低容量、逐高度条件化标定；只有 R0 支持时才加有界局部配准；
3. `R3`：使用独立 support 解码器，并把 10/15/20/30/35 dBZ 支持域作为有序嵌套任务；
4. `R4`：先用 oracle support 学条件 dBZ，再按 Q11/gap/outside 分区归约损失；outside 增加不确定性/分位数输出；
5. `R5`：逐步把 oracle 中间量换成预测量，审计梯度冲突后才考虑 GradNorm 或 PCGrad；
6. `R6`：子任务逐项过门槛后重新串联，先冻结 Stage 1，不让下游网络替 Stage 2 掩盖物理问题。

![当前发现和讨论问题](./outputs/progress_report_20260901/figures/11_findings_and_discussion.png)

## 14. 建议和导师重点讨论的问题

除了用户列出的六项进展，建议汇报中明确加入以下内容：

1. **先说明评价口径。**每个指标是 validation 还是 test、是否只在正降水/真实支持域、Mask 是什么，否则数值无法公平比较。
2. **强调消融带来的决策证据。**说明为什么最终选择当前通道、损失和检查点，也说明哪些直觉方法已经被排除。
3. **把误差传播作为主问题。**Stage 1 单独很好、Stage 2 中间指标尚可、串联很差，这个落差比单看一个总体 r 更重要。
4. **展示可观测区域误差预算。**Q11/gap/outside 能把“数据稀疏”从模糊描述变成可验收的子任务。
5. **展示强回波和中强降水的分层问题。**总体平均指标会被大量弱降水掩盖。
6. **区分已完成、正在运行和计划。**当前 R0 Oracle 未完成前不提前下结论。
7. **讨论与师兄基线的公平复现。**请导师确认最低底线的具体评价数据、Mask 和统计方式。
8. **讨论是否获取更完整的 GR 几何元信息。**不是等待“更好数据”，而是询问站点位置、仰角、波束宽度、时间差和原始覆盖标志能否作为部署可得输入，帮助区分仰角间空洞与真正未覆盖。

建议现场向导师提出四个明确决策问题：

- 是否同意暂缓继续调 Stage 3 联训，把主要精力放在 Stage 2-v2 的逐子任务验收？
- 对 outside 区域，任务目标应当是单一确定值，还是允许输出概率、分位数或不确定性？
- 师兄约 `r=0.68` 的数据和 Mask 口径能否统一，以便建立严格可复现底线？
- 能否取得 GR 处理中的雷达站点、仰角/波束、时间差和原始覆盖状态等元信息？

## 15. 建议 PPT 结构（约 16 页）

| 页码 | 标题 | 推荐图片/内容 | 这一页只讲一个结论 |
|---:|---|---|---|
| 1 | 任务目标与本次汇报结论 | `01_overall_task_route.png` | 两阶段已建成，但瓶颈明确在 Stage 2 |
| 2 | 数据是什么 | GR 稀疏、DPR dBZ、DPR rain 简图 | 输入、标签及缺测不是同一语义 |
| 3 | 数据集划分 | `02_dataset_split_and_patch.png` 左半 | 按日期组隔离，避免相邻天气过程泄漏 |
| 4 | 轨道如何进入模型 | `02_dataset_split_and_patch.png` 全图 | 32 核心 + 16/16 上下文，Batch 形状固定 |
| 5 | Mask 为什么必要 | `05_masks_and_supervision.png` | 填 0 不等于物理零，Mask 决定语义和监督 |
| 6 | Stage 1 架构 | `03_stage1_architecture.png` | 只压缩水平维，高度 60 层全程保留 |
| 7 | Stage 1 损失 | 公式与三项损失 | 主降水 + 垂直梯度 + 类型辅助监督 |
| 8 | Stage 1 结果 | `07_stage1_performance.png` | 总体已较好，但中强/极强降水仍低估 |
| 9 | Stage 1 轨道案例 | 选一张 Stage 1 diagnostics | 展示平面、剖面、分布和低层/强尾部误差 |
| 10 | Stage 2 架构 | `04_stage2_architecture.png` | 共享主干同时预测 support 和 dBZ |
| 11 | 双头如何训练 | `06_multitask_loss_gradient_flow.png` | 两个子损失相加；共享层收梯度之和 |
| 12 | Stage 2 结果 | `08_stage2_performance.png` | 总体 dBZ r=0.702，但 support 与分区不均衡 |
| 13 | Stage 2 错在哪里 | `10_stage2_error_budget.png` | outside 和强回波贡献了不成比例的误差 |
| 14 | 串联后误差放大 | `09_cascade_error_propagation.png` | dBZ 值场错误比 support 阈值更致命 |
| 15 | 轨道失败案例 | `cascade_w1p25_failure_detail.png` | 空间收缩、偏移、强度压缩会传到降水 |
| 16 | 下一步与需要导师决策 | `11_findings_and_discussion.png` | Stage 2-v2 分解路线和四个讨论问题 |

详细轨道图、训练曲线、全部消融表格建议放在附录。现场若导师追问，再从附录展开，避免主线被实验编号淹没。

## 16. 配套素材和证据追踪

- 全部图片索引：[`outputs/progress_report_20260901/figure_manifest.md`](./outputs/progress_report_20260901/figure_manifest.md)
- 每张图的原始结果来源：[`outputs/progress_report_20260901/figure_sources.json`](./outputs/progress_report_20260901/figure_sources.json)
- Stage 1 配置：[`configs/stage1_ablation_i_g002_t3d.yaml`](./configs/stage1_ablation_i_g002_t3d.yaml)
- Stage 2 配置：[`configs/stage2_unet3d_4ch_distance_intensity_w1p25.yaml`](./configs/stage2_unet3d_4ch_distance_intensity_w1p25.yaml)
- 数据处理细节：[`模型数据处理流程与张量形状.md`](./模型数据处理流程与张量形状.md)
- Stage 2-v2 路线：[`第二阶段GR稀疏反射率到DPR反射率实验路线.md`](./第二阶段GR稀疏反射率到DPR反射率实验路线.md)
- Stage 3 历史路线：[`第三阶段串联适配、联合优化与直接反演实验路线.md`](./第三阶段串联适配、联合优化与直接反演实验路线.md)

重新生成汇报图片的 CPU 指令：

```bash
cd /home/koujizhi/projects/precipitation-inversion
source .venv/bin/activate
python scripts/build_progress_report_assets.py \
  --output-dir outputs/progress_report_20260901 \
  --dpi 150 \
  --overwrite
```

该脚本只读取已经完成的 JSON/NPZ/PNG 结果，不加载模型、不占用 GPU，也不会修改当前正在运行的实验目录。
