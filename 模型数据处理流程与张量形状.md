# 模型数据处理流程、变量与张量形状

本文档记录截至2026-09-01项目代码实际实现的数据接口与实验状态，重点回答：当前能读取什么、读出后是什么形状、数值经过了什么处理，以及已测/待测方法分别改变了哪一环。Stage 2与Stage 1的全冻结串联已经完成，第21节进一步预注册Stage 3单边冻结和可导接口契约。

需要先区分以下接口层次：

1. `read_nc_sample` 是通用NC读取层，可以选择读取原文件中的任意变量；
2. `Stage1IntensityDataset` 是早期单体素基线接口，生成 `(5,)` 特征；
3. `Stage1PatchDataset` 是当前第一版3D U-Net接口，生成固定三维核心+halo窗口。
4. `stage2_masks.py` 是阶段二原始存储语义层，在通用读取器合并缺测前保留原生 mask/NaN、`-9999.9`和物理 dBZ 三种状态。
5. `Stage2PatchDataset` 产生可配置的3/4/5通道GR-only输入、DPR支持域/dBZ双目标和独立mask；E3还返回只供损失使用的dBZ强度权重。
6. `Stage2UNet3D` 接收 `(B,C,64,64,60)` 并输出同尺寸支持域logits和标准化DPR dBZ；当前主模型与E3均为`C=4`。

“读取器支持某变量”不等于“该变量已经进入模型”。GR变量不进入阶段一Dataset；阶段二数据、分层采样、双头模型、双mask损失、AMP/DDP训练、整轨评价、验证集阈值选择和预测可视化已实现。阶段一第一版3D U-Net使用DPR反射率、其有效性mask和高度坐标，不使用 `p/t/q`。

## 1. 数据处理总流程

下图是已实现的阶段一流程；阶段二不能直接套用其“先将缺测合并为NaN”的读取步骤，详见第17节。

```text
原始NetCDF文件
  │
  ├─ 按轨道文件读取 z/dbz_dpr/pre_dpr/cfb
  │
  ├─ masked/_FillValue/NaN/Inf/≤-9990 → NaN，转为float32
  │
  ├─ 生成DPR有效、CFB杂波和正降水质量控制mask
  │
  ├─ 按日期隔离为train/val/test
  │
  ├─ 只用train：dbz_dpr自身有效值拟合输入均值/标准差
  │              pre_positive_qc只决定可靠标签与主loss
  │
  ├─ 沿nscan建立互不重叠的32扫描线核心索引
  │
  ├─ 每个核心左右各取16扫描线上下文，轨道外位置先补缺测
  │
  ├─ dbz_dpr逐高度标准化；pre_dpr在选定监督位置做log1p
  │
  ├─ (64,49,60)只在nray高端补齐为(64,64,60)，高度不padding
  │
  ├─ E0/E1/E0-N系列样本：(3,64,64,60)；E2：(4,64,64,60)
  │
  └─ 模型输入：(B,C,64,64,60) → 高度保持3D U-Net
```

原始NC始终只读。索引和归一化统计是可再生的元数据，不会改写源文件。

## 2. 原始网格和基本维度

每个NC文件表示一段已经配准到GPM DPR条带网格上的数据。

| 维度 | 含义 | 当前数据集大小 |
|---|---|---:|
| `nscan` | 沿卫星飞行方向的扫描线 | 每文件不同，85–638，中位数345.5 |
| `nray` | 每条扫描线的横轨波束 | 固定49 |
| `z` | 垂直高度层 | 固定60 |

高度坐标为：

```text
z = [0.125, 0.375, ..., 14.875] km
```

相邻高度层间隔0.25 km。示例文件：

```text
2A.GPM.DPR.V9-20211125.20170501-S071945-E085219.018026.V07A.nc
```

其维度为：

```text
(nscan, nray, z) = (596, 49, 60)
```

因此不能在模型中写死 `nscan=596`；不同文件的沿轨长度不同。

### 2.1 全文件和扫描切片的形状

若三维变量完整读取，其形状是：

```text
(N, 49, 60)，N为当前文件的nscan
```

若使用：

```python
scan_slice=slice(100, 132)
```

则所有带 `nscan` 维度的变量保持对齐地变为：

```text
三维变量：(32, 49, 60)
二维变量：(32, 49)
一维扫描变量：(32,)
高度z：(60,)，不裁剪
```

当前读取器只接受步长为1的连续扫描切片，避免不同变量发生错位。

## 3. 当前支持读取的变量

`read_nc_sample(path, variables=(...))` 只把显式请求的变量加载进内存。只要变量确实存在于NC文件中，就可以按名称读取；以下23个已知变量还会额外检查维度顺序，防止转置数据静默进入训练。

### 3.1 三维变量 `(nscan, nray, z)`

示例文件中的形状均为 `(596,49,60)`。

| 变量 | 来源/含义 | 单位 | 默认读取 | 当前阶段一模型 |
|---|---|---|---|---|
| `p` | 气压背景场 | 变量文档为hPa；NC属性未声明 | 是 | 第一版3D U-Net暂不输入；旧体素接口使用 |
| `t` | 温度背景场 | 变量文档为K；NC属性未声明 | 是 | 第一版3D U-Net暂不输入；旧体素接口使用 |
| `q` | 比湿背景场 | 变量文档为kg/kg；NC属性未声明 | 是 | 第一版3D U-Net暂不输入；旧体素接口使用 |
| `dbz_gr_sparse` | GR稀疏平均反射率 | dBZ | 是 | 尚未使用 |
| `dbz_gr_sparse_min` | GR格点内最小反射率 | dBZ | 否 | 尚未使用 |
| `dbz_gr_sparse_max` | GR格点内最大反射率 | dBZ | 否 | 尚未使用 |
| `dbz_gr_interp` | GR空间插值反射率 | dBZ | 是 | 尚未使用，传统基线候选 |
| `sw_gr_sparse` | GR稀疏平均谱宽 | m/s | 否 | 尚未使用 |
| `sw_gr_sparse_min` | GR格点内最小谱宽 | m/s | 否 | 尚未使用 |
| `sw_gr_sparse_max` | GR格点内最大谱宽 | m/s | 否 | 尚未使用 |
| `dbz_dpr` | 星载DPR Ku反射率 | dBZ | 是 | 输入 |
| `pre_dpr` | 星载DPR三维降水率 | mm/h | 是 | 标签 |

这里的“默认读取”指不传 `variables` 参数时的通用读取器行为，不代表模型一定使用。

### 3.2 二维变量 `(nscan, nray)`

示例文件中的形状均为 `(596,49)`。

| 变量 | 含义 | 默认读取 | 当前阶段一用途 |
|---|---|---|---|
| `lat` | 每根廓线的纬度 | 是 | 不进入特征 |
| `lon` | 每根廓线的经度 | 是 | 不进入特征 |
| `cfb` | clutter-free bottom的高度层索引 | 是 | 只用于标签质量控制 |
| `nsrr_dpr` | DPR近地面降水率 | 否 | 未使用 |
| `srr_dpr` | DPR地表降水率 | 否 | 未使用 |
| `binRealSurface` | DPR真实地表高度层索引 | 否 | 未使用 |
| `typePrecip` | 降水类型 | 是 | 未作为输入，可用于分组评价 |
| `flagPrecip` | DPR降水检测方法与置信标志 | 是 | 未作为输入，可用于质量分析 |

`typePrecip` 的已知编码包括：层云1、对流2、其它3、无降水-1111、缺测-9999。它来自DPR产品，未来最终GR部署模型不能直接把它作为输入，否则会发生卫星信息泄漏。

### 3.3 一维变量

| 变量 | 原始维度 | 示例形状 | 含义 | 默认读取 |
|---|---|---:|---|---|
| `z` | `(z,)` | `(60,)` | 高度，km | 是 |
| `scan_id` | `(nscan,)` | `(596,)` | DPR扫描编号 | 否 |
| `time` | `(nscan,)` | `(596,)` | 每条扫描线的时间 | 否 |

### 3.4 默认变量集合

不传 `variables` 时读取以下13个变量：

```text
z, lat, lon,
dbz_gr_sparse, dbz_gr_interp,
dbz_dpr, pre_dpr, cfb,
typePrecip, flagPrecip,
p, t, q
```

也可以显式读取非默认变量：

```python
sample = read_nc_sample(
    nc_path,
    variables=("z", "dbz_gr_sparse_min", "sw_gr_sparse", "scan_id", "time"),
)
```

读取结果位于 `sample.variables[name]`；原始/返回形状、数据类型、单位和 `long_name` 位于 `sample.metadata[name]`。

## 4. 原始数值进入内存时的统一处理

> 本节描述通用 `read_nc_sample` 和阶段一口径。阶段二审计会在这一步之前直接分类 NetCDF masked array，以免丢失有限 `-9999.9` 与原生 mask/NaN 的区别。

### 4.1 数据类型

读取器默认将所有请求变量转换为NumPy `float32`：

```text
NetCDF float64/float32/int16/int32/int64 → NumPy float32
```

将整数标志也转换为浮点数，是为了能用 `NaN` 统一表达缺测。若读取分类变量，后续使用前需要在有效位置再转换或解释编码。

### 4.2 缺测值

以下值统一转换为 `NaN`：

- NetCDF masked值和 `_FillValue`；
- 原始 `NaN`、正负无穷；
- 小于等于 `-9990` 的旧式填充值，例如 `-9999`。

以下值不会被当作缺测：

- 弱的负反射率，例如 `-5 dBZ`、`-30 dBZ`；
- `typePrecip=-1111`，它表示有效的“无降水”类别；
- `pre_dpr=0`，它表示有效的零降水标签。

当前代码没有在读取阶段执行以下操作：

- 不把缺测填成0；
- 不把负dBZ裁剪成0；
- 不应用9 dBZ阈值；
- 不对GR插值；
- 不对变量做全局归一化。

这些行为很重要，因为“没有观测”和“观测到零/弱回波”具有不同物理含义。

### 4.3 结构检查

读取后会检查：

- 文件必须具有 `nscan/nray/z` 三个维度；
- 已知变量的维度名称和顺序必须符合上表；
- 返回数组形状必须与裁剪后的维度一致；
- `z` 必须全部有限并严格递增。

## 5. 自动生成的mask

下表是通用/阶段一mask。其中 `gr_sparse_observed` 使用“有限且不是 `-9999.9`”口径；它不保留阶段二的三态存储信息。

只有请求了对应源变量，相关mask才会生成。所有mask均为NumPy `bool`。

| mask | 形状 | 所需变量 | 含义 |
|---|---:|---|---|
| `gr_sparse_observed` | `(N,49,60)` | `dbz_gr_sparse` | GR稀疏反射率有数值 |
| `gr_sparse_profile_observed` | `(N,49)` | 同上 | 垂直廓线至少一层有GR值 |
| `gr_interp_observed` | `(N,49,60)` | `dbz_gr_interp` | GR插值反射率有数值 |
| `gr_interp_profile_observed` | `(N,49)` | 同上 | 廓线至少一层有插值值 |
| `dpr_reflectivity_valid` | `(N,49,60)` | `dbz_dpr` | DPR反射率有数值 |
| `dpr_reflectivity_profile_valid` | `(N,49)` | 同上 | 廓线至少一层有DPR反射率 |
| `cfb_profile_valid` | `(N,49)` | `cfb,z` | CFB是 `[0,60)` 内的整数索引 |
| `cfb_clutter` | `(N,49,60)` | `cfb,z` | 位于CFB以下的杂波区域 |
| `pre_valid_native` | `(N,49,60)` | `pre_dpr` | 原始有限且非负降水标签 |
| `pre_zero_native` | `(N,49,60)` | `pre_dpr` | 原始有效且等于0 |
| `pre_positive_native` | `(N,49,60)` | `pre_dpr` | 原始有效且大于0 |
| `pre_valid_qc` | `(N,49,60)` | `pre_dpr,cfb,z` | 原始有效并剔除CFB以下区域 |
| `pre_zero_qc` | `(N,49,60)` | 同上 | 质控后有效且等于0 |
| `pre_positive_qc` | `(N,49,60)` | 同上 | 质控后有效且大于0 |
| `gr_dpr_overlap` | `(N,49,60)` | GR稀疏和DPR反射率 | 两类反射率同时有效 |

CFB杂波判定严格使用：

```text
z[level] < z[cfb[scan, ray]]
```

所以CFB指向的高度层本身被保留，只剔除更低层。无效CFB不会擅自把整根廓线标成杂波，而由 `cfb_profile_valid` 单独暴露，便于审计。

示例文件中，质控前有95,263个正降水体素，剔除CFB以下区域后剩75,355个。

## 6. 数据划分与防止泄漏

当前254个文件按完整日期分组划分，同一天的文件不会跨集合：

| split | 文件数 | 阶段一正降水体素数 |
|---|---:|---:|
| train | 175 | 5,481,557 |
| val | 38 | 1,195,966 |
| test | 41 | 1,143,592 |

归一化统计只使用175个训练文件；验证集和测试集只应用训练集统计，不参与拟合。当前3D实验使用 `stage1_dbz_valid.json`：输入统计只选中每个变量自身的 finite/non-fill 值，标签质控不反向删除可作上下文的反射率。该文件的 `dbz_dpr` 共6,822,356个训练输入，60层全部有统计；0.125/0.375 km两层分别有131,972/172,153个值。

可靠标签仍是 `pre_positive_qc`，训练集共5,481,557个，最低两层仍为0。同一JSON的 `height_loss_weight_reference` 保存这套独立标签计数，防止将“输入频率”错用为“标签高度权重”。旧 `stage1_positive_qc.json` 仅用于复现E0/E1/E2。

## 7. 保留的单体素Dataset是什么数据形式

本节至第10节描述的是早期 `Stage1IntensityDataset`，用于保留MLP对照实验，
不是当前第一版3D U-Net的数据入口。当前3D流程见第11节。

### 7.1 当前任务定义

当前Dataset解决的是：

```text
已知DPR在该体素检测到了降水
              ↓
根据DPR反射率和气象背景场估计正降水强度
```

它不是“全空间降水发生检测”，也不是“地基雷达稀疏到三维稠密降水图”。这是阶段一中最简单的单体素MLP基线。

### 7.2 一个体素进入索引的条件

体素必须同时满足：

```text
pre_positive_qc
AND dbz_dpr有效
AND dbz_dpr/p/t/q全部有限
```

即：

```text
pre_dpr > 0
AND 不在CFB以下
AND 当前四个输入均可用
```

真实数据的三个split中，没有正降水候选因 `dbz_dpr/p/t/q` 缺失而被额外剔除。

### 7.3 索引形式

索引不是复制后的模型数据，而是一个一维结构化NumPy数组：

```text
shape = (样本数,)
dtype = [file_id:uint16, scan:uint16, ray:uint8, level:uint8]
每条记录 = 6 bytes
```

例如训练索引形状为：

```text
(5_481_557,)
```

索引只回答“去哪个文件的哪个体素取值”。实际变量仍保存在原始NC中，Dataset通过内存映射读取索引，并按需加载NC文件。

`file_id` 是当前split内部的文件编号；`sample_index` 是当前split索引中的位置，它们都不是物理量。

### 7.4 Dataset实际从NC加载的变量

当某个文件第一次被访问时，Dataset完整加载：

```text
z, dbz_dpr, p, t, q, pre_dpr
```

其中5个三维变量的形状均为 `(N,49,60)`。默认每个DataLoader worker缓存1个已解码文件，文件切换时淘汰旧文件；不会长期保持NetCDF文件句柄打开。

以 `(596,49,60)` 文件为例，一个float32三维变量约6.68 MiB，5个三维变量约33.4 MiB。最大638扫描文件的5个变量约35.8 MiB。因此总内存还要乘以DataLoader worker数和DDP进程数，正式训练时不能盲目增加worker。

## 8. 单体素接口的输入特征和标签变换

### 8.1 逐高度标准化

对每个输入变量、每个高度层分别计算训练集统计：

\[
x' = \frac{x-\mu_{variable,level}}{\sigma_{variable,level}}
\]

统计范围只包括 `train + pre_positive_qc`，并按扫描块在线累计，不把全部数据同时放进内存。

四个变量各自拥有60组均值和标准差。0.125 km和0.375 km两层没有质控后的正降水训练样本，因此这两层统计量为 `null`；当前实际索引只覆盖高度层2–59，即0.625–14.875 km。

作为直观例子，训练集0.625 km层的统计为：

| 变量 | 均值 | 标准差 |
|---|---:|---:|
| `dbz_dpr` | 31.8723 dBZ | 8.5112 dBZ |
| `p` | 937.6193 | 3.3642 |
| `t` | 294.7598 | 2.6942 |
| `q` | 0.0159305 | 0.0027329 |

不同高度的统计值不同，不能使用这一行代替完整的60层JSON统计。

### 8.2 高度缩放

若 `include_height=True`（默认），高度使用固定物理范围缩放到 `[-1,1]`：

\[
z' = 2\frac{z-z_{min}}{z_{max}-z_{min}}-1
\]

其中 `z_min=0.125 km`，`z_max=14.875 km`。高度不是使用训练集均值/标准差归一化。

### 8.3 降水标签变换

模型回归标签为：

\[
y=\log(1+R)
\]

其中 `R=pre_dpr`，单位mm/h。预测转回物理降水率时使用：

\[
\hat R=\exp(\hat y)-1
\]

`log1p`压缩强降水长尾，使少量极端值不至于完全主导普通平方误差。Dataset同时返回原始 `rain_rate`，便于直接计算mm/h空间的指标。

### 8.4 单体素接口没有执行的模型前处理

阶段一Dataset当前没有：

- 对反射率应用9 dBZ裁剪；
- 对输入NaN做数值填充，因为索引已经要求四个输入全部有限；
- 加入缺测mask通道；
- 对标签截断上限；
- 对强降水样本加权；
- 对降水强度分层重采样；
- 使用空间邻域、整根垂直廓线或三维patch。

## 9. 单体素接口的样本和batch形状

### 9.1 `dataset[index]`

默认 `include_height=True` 时返回：

| 字段 | PyTorch dtype | 形状 | 含义 |
|---|---|---:|---|
| `features` | `float32` | `(5,)` | `[dbz_dpr', p', t', q', height_scaled]` |
| `target` | `float32` | `()` | `log1p(pre_dpr)` 标量 |
| `rain_rate` | `float32` | `()` | 原始mm/h标量 |
| `sample_index` | `int64` | `()` | split内样本索引 |
| `file_id` | `int64` | `()` | split内文件编号 |
| `scan` | `int64` | `()` | 沿轨扫描索引 |
| `ray` | `int64` | `()` | 横轨波束索引 |
| `level` | `int64` | `()` | 高度层索引 |

若 `include_height=False`，只有 `features` 变为 `(4,)`。

注意：一个Dataset样本是一个体素，不是 `(49,60)` 廓线切片，也不是 `(N,49,60)` 三维图。

### 9.2 DataLoader batch

batch size为 `B` 时，默认整理结果为：

| 字段 | 形状 |
|---|---:|
| `features` | `(B,5)` |
| `target` | `(B,)` |
| `rain_rate` | `(B,)` |
| `sample_index/file_id/scan/ray/level` | 各为 `(B,)` |

当前真实冒烟测试使用 `B=256`，得到：

```text
features:  (256, 5)
target:    (256,)
rain_rate: (256,)
```

所以接下来单体素MLP的输入维数应是5，输出维数应是1。

## 10. 单体素接口的批次洗牌和文件缓存

训练索引按文件连续存储。不能对548万个索引直接做完全全局随机访问，否则每个worker会频繁加载不同NC文件。

`FileBlockBatchSampler` 执行：

```text
打乱文件顺序
  → 打乱该文件内的连续块顺序
  → 打乱块内样本
  → 切成batch
```

它保证一个batch不跨NC文件。训练循环每个epoch必须调用：

```python
batch_sampler.set_epoch(epoch)
```

当前建议起始配置为：

```text
batch_size = 256
block_size = 4096 = 16 batches
cache_size = 1 file/worker
```

在训练集上设置 `drop_last=True` 时，每个epoch有21,327个batch。

采样器支持DDP：各rank获得互不重叠的全局batch；默认最多舍弃 `world_size-1` 个batch，使所有rank步数一致，避免梯度同步挂起。

## 11. 当前3D U-Net：从原始数值到模型batch的完整流程

本节按照 `Stage1PatchDataset.__getitem__` 的真实执行顺序说明每一步。
除特别注明外，NumPy数组的轴顺序都是：

```text
(D,H,W) = (nscan,nray,z)
```

转成PyTorch张量后才在最前面增加通道轴 `C`，DataLoader再增加batch轴 `B`：

```text
(D,H,W) → (C,D,H,W) → (B,C,D,H,W)
```

### 11.1 第一步：Patch索引决定读取哪个核心

索引中的每条记录包含：

```text
file_id, core_start, core_length, input_count, positive_count
```

对一个具有 `N` 条扫描线的轨道，核心依次为：

```text
[0,32), [32,64), [64,96), ...
```

这些核心互不重叠，并且恰好覆盖 `[0,N)`。最后一个核心不足32条时，
`core_length` 保存其真实长度。例如 `N=70` 时为：

```text
core_start  = 0, 32, 64
core_length = 32, 32, 6
```

索引只保存位置和统计数，不复制反射率或降水值。`positive_only=True` 只会过滤
`positive_count=0` 的核心，不会更改保留下来的任何数值。完整轨道推理必须设置
`positive_only=False`，否则被过滤核心会造成扫描线缺口。

### 11.2 第二步：从NetCDF读取原始物理量

第一次访问某个文件时读取：

| 变量 | 源形状 | 转换后的dtype | 此时的数值处理 |
|---|---:|---|---|
| `z` | `(60,)` | `float32` | 保留km高度值 |
| `dbz_dpr` | `(N,49,60)` | `float32` | 保留有效dBZ；缺测统一为`NaN` |
| `pre_dpr` | `(N,49,60)` | `float32` | 保留mm/h；缺测统一为`NaN` |
| `cfb` | `(N,49)` | `float32` | 保留高度层索引；缺测统一为`NaN` |

这里的缺测包括masked、`_FillValue`、`NaN/Inf`和不大于 `-9990` 的旧填充值。
读取时不会把弱的负dBZ、有效的 `pre_dpr=0` 或有效的 `dbz_dpr=0 dBZ`
当作缺测，也不会执行反射率阈值裁剪。

### 11.3 第三步：由原始值生成布尔mask

加载完整轨道后生成以下形状均为 `(N,49,60)` 的布尔数组：

```text
dpr_valid = isfinite(dbz_dpr)
clutter   = z[level] < z[cfb[scan,ray]]
positive  = isfinite(pre_dpr) AND pre_dpr >= 0 AND pre_dpr > 0 AND NOT clutter
```

其中：

- `dpr_valid=True` 仅表示该位置存在DPR反射率数值；负dBZ仍然可以为True；
- `clutter=True` 表示该高度层位于CFB以下，CFB所指向的层本身保留；
- `positive` 就是 `pre_positive_qc`，只保留CFB以上严格大于0 mm/h的标签；
- 有效的 `pre_dpr=0` 属于零降水，但当前条件强度任务不把它放进loss。

mask本身不修改源数组，只决定后面哪些值可以进入输入、标签和损失。

### 11.4 第四步：围绕核心提取64条扫描线的上下文窗口

设核心起点为 `s`，则期望读取的窗口是：

```text
[s-16, s+48)，共64条扫描线
```

在局部窗口中：

```text
local scan 0:15   = 左侧halo
local scan 16:47  = 长度最多32的输出核心
local scan 48:63  = 右侧halo
```

源轨道有数据的部分从 `(N,49,60)` 复制为固定 `(64,49,60)`。超出轨道首尾的
部分没有对应物理观测，按变量类型进行第一次填充：

| 局部数组 | 轨道外填充值 | 原因 |
|---|---:|---|
| `dbz_window` | `NaN` | 保持“缺测”语义，等待标准化器处理 |
| `rain_window` | `NaN` | 不允许轨道外位置成为有效标签 |
| `valid_window` | `False` | 没有DPR观测 |
| `clutter_window` | `False` | 轨道外没有可判定的CFB杂波 |
| `positive_window` | `False` | 轨道外没有正降水标签 |

例如首个核心 `s=0` 的窗口理论范围是 `[-16,48)`：局部扫描线0—15填缺测，
原轨道扫描线0放在局部扫描线16。最后一个短核心的右侧同样补缺测。

halo中的真实反射率会作为模型上下文输入，但halo永远不属于当前Patch的loss。
相邻Patch会把该区域作为自己的核心，因此每条真实扫描线只负责一次输出。

### 11.5 第五步：DPR反射率逐高度标准化并显式保留有效性

`dbz_window` 的每个高度层使用训练集拟合的独立统计量：

\[
x'_{d,r,l}=\frac{x_{d,r,l}-\mu_l}{\sigma_l}
\]

当前E0-N及后续实验使用 `stage1_dbz_valid.json`。每层均值和标准差由训练集该层所有原生有效 `dbz_dpr` 拟合，不要求 `pre_dpr>0`，也不因该位置处于CFB以下而删除输入。验证、测试和整轨推理都仅应用这份训练集统计。

这一改动只改变输入标准化的拟合范围，不改变 `pre_positive_qc`、标签数值或主loss mask。旧E0/E1/E2使用 `stage1_positive_qc.json`，因最低两层没有可靠正降水标签，这两层的反射率统计也为空，有效输入被连带屏蔽；新统计修复了这一耦合。

有效输入mask实际为：

```text
effective_input
  = isfinite(dbz_window)
  AND valid_window
  AND 该高度层mean/std有限
  AND std > 1e-6
```

标准化前后形状不变：

```text
dbz_window:      (64,49,60)，单位dBZ，包含NaN
normalized_dbz:  (64,49,60)，无单位，全部为有限float32
effective_input: (64,49,60)，bool
```

只在 `effective_input=True` 处计算并保留标准化结果。其余位置在标准化完成后填 `0.0`，同时 `effective_input=False`。新统计下60层都有可用参数，因此最低两层的原生有效DPR反射率不再被输入管线丢弃。

一个直观例子：新统计中0.625 km层训练均值约24.8092 dBZ、标准差约7.8164 dBZ：

```text
原始24.8092 dBZ → 标准化约0.0，mask=True
原始32.6256 dBZ → 标准化约1.0，mask=True
原始NaN          → 填0.0，       mask=False
```

因此标准化反射率通道里的 `0.0` 既可能表示“有效且刚好等于该高度均值”，
也可能是“缺测后的中性填充值”。模型必须结合第二个有效性通道区分二者。

第三个通道由原始高度坐标直接计算，不使用训练集统计：

\[
z'=2\frac{z-z_{min}}{z_{max}-z_{min}}-1
\]

60层高度从低到高映射到 `[-1,1]`，再广播为 `(64,49,60)`。真实轨道网格即使
DPR反射率缺测也保留高度值；超出轨道首尾的位置和之后增加的横轨padding填 `0.0`。
高度通道是几何坐标而不是额外气象观测，帮助共享卷积核区分同一标准化反射率位于
低层、中层还是高层。

### 11.6 第六步：构造核心、输出和损失mask

先建立几何核心mask，形状仍为 `(64,49,60)`：

```text
core_mask[16 : 16+core_length, :, :] = True
其余位置 = False
```

随后先构造历史基线不变的可靠监督：

```text
reliable_output_mask = core_mask AND effective_input AND NOT clutter_window

reliable_loss_mask = reliable_output_mask
                     AND pre_positive_qc
                     AND isfinite(pre_dpr)
```

待测的W方法另外只选CFB下方第1、2个高度层，且要求原生DPR反射率有效、`pre_dpr>0` 且位于核心：

```text
weak_loss_mask = native positive AND native DPR valid
                 AND relative_cfb_level in {-1,-2}
loss_mask       = reliable_loss_mask OR weak_loss_mask

weak_output_mask = core AND native DPR valid AND valid CFB geometry
                   AND relative_cfb_level in configured positive-weight layers
output_mask      = reliable_output_mask OR weak_output_mask
```

E0/E1/E2/E0-N/E0-N-I的 `weak_loss_mask` 全为False，因此 `loss_mask == reliable_loss_mask`。E0-N-W/E0-N-IW才将近CFB的低置信标签以小权重加入损失，它们与真缺测仍由显式mask区分。特别地，`weak_output_mask` 不查看 `pre_dpr`或是否正降水；改变真实标签只会改变弱监督，不会改变整轨推理在哪些格点保留预测，因而避免目标泄漏。

各mask用途如下：

| mask | 排除的内容 | 用途 |
|---|---|---|
| `core_mask` | halo、轨道首尾补位 | 表示当前Patch负责的几何区域 |
| `reliable_loss_mask` | 再排除零降水、无效标签和CFB以下 | 主验证指标和 `best.pt` 的唯一支持域 |
| `weak_loss_mask` | 只保留已配置的CFB下近邻原生正标签 | 可选弱监督，不进入主指标 |
| `loss_mask` | 可靠监督与弱监督并集 | 实际参与loss的位置 |
| `output_mask` | 可靠输出支持域+配置的近CFB几何支持域 | 整轨重建时可保留的位置，与标签无关 |
| `native_positive_mask` | 只限核心且原生 `pre_dpr>0` | 独立CFB以下诊断，不是训练mask |

### 11.7 第七步：标签做log1p并生成独立权重

首先创建全0的 `target`，形状 `(64,49,60)`。只在 `loss_mask=True` 的位置写入：

\[
target=\log(1+pre\_dpr)
\]

例如：

```text
pre_dpr = 1 mm/h  → target = log(2)  ≈ 0.6931
pre_dpr = 9 mm/h  → target = log(10) ≈ 2.3026
pre_dpr = 0 mm/h  → 当前loss_mask=False，target保持填充值0.0
pre_dpr = NaN     → loss_mask=False，target保持填充值0.0
```

所以不能只查看 `target==0` 就断言原始标签是0 mm/h：它更常表示“该位置不参与
当前loss”。训练和评价必须始终把 `target` 与 `loss_mask` 配对使用。

`loss_weights` 与 `target` 同形状，loss外填0。每个loss体素的最终权重是三个独立因子的乘积：

\[
w_i=w_{height,i}\,w_{intensity,i}\,w_{quality,i}
\]

E0-N系列暂不测高度加权，所以 `w_height=1`。具体变体为：

| 方法 | 强度权重 `w_intensity` | CFB质量权重 `w_quality` |
|---|---|---|
| E0-N | 全为1 | 可靠区全为1，无弱监督 |
| E0-N-I | `<1/1–5/5–10/10–30/≥30` 对应 `1/1/1.5/2/3` | 可靠区全为1，无弱监督 |
| E0-N-W | 全为1 | CFB下第1/2层为 `0.1/0.05` |
| E0-N-IW | 与I相同 | 与W相同 |

当前 `MaskedSmoothL1Loss(beta=0.2)` 使用权重和而不是体素数做分母：

\[
L=\frac{\sum_i m_iw_i\,\operatorname{SmoothL1}(\hat y_i,y_i)}
        {\sum_i m_iw_i}
\]

公式中的 `i` 指当前进程当前local batch内的体素。正式配置每卡 `batch_size=1`，因此每个Patch先独立按权重和归一，DDP再对各rank的标量loss/梯度等权平均。它与旧E0的“每Patch等权”优化语义一致，但不是把当前全部GPU的体素放进一个全局分母。日志里的epoch loss会再按各batch权重和汇总，所以“优化时每Patch等权”和“报告时按体素权重汇总”需要区分。

主评价始终使用 `reliable_loss_mask`，因此W方法不会通过扩大验证支持域获得假性优势。
日志中 `valid_voxels` 统计实际 `loss_mask`，所以W实验会包含弱监督体素；主指标样本数应查看 `metrics.rain.all.count`，它始终只计数 `reliable_loss_mask`。加权实验的loss数值也不宜与未加权loss横向排名，应以相同可靠mask上的物理空间指标判定。

`I+G-0.02`完整保留以上I主项，并新增物理垂直梯度辅助项。首先只在相邻两端都可靠的位置构造pair mask：

```text
reliable_loss_mask: (B,1,64,64,60)
pair_mask = mask[...,1:] & mask[...,:-1]
pair_mask:          (B,1,64,64,59)
```

模型和标签的`log1p`值先以float32转换回非负物理雨强，再使用真实高度间隔：

\[
R=\operatorname{expm1}(\max(y,0)),\qquad
G_k=\frac{R_{k+1}-R_k}{z_{k+1}-z_k}
\]

G使用`beta_G=1.0 mm h^-1 km^-1`的masked Smooth-L1，不使用I的强度权重、高度权重或CFB质量权重；总损失固定为`L=L_I+0.02L_G`。因此缺测、halo、水平padding、CFB屏蔽、W弱标签和孤立可靠点都不能被差分跨越。该支持域只代表连续可靠正降水内部，不包含零雨边界、雨顶/雨底或CFB以下区域。

逐epoch日志分别保存`primary_log_smooth_l1`、`physical_drdz_smooth_l1`、`weighted_physical_drdz`、G占总目标比例和pair数。I按体素权重和汇总，G按pair数汇总；`best.pt`和早停仍只由可靠验证集降水RMSE决定。

### 11.8 第八步：只将水平尺寸补齐到U-Net要求的16倍数

标准配置下，扫描维已经是64，不再补；横轨49补到64；高度始终保留60层：

```text
(64,49,60) → (64,64,60)
```

padding只加在各轴的高端，不在两侧平均分配：

```text
nscan: (0, 0)   # 不补
nray:  (0,15)   # 在ray索引49—63补15列
z:     (0, 0)   # 不补，保留全部60个物理高度层
```

各数组的padding值为：

| 数组 | padding值 |
|---|---:|
| 标准化DPR反射率 | `0.0` |
| DPR有效性通道 | `0.0`（padding前为`False`） |
| 缩放高度通道 | `0.0` |
| `target` | `0.0` |
| `loss_mask` | `False` |
| `loss_weights` | `0.0` |
| `reliable_loss_mask` / `weak_loss_mask` | `False` |
| `output_mask` | `False` |
| `core_mask` | `False` |
| `diagnostic_target` | `0.0` |
| `native_positive_mask` | `False` |
| `cfb_distance_km` / `relative_cfb_level` | `NaN` |
| `cfb_quality_region` | unknown编码 |
| `cfb_profile_valid` | `False` |
| `precipitation_type` | `-9999`（unknown） |

这些横轨padding单元完全是为了满足四次水平下采样，没有对应真实空间。即使卷积
在这些位置产生非零结果，也不会进入masked loss，整轨重建时会裁掉多出的ray。
高度方向没有padding，也不会在网络中被下采样或上采样。

### 11.9 第九步：增加通道轴并转换为PyTorch张量

基线三个 `(64,64,60)` 数组沿新通道轴堆叠：

```text
inputs[0] = normalized_dbz  # 标准化反射率，float32
inputs[1] = effective_input # bool转为0.0/1.0，float32
inputs[2] = height_scaled   # 物理高度线性缩放到[-1,1]，float32

三个(64,64,60)
      ↓ np.stack(axis=0)
inputs: (3,64,64,60), float32
```

E2的 `signed_distance` 模式还追加：

```text
inputs[3] = clip((height_km - cfb_height_km) / 2 km, -1, 1)
```

无效CFB和padding填0，所以E2单样本为 `(4,64,64,60)`。E0、E1和新的E0-N系列均保持 `(3,64,64,60)`。E1不改通道数，但在标准化前将CFB以下输入从有效mask排除，最终表现为 `dbz=0, valid=0`。

标签和mask各自增加一个单通道轴：

| 字段 | 单个Dataset样本形状 | dtype |
|---|---:|---|
| `inputs` | `(C,64,64,60)` | `torch.float32`；通常C=3，E2为4 |
| `target` | `(1,64,64,60)` | `torch.float32` |
| `loss_mask` | `(1,64,64,60)` | `torch.bool` |
| `loss_weights` | `(1,64,64,60)` | `torch.float32` |
| `reliable_loss_mask` / `weak_loss_mask` | 各 `(1,64,64,60)` | `torch.bool` |
| `output_mask` | `(1,64,64,60)` | `torch.bool` |
| `core_mask` | `(1,64,64,60)` | `torch.bool` |
| `diagnostic_target` / `native_positive_mask` | 各 `(1,64,64,60)` | `float32` / `bool` |
| `height_km` / `height_index` | 各 `(1,1,1,60)` | `float32` / `int64` |
| `cfb_distance_km` / `relative_cfb_level` | 各 `(1,64,64,60)` | `float32` |
| `cfb_profile_valid` / `precipitation_type` | 各 `(1,64,64,1)` | `bool` / `int16` |
| `cfb_quality_region` | `(1,64,64,60)` | `int8` |

此时没有再对数值进行裁剪、缩放、数据增强或随机扰动。

### 11.10 第十步：DataLoader堆叠为最终模型batch

PyTorch默认整理函数只在最前面增加batch轴，不再改变单样本内部数值：

| 字段 | 模型前的batch形状 | dtype |
|---|---:|---|
| `inputs` | `(B,C,64,64,60)` | `float32`，C=3或4 |
| `target` | `(B,1,64,64,60)` | `float32` |
| `loss_mask` | `(B,1,64,64,60)` | `bool` |
| `loss_weights` | `(B,1,64,64,60)` | `float32` |
| `reliable_loss_mask/weak_loss_mask` | 各 `(B,1,64,64,60)` | `bool` |
| `output_mask` | `(B,1,64,64,60)` | `bool` |
| `core_mask` | `(B,1,64,64,60)` | `bool` |
| `diagnostic_target/native_positive_mask` | 各 `(B,1,64,64,60)` | `float32` / `bool` |
| `height_km/height_index` | 各 `(B,1,1,1,60)` | `float32` / `int64` |
| `cfb_distance_km/relative_cfb_level` | 各 `(B,1,64,64,60)` | `float32` |
| `cfb_profile_valid/precipitation_type` | 各 `(B,1,64,64,1)` | `bool` / `int16` |
| `file_id/core_start/core_length` | `(B,)` | `int64` |
| `original_shape/unpadded_shape/padded_shape` | `(B,3)` | `int64` |

模型只接收 `batch["inputs"]`，输出应为 `(B,1,64,64,60)`。标签和mask留在
训练循环中计算损失，例如：

```python
prediction = model(batch["inputs"])
loss = criterion(
    prediction,
    batch["target"],
    batch["loss_mask"],
    batch["loss_weights"],
)
```

必须处理一个batch中 `mask.sum()==0` 的情况。`positive_only=True` 依据索引中的
正降水标签数过滤；当前数据上正标签位置都有可用DPR输入，因此过滤后的Patch至少有
一个loss体素。但代码设计没有永久假设二者相等，未来数据或mask规则变化后仍可能出现
空mask，训练代码应显式检查，避免空张量均值产生 `NaN`。

### 11.11 不同“0”的含义速查

| 出现位置 | `0` 的实际含义 | 是否需要mask判断 |
|---|---|---|
| 原始 `dbz_dpr=0 dBZ` | 有效的弱反射率物理值 | 是，`dpr_valid=True` |
| 原始 `pre_dpr=0 mm/h` | 有效的零降水标签 | 是，当前`loss_mask=False` |
| 标准化反射率 `=0` 且mask=True | 等于该高度训练均值 | 是 |
| 标准化反射率 `=0` 且mask=False | 缺测、轨道外、无统计层或空间padding | 是 |
| `target=0` 且loss_mask=False | 不参与当前损失，原值不一定为零降水 | 是 |
| mask通道 `=0` | False/无效 | 本身就是判别信息 |

最关键的原则是：数值填0只是让张量可以进入神经网络，不会把缺测变成真实的
0 dBZ或0 mm/h。所有填充值都必须依靠配套mask解释。

## 12. 代码使用示例

### 12.1 选择性读取原始变量

```python
from precipitation_inversion.data.nc_reader import read_nc_sample

sample = read_nc_sample(
    "/path/to/file.nc",
    variables=("z", "dbz_gr_sparse", "dbz_dpr", "pre_dpr", "cfb"),
    scan_slice=slice(100, 132),
)

print(sample.variables["dbz_dpr"].shape)       # (32, 49, 60)
print(sample.masks["pre_positive_qc"].shape)  # (32, 49, 60)
```

上例要生成 `pre_positive_qc`，必须同时读取 `pre_dpr/cfb/z`。

### 12.2 构造保留的单体素训练DataLoader

```python
import torch

from precipitation_inversion.data.dataset import Stage1IntensityDataset
from precipitation_inversion.data.samplers import FileBlockBatchSampler

dataset = Stage1IntensityDataset(
    "metadata/stage1_indices/train.json",
    "metadata/normalization/stage1_positive_qc.json",
)
batch_sampler = FileBlockBatchSampler(
    dataset,
    batch_size=256,
    block_size=4096,
    drop_last=True,
    seed=2026,
)
loader = torch.utils.data.DataLoader(
    dataset,
    batch_sampler=batch_sampler,
    num_workers=2,
)

for epoch in range(100):
    batch_sampler.set_epoch(epoch)
    for batch in loader:
        x = batch["features"]  # (B,5)
        y = batch["target"]    # (B,)
```

设置 `batch_sampler` 后，不要再同时向DataLoader设置 `batch_size/shuffle/drop_last`。

### 12.3 构造当前3D Patch训练DataLoader

```python
import torch

from precipitation_inversion.data.patch_dataset import Stage1PatchDataset
from precipitation_inversion.data.samplers import FileBlockBatchSampler

dataset = Stage1PatchDataset(
    "metadata/stage1_patch_indices/train.json",
    "metadata/normalization/stage1_dbz_valid.json",
    positive_only=True,
)
batch_sampler = FileBlockBatchSampler(
    dataset,
    batch_size=1,
    block_size=16,
    drop_last=False,
    seed=2026,
)
loader = torch.utils.data.DataLoader(
    dataset,
    batch_sampler=batch_sampler,
    num_workers=2,
)

batch = next(iter(loader))
print(batch["inputs"].shape)     # torch.Size([1,3,64,64,60])
print(batch["target"].shape)     # torch.Size([1,1,64,64,60])
print(batch["loss_mask"].shape)  # torch.Size([1,1,64,64,60])
print(batch["loss_weights"].shape)       # torch.Size([1,1,64,64,60])
print(batch["reliable_loss_mask"].shape) # torch.Size([1,1,64,64,60])
print(batch["inputs"].dtype)     # torch.float32
print(batch["loss_mask"].dtype)  # torch.bool
```

3D张量显存占用远大于单体素数据，第一版建议从 `batch_size=1` 开始，确认单步
前向、反向和显存峰值后再增加。使用 `FileBlockBatchSampler` 时，每个epoch同样应调用
`batch_sampler.set_epoch(epoch)`，并且不能再给DataLoader传入 `batch_size/shuffle`。

## 13. 对后续模型设计的直接影响

### 13.1 单体素接口直接支持的模型

`Stage1IntensityDataset` 每个样本有5个标量输入，可支持：

```text
MLP: (B,5) → (B,1)
```

它回答的是局部数值映射是否已经足够，而不利用空间连续性。

### 13.2 一维垂直廓线模型尚需新的Dataset

如果要训练垂直一维CNN，建议未来Dataset输出类似：

```text
features: (C,60)
input_mask: (C_mask,60)
target: (60,) 或一个选定高度的标量
```

需要明确一根廓线中无降水和缺测层的处理，目前的单体素索引不能直接提供这一形式。

### 13.3 已实现的三维CNN/U-Net Patch Dataset

`Stage1PatchDataset` 已经输出：

```text
inputs:             (C,64,64,60)
target/loss_mask:   (1,64,64,60)
loss_weights:       (1,64,64,60)
reliable/weak mask: (1,64,64,60)
```

默认C=3，通道分别是逐高度标准化后的 `dbz_dpr`、其有效性mask和缩放高度坐标；仅E2追加CFB符号距离而使C=4。每个轨道按32扫描线划分非重叠输出核心，左右各读取16扫描线上下文；原始 `(64,49,60)` 只补齐横轨得到 `(64,64,60)`。测试时由 `predict_full_orbit` 自动裁取核心并重建原始 `(nscan,49,60)`。

一次 `__getitem__` 返回的数组/张量含义如下：

| 键 | 形状 | 含义 |
|---|---|---|
| `inputs` | `(C,64,64,60)` | 标准化DPR、有效性mask、高度；E2另有CFB距离 |
| `target` | `(1,64,64,60)` | `log1p(pre_dpr)`；loss外填0，不能脱离mask使用 |
| `loss_mask` | `(1,64,64,60)` | 可靠监督与可选弱监督的并集 |
| `loss_weights` | `(1,64,64,60)` | 高度×降水强度×CFB质量权重，mask外为0 |
| `reliable_loss_mask` | `(1,64,64,60)` | `pre_positive_qc` 可靠支持域，主指标只用它 |
| `weak_loss_mask` | `(1,64,64,60)` | 待测W方法的CFB下近邻弱监督 |
| `output_mask` | `(1,64,64,60)` | 可靠支持域；W/IW另包含近CFB配置层，仅由输入/几何决定 |
| `core_mask` | `(1,64,64,60)` | 中心核心的几何范围，排除halo、轨道边界和水平padding |
| `diagnostic_target/native_positive_mask` | 各 `(1,64,64,60)` | 质控前原生正降水，仅用于CFB以下诊断 |
| `cfb_distance_km/relative_cfb_level` | 各 `(1,64,64,60)` | 相对CFB的物理距离/层数，评价元数据 |
| `precipitation_type` | `(1,64,64,1)` | 逐廓线降水类型，只分组评价，不进模型 |
| `type_target` | `(64,64)` | 源代码1/2/3映射为0/1/2；其它位置为`ignore_index=-100` |
| `type_loss_mask` | `(64,64)` | 非重叠核心、合法类型且廓线存在原生DPR回波；不进模型 |
| `core_start/core_length` | 标量 | 当前核心在原轨道中的起点及真实长度 |
| `original_shape` | `(3,)` | 原文件 `(nscan,nray,z)`，用于整轨重建 |

必须使用masked reduction，例如先计算逐体素误差，再以 `loss_mask` 选择并求均值；
若直接对整个 `(64,64,60)` 张量求均值，大量人为补零会主导梯度并产生错误结果。

当前真实数据索引统计为：训练/验证/测试分别有2076/437/459个核心，其中
含正降水监督的核心为1340/299/288个。训练可用 `positive_only=True` 节省空核心计算；
当前W实验也保持相同的1340个训练Patch，仅在这些Patch内加入弱标签，便于控制变量。独立完整验证、整轨验证和测试必须用 `positive_only=False`，否则不能覆盖完整 `nscan` 或只含CFB下原生标签的Patch。

### 13.4 已实现的高度保持3D U-Net与masked loss

`Stage1UNet3D` 使用 `(3,3,1)` 水平卷积和 `(1,1,5)` 垂直卷积。四次编码
下采样与解码上采样的倍率都是 `(2,2,1)`，所以形状依次为：

```text
(B,C,64,64,60)       # C=3，仅E2为4
→ (B,16,64,64,60)
→ (B,32,32,32,60)
→ (B,64,16,16,60)
→ (B,128,8,8,60)
→ (B,256,4,4,60)
→ 对称解码
→ (B,1,64,64,60)
```

所有层的高度都保持60。主损失为log1p空间的masked Smooth L1，`beta=0.2`；`I+G-0.02`另从同一60层输出构造59层物理梯度辅助损失，不改变网络输出形状。同时底层实现了masked MSE和MAE作为对照。损失可接收与mask同形状的体素权重，按权重和归约；mask外体素梯度严格为0，空mask可返回与计算图连接的零损失或显式抛错。

### 13.5 阶段二数据、Sampler、双头GR模型和损失已实现

已实现从原生三态编码审计、train-only标准化、Patch Dataset、分层/DDP Sampler，到高度保持双头3D U-Net和双mask组合损失的数据流。当前第一版模型输入为：

```text
GR逐高度标准化dBZ
+ GR物理值mask
+ GR原生可用状态mask（已全数据审计，只作存储编码proxy）
+ 高度
→ (B,4,64,64,60)
```

不能把缺测GR直接填0后丢弃mask。`dbz_gr_interp` 作为传统基线，暂不进入当前4通道模型；后续如作辅助输入消融，必须与原始稀疏观测模型分开评价。

### 13.6 当前阶段一不能评价降水发生识别

当前样本全部满足 `pre_dpr>0`，而且DPR反射率有效位置本身与正降水高度相关。因此当前模型只能评价“已检测降水条件下的强度反演”，不能报告无降水识别率、命中率或虚警率。未来稠密输出模型需要单独构造包含有效零降水的标签和公平的输入mask。

## 14. 已测与待测方法对照

所有正式方法使用相同的按日期 train/val/test 划分、随机种子2026、Patch几何、U-Net宽度、AdamW、学习率调度和早停规则。实验只改表中所列因素。

| 实验 | 状态 | 输入标准化 | C | CFB相关输入 | loss支持域 | 体素权重 |
|---|---|---|---:|---|---|---|
| E0 | 已测，首轮基线 | `pre_positive_qc` 拟合 | 3 | CFB下反射率保留 | 可靠 `pre_positive_qc` | 全1 |
| E1 | 已测 | 同E0 | 3 | CFB下输入改为 `(dbz=0,valid=0)` | 同E0 | 全1 |
| E2 | 已测 | 同E0 | 4 | 保留输入，加截断符号距离通道 | 同E0 | 全1 |
| E0-N | 已测，主指标略退化 | 全部原生有效DPR输入拟合 | 3 | 与E0相同 | 同E0 | 全1 |
| E0-N-I | 已测，当前主任务最优 | 同E0-N | 3 | 同E0 | 同E0 | 强度 `1/1/1.5/2/3` |
| E0-N-W | 已测，主任务未改善 | 同E0-N | 3 | 同E0 | 可靠+近CFB弱监督 | CFB下第1/2层 `0.1/0.05` |
| E0-N-IW | 待测，只在I/W单独有效后解读 | 同E0-N | 3 | 同E0 | 同W | I×W |
| E0-N-I-G-0.02 | 代码已测，待正式训练 | 同E0-N | 3 | 同E0 | I主项+可靠相邻pair | I权重；G无额外权重，lambda=0.02 |

这里N代表“输入归一化与标签质控解耦”，I代表强降水加权，W代表CFB下近邻弱监督，G代表可靠正降水内部的物理垂直梯度约束。I+G配置与I相比只改变G损失块及实验名称/输出目录，输入、网络、数据划分、种子、优化器和选模规则完全相同，并要求从头训练。

## 15. 首轮E0/E1/E2结果与评价口径

### 15.1 完整可靠验证集

主指标在完全相同的1,195,966个 `pre_positive_qc` 体素上计算，表中使用各实验的 `best.pt`：

| 实验 | best epoch | MAE (mm/h) | RMSE (mm/h) | Bias (mm/h) | R² | Pearson r |
|---|---:|---:|---:|---:|---:|---:|
| E0 | 31 | 0.489751 | **2.347860** | -0.094304 | **0.717183** | **0.848497** |
| E1 | 42 | 0.504331 | 2.478820 | -0.103190 | 0.684753 | 0.831565 |
| E2 | 34 | 0.489888 | 2.405470 | -0.097537 | 0.703134 | 0.842449 |

E1的整体RMSE比E0高5.58%，E2高2.45%，所以本轮主结论是E1和E2都没有超过E0。E0/E1/E2分别记录44/55/47个epoch，且最优epoch后验证性能均变差，因此后续比较必须用 `best.pt`，不用 `last.pt`。

### 15.2 低层、相对CFB和强降水

| 分组RMSE (mm/h) | E0 | E1 | E2 |
|---|---:|---:|---:|
| 0–2 km | **4.9414** | 5.3481 | 5.1676 |
| 2–4 km | **2.6074** | 2.7567 | 2.6996 |
| 4–6 km | 1.7914 | 1.7875 | **1.7624** |
| 6–10 km | 1.0594 | 1.1096 | **0.9943** |
| 10–15 km | 1.0116 | 1.0777 | **0.9014** |
| CFB上0–0.5 km | **4.2810** | 4.6109 | 4.3823 |
| CFB上0.5–2 km | **2.8961** | 3.0718 | 3.0412 |

E2在6 km以上有改善，但最关键的0–4 km及CFB边界附近仍退化，不能用高层改善掩盖主问题。强度分组进一步表明：`≥30 mm/h` 只有4,624个体素，占可靠验证支持的0.39%，却贡献E0总平方误差的68.16%；E0/E1/E2该组RMSE为31.17/33.48/32.76 mm/h，Bias均大幅为负。因此下一轮I加权是针对明确的误差结构，不是盲目增加损失复杂度。

### 15.3 为什么E1/E2退化

- E1在训练输入中移除了1,036,674个有效DPR体素，约15.90%，并把它们编码得与真正缺测相同。它验证的实际是“删除低层上下文”，而不是“区分低置信与缺测”。
- E2只比E0多160个参数（约0.008%），所以不能归因为参数量上的“通道稀释”。但该距离通道是稠密场，输入RMS约0.911，距离截断后约75.39%全网格值饱和在+1，且与高度通道相关约0.68。它可能在优化中形成过强的几何捷径。

固定6条测试轨道上E0/E1/E2的RMSE约为1.6347/1.7779/1.5777 mm/h，E2在这个小子集上更好。这与完整验证集结论不一致，恰好说明不能用6条测试轨道调参。当前只有单随机种子且 `deterministic=false`，接近的差异仍需要复现实验或多种子确认。

### 15.4 当前验证与诊断语义

- DDP训练仍以 `even_batches=True` 保证各rank反向步数相同；逐epoch验证改为互斥非等长分片，3卡可完整且不重复地评价299个正样本Patch，不再截成297个。
- 训练内验证的 `val_positive_only=true`，用于快速选模；训练后独立完整验证使用 `positive_only=false`，遍历全437个Patch。两者的主指标仍只计算同一批 `reliable_loss_mask`体素。
- `diagnostics.below_cfb_native_positive` 只在质控前原生 `pre_dpr>0` 且CFB符号距离小于0的位置评价。它不进主loss、主指标或 `best.pt` 选择，只用于观察低置信区。
- `filewise` 按轨道先计算MAE/RMSE/Bias/R²/r，再对非空轨道等权宏平均；Bootstrap以整条轨道为重采样单位，默认seed=2026、2,000次、95%置信区间。宏平均RMSE与全体体素pooled RMSE是不同问题，应并列报告。

当前Stage1仍是“已知DPR检出正降水时的强度回归”。因主标签不包含零降水，不应从这些实验报告命中率、虚警率或“全空间降水检测能力”。

## 16. 相关代码与元数据

| 文件 | 作用 |
|---|---|
| `src/precipitation_inversion/data/masks.py` | 缺测、降水和CFB mask规则 |
| `src/precipitation_inversion/data/stage2_masks.py` | Stage 2原生mask/NaN、有限sentinel和物理dBZ三态分类 |
| `src/precipitation_inversion/data/stage2_geometry.py` | Stage 2逐高度水平最近GR观测距离、截断与缩放 |
| `src/precipitation_inversion/data/nc_reader.py` | 变量选择、扫描切片、形状检查 |
| `src/precipitation_inversion/data/transforms.py` | 在线统计、逐高度标准化和log1p变换 |
| `src/precipitation_inversion/data/dataset.py` | 阶段一体素索引和PyTorch Dataset |
| `src/precipitation_inversion/data/samplers.py` | 文件块洗牌和DDP批次分片 |
| `src/precipitation_inversion/data/stage2_samplers.py` | Stage 2四分层训练采样、循环过采样、文件局部性和DDP批次分片 |
| `src/precipitation_inversion/data/patch_dataset.py` | 3D核心+halo窗口、padding和mask张量 |
| `src/precipitation_inversion/data/stage2_patch_dataset.py` | Stage 2存储感知索引、可配置3/4/5通道GR输入、双目标与诊断mask |
| `src/precipitation_inversion/models/blocks3d.py` | 高度保持的水平/垂直分解残差块 |
| `src/precipitation_inversion/models/unet3d.py` | 阶段一各向异性3D U-Net |
| `src/precipitation_inversion/models/stage2_unet3d.py` | Stage 2共享高度保持骨干及支持域/dBZ双输出头 |
| `src/precipitation_inversion/losses/masked_losses.py` | masked Smooth L1/MSE/MAE、物理G与组合损失工厂 |
| `src/precipitation_inversion/losses/stage2_losses.py` | Stage 2 masked BCE/Focal支持域损失与masked Smooth L1 dBZ组合损失 |
| `src/precipitation_inversion/metrics/regression.py` | log/mm/h空间流式指标、强度分箱和统一物理dR/dz |
| `src/precipitation_inversion/metrics/stage2_reflectivity.py` | Stage 2支持域混淆矩阵、dBZ回归和水平FSS |
| `src/precipitation_inversion/training/engine.py` | AMP/DDP训练、验证和checkpoint |
| `src/precipitation_inversion/training/stage2_engine.py` | Stage 2双任务独立归约、AMP/DDP训练与验证 |
| `src/precipitation_inversion/inference/sliding_window.py` | 中心核心裁剪及完整轨道重建 |
| `src/precipitation_inversion/inference/stage2_sliding_window.py` | Stage 2双输出整轨重建、目标mask重建和流式阈值选择 |
| `configs/stage1_unet3d.yaml` | 第一版训练超参数和数据路径 |
| `configs/stage2_unet3d.yaml` | Stage 2原始4通道基线配置 |
| `configs/stage2_unet3d_3ch_native.yaml` | 3N：稀疏dBZ+原生可用性mask+高度 |
| `configs/stage2_unet3d_3ch_value.yaml` | 3V：稀疏dBZ+物理值mask+高度 |
| `configs/stage2_unet3d_4ch_distance.yaml` | 已训S2-3V-D：在3V上只增加最近GR观测距离，当前平衡主模型 |
| `configs/stage2_unet3d_4ch_density.yaml` | 已训S2-3V-rho：在3V上只增加5×5局部GR观测密度 |
| `configs/stage2_unet3d_5ch_interp.yaml` | 已训E2：在3V上增加GR插值值与其mask |
| `configs/stage2_unet3d_4ch_distance_intensity_w1p5.yaml` | E3-W1.5：保持D的四通道输入，只增加物理dBZ强度权重 |
| `configs/stage1_ablation_e0_n_*.yaml` | N/I/W/IW及I+G-0.02受控实验配置 |
| `scripts/train_stage1_unet3d.py` | 单卡或八卡DDP训练入口 |
| `scripts/evaluate_stage1_unet3d.py` | 完整Patch、CFB诊断、轨道宏平均/置信区间和整轨评估 |
| `scripts/plot_stage1_stratified_metrics.py` | 分高度、CFB、强度、类型、逐轨及置信区间图表 |
| `scripts/launch_stage1_ablation_suite.sh` | 默认只打印、可顺序启动受控消融 |
| `scripts/analyze_stage2_gr_dpr_alignment.py` | Stage 2三态、四分区、距离、密度和小位移全数据审计 |
| `scripts/fit_stage2_normalization_stats.py` | 仅用train物理反射率拟合GR/interp/DPR逐高度统计量 |
| `scripts/build_stage2_patch_index.py` | 生成train/val/test核心Patch索引和分层计数字段 |
| `scripts/evaluate_stage2_baselines.py` | 用统一mask评价稀疏GR/插值及其高度校准四个基线 |
| `scripts/train_stage2_unet3d.py` | Stage 2单卡/DDP训练、断点恢复、早停和训练后自动分析 |
| `scripts/launch_stage2_ddp.sh` | 要求显式选择空闲GPU的单机多卡Stage 2启动器 |
| `scripts/evaluate_stage2_unet3d.py` | 完整val/test整轨重建、val-only阈值选择和分层指标 |
| `scripts/visualize_stage2_test_predictions.py` | 固定测试轨道的支持域、dBZ、A-B剖面、分布和相关性图 |
| `metadata/splits/` | train/val/test文件清单及划分审计 |
| `metadata/normalization/stage1_positive_qc.json` | 老E0/E1/E2复现用的标签筛选输入统计 |
| `metadata/normalization/stage1_dbz_valid.json` | 新输入有效值统计+独立标签高度计数 |
| `metadata/normalization/stage2_reflectivity.json` | Stage 2正式train-only GR/interp/DPR物理dBZ统计 |
| `metadata/stage1_indices/` | 本机生成的三个split体素索引 |
| `metadata/stage1_patch_indices/` | 本机生成的三个split Patch索引 |
| `metadata/stage2_patch_indices/` | Stage 2三个split的完整Patch索引与mask计数 |
| `outputs/stage2_alignment_audit/` | 254文件Stage 2审计JSON/CSV/PNG产物 |
| `outputs/stage2_baselines/` | 四个传统基线的全量/分split/分高度/分区域指标 |

在更改输入变量、mask条件、归一化范围或标签定义后，必须重新生成归一化统计和样本索引，并重新核对此文档。

## 17. Stage 2：从GR/DPR原始存储到模型batch

本节描述当前已经实现并通过测试的Stage 2数据管线。代码已经能够保留原始三态、拟合train-only统计量、构建三个split索引，并实际产生PyTorch batch。第18节继续说明已实现的分层Sampler、双头3D U-Net和双mask组合损失。

### 17.1 为什么Stage 2不能先调用通用`to_float_array`

原始NetCDF反射率在存储上存在三态：

```text
NetCDF masked array
  │
  ├─ 原生mask或非有限NaN/Inf
  │    → native_missing
  │
  ├─ 未被mask，但raw_value <= -9990
  │    → sentinel（常见为-9999.9）
  │
  └─ 未被mask且有限，raw_value > -9990
       → value（物理dBZ，包括负dBZ和弱回波）
```

通用`to_float_array`/`read_nc_sample`会把前两种都转为`NaN`。这对Stage 1只判断“是否有物理DPR dBZ”是正确的，但Stage 2将无法再审计GR的两种原始缺测编码。

因此Stage 2的顺序必须是：

```text
NetCDF variable[:]（保留MaskedArray）
  → classify_reflectivity_storage
  → 先生成native_missing/sentinel/value
  → 再生成物理dBZ浮点数组
  → 最后才做归一化和缺测占位
```

`sentinel`是存储语义名称。当前NC没有雷达站ID、仰角、方位角、距离或波束宽度，所以不能将它直接重命名为“仰角间空洞”或“有效无回波”。

### 17.2 已实现的三态mask

`ReflectivityStorageMasks`中的mask都是NumPy `bool`，形状与源反射率完全相同：

```text
整轨：       (nscan,49,60)
扫描窗口：   (64,49,60)
```

| mask/属性 | 定义 | 可否直接解释为物理覆盖 |
|---|---|---|
| `native_missing` | NetCDF原生mask或raw value非有限 | 不能完全确认，是“真未扫描”的候选代理 |
| `sentinel` | `~native_missing & raw<=-9990` | 不能确认是beam-gap、无回波还是其它填充 |
| `value` | `~native_missing & finite & raw>-9990` | 有保留的物理dBZ |
| `native_available` | `sentinel | value` | 仅表示源存储未进入native missing状态 |
| `physical_missing` | `native_missing | sentinel` | 当前没有可用物理dBZ |

三个基本mask互斥且遍历全部元素：

```text
native_missing + sentinel + value == 1   # 逐体素
```

对`dbz_gr_sparse/dbz_gr_interp/dbz_dpr`分别生成同样的三态。`physical_reflectivity_values`只保留`value=True`处的实dBZ，其余填`NaN`；它不会把负dBZ、`0 dBZ`或`<9 dBZ`删除。

### 17.3 254个文件的三态审计结果

本次审计覆盖`268,857,120`个体素：

| 变量 | native missing | finite sentinel | 物理dBZ value |
|---|---:|---:|---:|
| `dbz_gr_sparse` | 181,331,692（67.445%） | 82,411,676（30.653%） | 5,113,752（1.902%） |
| `dbz_gr_interp` | 70,387,860（26.180%） | 184,882,515（68.766%） | 13,586,745（5.054%） |
| `dbz_dpr` | 0（0%） | 259,094,581（96.369%） | 9,762,539（3.631%） |

跨文件一致性：

- 254个文件全部同时包含GR `native_missing`和`sentinel`；
- 只有1个文件没有GR物理dBZ，其余文件都有；
- DPR在254个文件中都没有native missing，其非回波区使用finite sentinel表示；
- DPR `value`与`pre_dpr>0`在全数据集逐体素完全一致，mismatch为0。

因此第一版Stage 2可以保留`gr_native_available`通道，但必须将它注明为**稳定的原始编码代理**，不宣称它是已确认的雷达物理覆盖mask。

### 17.4 四分区和标签域mask

先定义：

```text
M_GR     = gr_value
M_DPR    = dpr_value
M_DOMAIN = pre_dpr原生有效且非负
```

`M_DOMAIN`是DPR标签产品的分类区域，包含有效`pre_dpr=0`；它只能用于目标和loss mask，不能输入GR-only模型。

在`M_DOMAIN`内定义：

| mask | 定义 | 整个数据集数量 | 形状 |
|---|---|---:|---:|
| `q11_overlap` | `M_DOMAIN & M_GR & M_DPR` | 2,864,798 | `(nscan,49,60)` |
| `q01_dpr_only` | `M_DOMAIN & ~M_GR & M_DPR` | 6,897,741 | 同上 |
| `q10_gr_only` | `M_DOMAIN & M_GR & ~M_DPR` | 2,230,124 | 同上 |
| `q00_neither` | `M_DOMAIN & ~M_GR & ~M_DPR` | 251,627,971 | 同上 |

四者互斥并且总数等于`M_DOMAIN=263,620,634`。

`dbz_gr_interp`还可用来生成可达性代理：

```text
gap_proxy     = ~gr_value & gr_interp_value
outside_proxy = ~gr_value & ~gr_interp_value
```

在所有DPR目标中：

- `q11_overlap`占29.345%；
- 同格点没有GR物理值的`q01_dpr_only`占70.655%；
- `dpr_only_gap_proxy`有3,329,194个，占DPR目标的34.102%；
- `dpr_only_outside_proxy`有3,568,547个，占DPR目标的36.553%。

`gap_proxy`只表示师兄插值算法能够覆盖的空洞，不是真实仰角间空洞mask。

### 17.5 距离、局部密度和偏移审计

距离只在水平`nscan/nray`中计算Chebyshev距离，不将不同高度混合。DPR目标到最近GR物理值的累积覆盖为：

| 距离上限 | DPR目标覆盖比 |
|---:|---:|
| 0格（同格点） | 29.345% |
| ≤1格 | 47.815% |
| ≤2格 | 55.134% |
| ≤3格 | 59.230% |
| ≤5格 | 64.119% |
| ≤8格 | 68.790% |
| >8格 | 31.210% |

在同高度`5×5`水平窗口内，44.866%的DPR目标周围没有任何GR物理值。这些点很可能只能依靠更大上下文和降水结构先验，无法通过同格点映射恢复。

对`[-2,+2]×[-2,+2]`全25个水平平移做了逐高度支持域CSI审计：

- 60个高度层中59层的全局最佳位移是`(scan=0, ray=0)`；
- 只最0.125 km层的最佳位移是`(0,+1)`，但该层最佳CSI仅约0.0159；
- 全局`q11` 上GR−DPR Bias为-1.381 dBZ，MAE 4.108 dBZ，RMSE 5.953 dBZ，Pearson r=0.748。

因此当前没有证据支持在第一版模型前加全局固定平移或复杂STN。局部、轨道级或事件级偏移仍可能存在，需在模型评价时用邻域CSI/FSS继续分析。

### 17.6 正式归一化统计与Patch索引

`fit_stage2_normalization_stats.py`只读取train split的175个文件。每个变量独立使用自己的`storage_value=True`位置拟合逐高度均值和总体标准差，绝不使用DPR/pre_dpr mask筛选GR统计量：

| 变量 | train物理值数量 | 空高度层 | 各高度std范围 |
|---|---:|---:|---:|
| `dbz_gr_sparse` | 3,616,795 | 0 | 6.653～11.430 dBZ |
| `dbz_gr_interp` | 9,660,180 | 0 | 7.674～11.502 dBZ |
| `dbz_dpr` | 6,822,356 | 0 | 4.931～7.835 dBZ |

因此当前60层都可以安全标准化；Dataset会拒绝部分`--max-files`统计、split哈希不一致、空高度或零方差统计，避免静默丢失某层物理值。

`build_stage2_patch_index.py`为每个非重叠核心记录：`gr_count/dpr_count/q11_count/q01_count/q10_count/gap_target_count/outside_target_count/strong_dpr_count`。强目标当前定义为`DPR dBZ >= 35`，只服务于后续分层采样，不改变标签。

| split | 文件 | 全部Patch | 含DPR目标Patch | 含≥35 dBZ目标Patch |
|---|---:|---:|---:|---:|
| train | 175 | 2,076 | 1,340 | 883 |
| val | 38 | 437 | 299 | 185 |
| test | 41 | 459 | 288 | 183 |

背景Patch不会在索引阶段删除：支持域分类必须同时学习正例和真正负例。训练时的采样平衡应由后续Sampler完成，完整验证/测试始终遍历全部Patch。

### 17.7 从整轨数组到Stage 2 Patch

实际Dataset沿用阶段一的非重叠输出核心+重叠上下文：

```text
原始整轨变量/每个mask：        (nscan,49,60)
当前输出核心：                   (32,49,60)
左右各16条scan上下文后：       (64,49,60)
nray高端补0：                    (64,64,60)
增加channel轴：                  (C,64,64,60)
DataLoader堆叠：                   (B,C,64,64,60)
```

高度保持60层，不做padding；仅对`nscan/nray`做水平处理。轨道边界缺失的halo和`nray=49→64`的伪造格点都由`core_mask=False`排除loss。

Stage 2 Patch不能只保留`Q11`，因为这会删除70.655%的DPR填充目标。首尾轨道外halo、`nray=49→64`新增的15条ray和连续通道都填`0`；对应`geometry_mask/core_mask`为False。原始轨道内部即使反射率缺测，仍是实际网格点，`geometry_mask=True`，不能与padding混淆。

### 17.8 第一版输入通道的逐值变换

全数据审计证明三态编码跨文件稳定，因此第一版使用4通道：

#### 通道0：`gr_dbz_standardized`

1. 原始`dbz_gr_sparse`形状为`(64,49,60)`；
2. 仅`gr_value=True`的点保留物理dBZ；
3. 只用train split中同高度的GR物理值拟合`mu_gr[z]` 和 `sigma_gr[z]`；
4. 对每个物理值计算：

\[
Z'_{GR}(i,j,z)=\frac{Z_{GR}(i,j,z)-\mu_{GR}(z)}{\sigma_{GR}(z)};
\]

5. `native_missing/sentinel`、轨道外halo和水平padding在**标准化空间**填`0.0`。

这个`0.0`表示当前高度训练集平均dBZ占位，不是物理`0 dBZ`，也不是无降水。模型必须结合后续mask通道解释该占位值。

第一版不执行`<9 dBZ→0`。全数据约20.8%的GR物理值低于9 dBZ，它们可能为弱回波/噪声，但删除它们应是后续受控消融，不是默认读取语义。

#### 通道1：`gr_value_mask`

```text
原始物理dBZ：1
native missing/sentinel/padding：0
```

转为`float32`，补齐后形状`(1,64,64,60)`。它让模型区分“通道0的0是真实标准化数值”还是“缺测占位”。

#### 通道2：`gr_native_available`

```text
sentinel或物理dBZ：1
native mask/NaN/Inf/padding：0
```

形状同样为`(1,64,64,60)`。结合通道1可推出：

```text
sentinel = gr_native_available & ~gr_value_mask
```

因此不再增加冗余`sentinel`通道。该通道是原始编码代理，不代表已确认的真实雷达覆盖。

#### 通道3：`height_scaled`

```text
z = 0.125,...,14.875 km
→ 线性缩放到[-1,1]
→ 广播到(1,64,64,60)
```

真实轨道网格（包括GR缺测位置）保留高度坐标；轨道外halo和`nray`水平padding位置填中性`0.0`。`geometry_mask=False`进一步明确这些位置不是物理网格。

最后拼接：

```text
channel 0: gr_dbz_standardized   (1,64,64,60)
channel 1: gr_value_mask         (1,64,64,60)
channel 2: gr_native_available   (1,64,64,60)
channel 3: height_scaled         (1,64,64,60)
------------------------------------------------
inputs                              (4,64,64,60)
batch inputs                      (B,4,64,64,60)
```

`dbz_gr_interp`不进入`S2-E0`。它先作为传统基线，现在已实现与`gr_interp_value_mask`成对输入的严格受控五通道消融。`p/t/q`、DPR变量、`pre_dpr/cfb/typePrecip`仍不进入该模型。

#### 17.8.1 3V母实验与当前S2-3V-D距离实验

3V删除了表现较差的`gr_native_available`，仅保留能正确解释稀疏dBZ占位值的`gr_value_mask`，输入为：

```text
channel 0: dbz_gr_sparse_standardized
channel 1: gr_value_mask
channel 2: height_scaled
inputs: (3,64,64,60) -> batch: (B,3,64,64,60)
```

当前严格受控实验`S2-3V-D`只增加一个由直接GR物理值mask派生的距离通道：

```text
channel 0: dbz_gr_sparse_standardized  原始稀疏GR物理dBZ逐高度标准化
channel 1: gr_value_mask               原始稀疏GR是否为物理dBZ
channel 2: gr_nearest_distance_scaled  同高度层最近直接GR观测距离
channel 3: height_scaled               物理高度缩放到[-1,1]
inputs: (4,64,64,60) -> batch: (B,4,64,64,60)
```

距离通道的数值流程是：

```text
gr_value_mask (nscan,49,60)
  -> 对每个高度z独立在nscan/nray平面搜索
  -> d(i,j,z)=min max(|delta_scan|,|delta_ray|)  （Chebyshev距离）
  -> clip(d,0,8)/8，得到[0,1]的float32张量
  -> 在整轨上先计算，再截取非重叠core+重叠halo
  -> nray从49 padding到64，得到(64,64,60)
```

其中`0`表示当前体素就有直接GR物理dBZ，`0.125`表示最近观测相距1格，`1`表示相距至少8格或该高度层完全没有直接GR值。不允许用其他高度的观测缩短当前层距离。轨道外halo和`nray=49→64`的padding同样填`1`，因为它们不是距离0的直接观测。

距离只由GR计算，推理时可得，不存在DPR标签泄漏。`S2-3V-D`与3V保持完全相同的split、seed、Sampler、标签、loss、网络宽度、优化器和训练策略，并从头训练。完整val结果表明D是当前支持域与dBZ回归之间最平衡的Stage 2模型。

#### 17.8.2 已测局部密度与E2插值辅助输入

局部密度实验只将D的距离通道替换为同高度5×5水平邻域中GR物理值的比例，输入仍为`(B,4,64,64,60)`。它未超过D，因而不与距离通道合并。

E2在3V上一次只增加一对语义不可分的“插值值+mask”通道：

```text
channel 0: dbz_gr_sparse_standardized  原始稀疏GR物理dBZ逐高度标准化
channel 1: gr_value_mask               原始稀疏GR是否有物理dBZ
channel 2: dbz_gr_interp_standardized  插值GR用自己的train-only逐高度统计标准化
channel 3: gr_interp_value_mask        插值GR是否有物理dBZ
channel 4: height_scaled               物理高度缩放到[-1,1]
inputs: (5,64,64,60) -> batch: (B,5,64,64,60)
```

`dbz_gr_interp_standardized`只在`gr_interp_value_mask=1`处保留标准化值；插值无值、轨道外halo和`ray=49:64`水平padding在连续通道中填`0.0`，同mask处填0。这对输入只由GR及师兄的GR插值产品构造，不使用DPR标签。

E2 `best_dbz`在完整val上将MAE从D的3.8408降至3.7971 dBZ，但Recall从0.6893降至0.6592，CSI从0.5771降至0.5727；`best_support`也未挽回总体退化。它在插值可达的`gap_proxy`内改善，在`outside_proxy`内退化，说明插值覆盖容易形成保守捷径。因此E2不取代D，不继续增加距离形成六通道模型。

### 17.9 Stage 2标签、loss mask和batch形状

DPR反射率目标使用同一份`stage2_reflectivity.json`中的train-only `dbz_dpr`统计量。该统计与Stage 1一样使用物理DPR有效值，但Stage 2文件同时绑定了GR、插值和DPR统计以及split哈希，避免拼接不同数据版本。逐高度变换为`(Z_DPR-mu_DPR[z])/sigma_DPR[z]`，无目标处在标准化空间填`0.0`。

| Dataset字段 | 单样本形状 | batch形状 | 数值/用途 |
|---|---:|---:|---|
| `inputs` | `(C,64,64,60)` | `(B,C,64,64,60)` | GR-only可部署输入；`C=3`为3N/3V，`C=4`为E0、D、rho或E3，`C=5`为已测E2插值输入 |
| `target_dbz` | `(1,64,64,60)` | `(B,1,64,64,60)` | DPR物理值逐高度标准化，mask外填入0 |
| `target_support` | 同上 | 同上 | `target_valid`转float32，供BCE类loss使用 |
| `target_valid` | `(1,64,64,60)` | `(B,1,64,64,60)` | `dpr_value`，支持域0/1目标 |
| `support_loss_mask` | 同上 | 同上 | 与`occupancy_domain_mask`相同的显式分类loss域 |
| `occupancy_domain_mask` | 同上 | 同上 | `core & pre_valid_native`，分类loss的可信区域 |
| `regression_mask` | 同上 | 同上 | `core & target_valid`，dBZ loss支持域 |
| `regression_weights` | `(1,64,64,60)` | `(B,1,64,64,60)` | E3由标准化前物理DPR dBZ分箱生成的`float32` loss权重；`M_dbz=0`处填0 |
| `overlap_mask` | 同上 | 同上 | `core & Q11`，GR–DPR校准项 |
| `dpr_only_mask` | 同上 | 同上 | `core & Q01`，稀疏填充项 |
| `gr_only_mask` | 同上 | 同上 | `core & Q10`，支持域抑制诊断 |
| `neither_mask` | 同上 | 同上 | `core & Q00`，可信背景诊断 |
| `below_cfb_target_mask` | 同上 | 同上 | DPR目标且CFB以下，质量分组/后续消融 |
| `gap_proxy_mask` | 同上 | 同上 | 插值可达空洞，只分组评价 |
| `outside_proxy_mask` | 同上 | 同上 | 插值不可达空洞，只分组评价 |
| `core_mask` | 同上 | 同上 | 排除halo、轨道边界和水平padding |
| `geometry_mask` | 同上 | 同上 | 标记整个窗口内来自原始轨道的真实网格点 |
| `gr_value_mask` | 同上 | 同上 | 输入通道1的bool原型，含halo中的真实GR值 |
| `gr_interp_value_mask` | 同上 | 同上 | 五通道输入通道3的bool原型；其他实验仍作诊断字段 |
| `gr_native_available` | 同上 | 同上 | 输入通道2的bool原型 |

`target_valid=False`不等于`target_dbz=0 dBZ`。`target_dbz`在mask外填的`0`仍是标准化空间的中性占位，反射率loss不得在这些位置计算。

支持域头在`occupancy_domain_mask`内同时看到正负样本；反射率头只在真实DPR有效点上监督。训练时不用预测支持域硬门控反射率头，避免早期分类错误切断回归梯度。

当前已实现模型的输出形状为：

```text
Stage2 3D U-Net共享解码特征: (B,16,64,64,60)
支持域logits:                        (B,1,64,64,60)
DPR标准化dBZ:                       (B,1,64,64,60)
```

### 17.10 这些mask能否进入模型

| 信息 | Stage 2输入 | 原因 |
|---|---|---|
| `gr_value_mask` | 是 | 推理时由GR自身可得 |
| `gr_native_available` | S2-E0保留 | 原始编码跨文件稳定且可从GR生成，但仅作proxy解释 |
| `distance_to_gr` | D与E3的第3通道 | 只由`gr_value_mask`计算，部署可得 |
| `local_density` | 已作为3V的独立单因素消融 | 未超过D，当前不与距离合并 |
| `dbz_gr_interp/interp_mask` | 只进入已测E2 | E2未取代D，不进入当前E3 |
| `dpr_value/target_dbz` | 否 | 是监督标签 |
| `pre_dpr/pre_valid_native` | 否 | 只定义支持域分类loss区域 |
| `cfb` | 否 | DPR来源，只用于标签质量分组 |
| `typePrecip` | 否 | DPR来源，只用于层云/对流分组评价 |

师兄旧`DatasetNew.py`中默认`maskSGRWithRMask=True`，会用DPR降水mask改写GR输入mask。Stage 2新数据管线不得复用该行为，否则会把部署时不可得的标签信息泄漏到输入。

### 17.11 元数据与审计复现命令

完整结果位于：

```text
outputs/stage2_alignment_audit/
├─ summary.json
├─ per_file.csv
├─ per_height.csv
├─ distance_to_gr.csv
├─ local_density.csv
├─ shift_metrics.csv
├─ best_shift_by_height.csv
├─ coverage_by_height.png
├─ regions_by_height.png
├─ distance_to_gr.png
└─ best_shift_by_height.png
```

复现命令：

```bash
cd /home/koujizhi/projects/precipitation-inversion
source .venv/bin/activate

python scripts/analyze_stage2_gr_dpr_alignment.py \
  --workers 4 \
  --output-dir outputs/stage2_alignment_audit \
  --overwrite
```

完整默认会审计train/val/test、0/1/2/3/5/8格距离和±2格水平位移。服务器共享存储压力较大时将`--workers`改为1或2，不会改变统计结果。

正式归一化和索引的复现命令：

```bash
python scripts/fit_stage2_normalization_stats.py --overwrite
python scripts/build_stage2_patch_index.py --split all --overwrite
```

生成文件：

```text
metadata/normalization/stage2_reflectivity.json
metadata/stage2_patch_indices/
├─ train.npy / train.json
├─ val.npy   / val.json
└─ test.npy  / test.json
```

### 17.12 Stage 2统一指标和四个传统基线

评价被有意拆为两部分：

1. **支持域能力**：在`occupancy_domain`内计算TP/FP/FN/TN、Precision、Recall/POD、F1、CSI、FAR和Specificity；不能把标签域外点计作大量TN。
2. **反射率能力**：只在`predicted_support & dpr_value & domain`共同有效位置计算dBZ MAE、RMSE、Bias、R²、Pearson和CCC。稀疏基线的dBZ指标必须与支持域Recall一起看，否则只评价容易的重叠点会虚高。

此外在水平`nscan/nray`上计算半径1/2/4格FSS。每个高度独立进行窗口统计，绝不把垂直相邻层混入水平邻域。指标还按split、绝对高度、Q11/Q01/Q10、gap/outside proxy、CFB上下和DPR dBZ强度区间输出。

全254文件基线结果：

| 基线 | Precision | Recall | CSI | common MAE | common RMSE | common r | FSS-r1 | FSS-r4 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| sparse GR raw | 0.562 | 0.293 | 0.239 | 4.108 | 5.953 | 0.748 | 0.477 | 0.532 |
| sparse GR高度校准 | 0.562 | 0.293 | 0.239 | 4.397 | 5.662 | 0.747 | 0.477 | 0.532 |
| legacy interp raw | 0.476 | 0.634 | 0.374 | 4.629 | 6.646 | 0.700 | 0.650 | 0.698 |
| legacy interp高度校准 | 0.476 | 0.634 | 0.374 | 5.137 | 6.512 | 0.700 | 0.650 | 0.698 |

独立test split的关键门槛：

| 基线 | Precision | Recall | CSI | common MAE | common RMSE | common r | FSS-r4 |
|---|---:|---:|---:|---:|---:|---:|---:|
| sparse GR raw | 0.558 | 0.262 | 0.217 | 4.120 | 5.910 | 0.751 | 0.483 |
| legacy interp raw | 0.478 | 0.578 | 0.354 | 4.699 | 6.699 | 0.701 | 0.667 |

逐高度均值/方差映射虽然降低RMSE，却没有稳定降低MAE或提高相关性，因此只保留为对照，不作为神经网络前的默认校准。Stage 2模型至少应在同一完整test集上同时超过插值基线的`Recall/CSI/FSS`和原始稀疏GR的共同支持`MAE/r`，不能只选择其中一类指标。

复现命令与输出：

```bash
python scripts/evaluate_stage2_baselines.py \
  --output-dir outputs/stage2_baselines \
  --overwrite

outputs/stage2_baselines/
├─ summary.json
├─ per_file.csv
├─ per_height.csv
└─ per_region.csv
```

## 18. Stage 2：从分层采样到双头模型输出

本节对应已实现的`stage2_samplers.py`、`stage2_unet3d.py`和`stage2_losses.py`。三者遵守一条关键规则：**GR只做部署时可得的模型输入，DPR/pre_dpr只生成训练目标和loss mask**。

### 18.1 端到端数据流

```text
Stage 2 train patch索引
  │  每条记录含dpr/q11/q01/strong_dpr体素数
  ↓
Stage2StratifiedBatchSampler
  │  只决定本epoch读取哪些patch及顺序，不改数值
  ↓
Stage2PatchDataset.__getitem__
  │  从NetCDF读取(64,49,60)上下文，沿ray补齐到(64,64,60)
  │  构造4通道GR输入、DPR支持域目标、DPR dBZ目标和各种mask
  ↓
DataLoader堆叠
  inputs:             (B,4,64,64,60)
  target_support:     (B,1,64,64,60)
  target_dbz:         (B,1,64,64,60)
  support_loss_mask:  (B,1,64,64,60)
  regression_mask:    (B,1,64,64,60)
  ↓
Stage2UNet3D
  support_logits:     (B,1,64,64,60)
  reflectivity:       (B,1,64,64,60)  # 标准化DPR dBZ
  ↓
Stage2CompositeLoss
  L = lambda_support * L_support + lambda_dbz * L_dbz
```

因此，Sampler不会对反射率做插值或强度变换；Dataset不会把反射率目标乘以DPR支持域预测；模型也不在`forward`内做阈值化。

### 18.2 四分层训练采样

仅对train split使用`Stage2StratifiedBatchSampler`。每个patch根据索引记录被严格分到一个且仅一个层，优先级从高到低为：

| 分层 | 定义 | train原始Patch数 | 默认epoch权重 | 2,076样本epoch配额 |
|---|---|---:|---:|---:|
| `strong_target` | `strong_dpr_count > 0`，即核心内有DPR `>=35 dBZ` | 883 | 0.40 | 830 |
| `fill_dominant_target` | 无强目标、`dpr_count>0`且`q01/dpr>=0.75` | 312 | 0.30 | 623 |
| `ordinary_target` | 其余无强目标的DPR目标Patch | 145 | 0.10 | 208 |
| `background` | `dpr_count == 0` | 736 | 0.20 | 415 |

`fill_dominant_target`用“DPR目标中同格点GR缺测比例至少75%”定义，而不是要求`q11=0`。原因是正式train索引中没有“无强反射率且所有DPR目标都是q01”的Patch；如果强行使用该定义，会产生有正配额却无候选样本的空分层。

采样行为如下：

- `epoch_size`默认等于Dataset长度，因此改变分层权重时不改变每轮总训练样本数；
- 整数配额用“最大余数法”分配，保证四层之和精确等于`epoch_size`；
- 某层配额大于其原始样本数时，先在该层内完整洗牌并访问一轮，然后才开始重复；
- 已选patch默认按`file_id`聚集，减少反复打开NetCDF文件；不会改变分层配额；
- DDP下所有rank先确定性地生成相同的全局batch序列，再按batch位置互斥分片；`even_batches=True`保证各rank反向步数相同；
- 每轮训练前必须调用`sampler.set_epoch(epoch)`，才能在保持可复现的同时更换洗牌序列；
- val/test不用该Sampler，必须完整且不重复地遍历所有Patch，否则指标会受训练采样权重污染。

默认权重的目标是保留可信背景负例，同时提高稀疏填充和强反射率Patch的出现频率。它仅是首训默认值，后续必须结合完整val的支持域与dBZ分层指标判断是否过采样。

### 18.3 高度保持的共享3D U-Net

`Stage2UNet3D`复用已验证的各向异性Stage 1骨干，默认`base_channels=16`、通道倍率`(1,2,4,8,16)`，参数量为1,986,498。编码、解码和跳连全程保留60个物理高度层：

| 位置 | 主要操作 | 默认张量形状 |
|---|---|---:|
| 输入 | 4个GR-only通道 | `(B,4,64,64,60)` |
| stem | 各向异性残差块 | `(B,16,64,64,60)` |
| encoder 1 | 水平下采样 | `(B,32,32,32,60)` |
| encoder 2 | 水平下采样 | `(B,64,16,16,60)` |
| encoder 3 | 水平下采样 | `(B,128,8,8,60)` |
| bottleneck | 水平下采样+dropout | `(B,256,4,4,60)` |
| decoder 1 | 上采样+拼接encoder 3 | `(B,128,8,8,60)` |
| decoder 2 | 上采样+拼接encoder 2 | `(B,64,16,16,60)` |
| decoder 3 | 上采样+拼接encoder 1 | `(B,32,32,32,60)` |
| 共享解码特征 | 上采样+拼接stem | `(B,16,64,64,60)` |
| 支持域头 | `1x1x1 Conv3d` | `(B,1,64,64,60)` |
| 反射率头 | `1x1x1 Conv3d` | `(B,1,64,64,60)` |

每个各向异性残差块先用`(3,3,1)`核聚合同高度的水平结构，再用`(1,1,5)`核学习相邻5层的垂直廓线。下采样卷积核/步长均为`(2,2,1)`，只将scan/ray各减半；上采样以skip张量的`(D,H,Z)`为目标尺寸，其中`Z`始终为60。这保留每个输出体素与原始0.25 km高度格点的一一对应。

### 18.4 两个输出的数值语义

1. `support_logits`是未限制实数，不是0/1和概率。训练时直接传给BCE-with-logits或Focal loss；评价/推理时才计算`sigmoid(support_logits)`并使用验证集选定的阈值。
2. `reflectivity`是逐高度标准化的DPR dBZ实数，不限制正负。转回物理dBZ时：

\[
\widehat{Z}_{DPR}(i,j,z)
=\widehat{Z}'_{DPR}(i,j,z)\,\sigma_{DPR}(z)+\mu_{DPR}(z).
\]

两个头都在共享解码特征上使用独立`1x1x1 Conv3d`。反射率不乘以`sigmoid(support_logits)`，因此支持域头训练初期的假阴性不会切断dBZ头梯度。生成最终稀疏物理场时才可用阈值后的支持域做mask，但同时必须保留未门控dBZ供连续评价和调阈值。

初始化时：

- 支持域头bias默认为`logit(0.04)=-3.178...`，使未训练模型的初始正例概率接近数据集的稀疏量级；
- dBZ头bias为0，即每个高度的train物理dBZ均值；
- 两个头的权重均用小方差正态分布初始化，避免未训练输出大幅震荡。

### 18.5 双mask组合损失

支持域与dBZ回归不能共用一个mask：

```text
M_support = support_loss_mask
          = core_mask & pre_dpr原生有效且非负

y_support = target_support
          = DPR有物理dBZ时为1，否则为0

M_dbz     = regression_mask
          = core_mask & DPR有物理dBZ
          = M_support & (y_support == 1)
```

支持域损失在`M_support`内同时学正例和可信负例：

\[
L_{support}=\operatorname{MaskedBCEWithLogits}
(\widehat{s}, y_{support};M_{support},w_{pos}).
\]

代码也支持masked sigmoid Focal loss（`gamma`和`alpha`可配）。BCE的API默认`pos_weight=1.0`；正式`stage2_unet3d.yaml`首训配置使用`pos_weight=5.0`。采样后每轮预计正例体素比例仍只约3.4%，直接取完全反频率权重会接近28；5.0是较保守的首训值，用于提升检出梯度又避免与Patch过采样叠加后导致大量虚警。使用Focal时不再同时使用`pos_weight`，防止正例重复加权。

dBZ损失只在`M_dbz`内计算标准化空间的Smooth L1：

\[
L_{dbz}=\operatorname{MaskedSmoothL1}
(\widehat{Z}'_{DPR}, Z'_{DPR};M_{dbz},\beta).
\]

总损失为：

\[
L=\lambda_{support}L_{support}+\lambda_{dbz}L_{dbz}.
\]

当前API默认`lambda_support=lambda_dbz=1`、`beta=0.2`。两个任务权重和`pos_weight`是不同概念：前者平衡两个任务对共享骨干的梯度，后者只平衡支持域BCE内的正负例。D及之前的实验等价于所有`M_dbz=1`体素的`regression_weights=1`。

E3-W1.5正式启用`regression_weights`。Dataset必须先在标准化前读取物理DPR dBZ，再逐体素生成：

```text
physical dbz_dpr < 25 dBZ:                  1.00
25 dBZ <= physical dbz_dpr < 35 dBZ:       1.25
physical dbz_dpr >= 35 dBZ:                 1.50
M_dbz=0（含DPR缺测、halo、padding）:       0.00
```

单样本权重为`float32 (1,64,64,60)`，DataLoader堆叠后为`(B,1,64,64,60)`。分箱不得使用已标准化的`target_dbz`，因为各高度的均值和方差不同，同一标准化值不对应固定的物理强度。加权回归损失为：

\[
L_{dbz}^{E3}=
\frac{\sum_i M_{dbz,i}w_i\,
\operatorname{SmoothL1}(\widehat Z'_{DPR,i},Z'_{DPR,i};\beta=0.2)}
{\sum_i M_{dbz,i}w_i}.
\]

分母是有效权重和，不是`M_dbz`体素数。因此epoch与DDP跨rank汇总也必须保留各batch的损失分子与`reflectivity_weight_sum`，不能用有效体素数对各batch的加权均值再次加权。`M_support`、`y_support`、`M_dbz`、支持域BCE与模型输入全部不变。`regression_weights`是只在训练/验证loss中使用的标签派生元数据，不是第5个输入通道，整轨推理不需要它。

实现还会显式拒绝以下静默错误：

- `regression_mask`不是`support_loss_mask`的子集；
- dBZ回归mask中包含`target_support=0`的体素；
- loss mask不是`bool`，或者不能广播到预测形状；
- 实际被监督的目标中出现NaN/Inf；
- 两个任务权重同时为0。

对mask外的NaN/Inf会先换成中性占位再调用BCE，保证被忽略的值不会污染损失或梯度。当某个Patch的有效mask为空时，返回与计算图相连的0，反向传播仍然安全。

### 18.6 一个真实Patch的联调结果

使用正式train索引、正式归一化统计和真实NC文件，已完成一次CPU前向/双loss/反向联调：

```text
inputs:                    (1,4,64,64,60)
support_logits:            (1,1,64,64,60)
reflectivity:              (1,1,64,64,60)
support supervised voxels: 92,202
support positive voxels:    7,818
dBZ supervised voxels:      7,818
support loss:                1.402469
dBZ loss:                    0.869931
total loss:                  2.272400
support/dBZ头梯度:       均有限
```

这些loss数值只是随机初始化小模型的管线健康检查，不是训练性能结果，也不用于选超参。

### 18.7 padding、缺测占位和loss之间的对应

| 位置/数据状态 | 连续输入值 | GR mask通道 | `core_mask` | 进支持域loss | 进dBZ loss |
|---|---:|---:|---:|---:|---:|
| 真实网格且GR有物理dBZ | 标准化GR dBZ | `value=1`，`native_available=1` | 仅核心为1 | 视`pre_dpr`标签域 | 仅DPR有dBZ |
| 真实网格且GR为sentinel | 0（标准化占位） | `value=0`，`native_available=1` | 仅核心为1 | 视`pre_dpr`标签域 | 仅DPR有dBZ |
| 真实网格且GR原生missing | 0（标准化占位） | 两者均0 | 仅核心为1 | 视`pre_dpr`标签域 | 仅DPR有dBZ |
| 轨道外halo或`ray 49:64`padding | 0 | 两者均0 | 0 | 否 | 否 |
| DPR无物理dBZ但在可信分类域 | `target_dbz=0`占位 | 与GR状态无关 | 1 | 是，目标为0 | 否 |
| DPR有物理dBZ | 标准化DPR dBZ目标 | 与GR状态无关 | 1 | 是，目标为1 | 是 |

特别注意：连续通道中的`0`可能是“同高度train均值”、“mask外占位”或“padding”，必须联合GR mask通道和`geometry/core mask`才能区分。不得把这些0反标准化后解释为真实DPR dBZ。

### 18.8 当前测试覆盖

- 新增Sampler/Loss/Model定向单元测试21项全部通过；
- Stage 2数据、审计、基线、指标、Sampler、Loss、Model、训练引擎、滑窗、阈值、评价、可视化、最近GR距离、3/4/5通道与E3加权契约的85项测试全部通过；
- 项目完整回归测试数见第19.8节最终交付检查；
- 已检查高度轴在每个encoder层仍为60，支持域与dBZ头的梯度均能到达共享骨干，双输出未被硬门控。

## 19. Stage 2正式训练、整轨评价与任务mask精确含义

### 19.1 一个输出体素在什么条件下才被监督

以batch内一个体素`(b,0,d,h,z)`为例。下面的“为1”都指该体素处的布尔mask为`True`。

#### `core_mask`：该点是否属于本Patch独占的输出核心

`core_mask=1`必须同时满足：

1. scan位置在当前32条非重叠输出核心内，不是左右各16条上下文halo；
2. 该scan真实存在于原轨道，不是轨道首尾补出的位置；
3. ray是原始49条之一，不是为适配U-Net而补出的`ray=49...63`；
4. 高度是原始60层之一。Stage 2不在高度轴padding，所以没有伪造高度层。

同一条轨道的各核心互不重叠且合起来精确覆盖`nscan`，因此每个真实scan在loss中最多出现一次。halo可以进模型提供上下文，但永远不进loss。

#### `M_support = support_loss_mask`：该点是否有资格参与“DPR反射率有/无”分类

\[
M_{support}=core\_mask\;\land\;valid(pre\_dpr)\;\land\;(pre\_dpr\ge0).
\]

`M_support=1`必须同时满足：

1. `core_mask=1`；
2. `pre_dpr`不是NetCDF mask、NaN、Inf或`<=-9990`的填充码；
3. `pre_dpr>=0 mm/h`。显式的`pre_dpr=0`是合法负样本，仍然使`M_support=1`；
4. Stage 2这里不因CFB删除体素，即CFB以下只要`pre_dpr`标签有效，仍可进入支持域分类loss。`below_cfb_target_mask`单独保留供分组评价。

`M_support=0`表示“这里不计算分类loss”，不表示“这里没有DPR反射率”。尤其是halo、padding和无效`pre_dpr`都不能被当作大量真负例。

#### `y_support = target_support`：在可监督位置，DPR是否真有物理dBZ

\[
y_{support}=1 \iff dbz\_dpr\text{是未被mask的有限值且}>-9990.
\]

- `y_support=1`：DPR在该格点保留了一个物理dBZ。该dBZ可以很弱甚至为负，“有反射率数值”不等于“dBZ大于0”；
- `y_support=0`：DPR在该格点是native missing或有限sentinel，没有可用物理dBZ；
- 只有`M_support=1`时，`y_support`才参与BCE/Focal。`M_support=0`位置的`y_support`数值不产生损失或梯度。

在本数据集254个文件的审计中，DPR物理dBZ有效点与`pre_dpr>0`逐体素完全一致；但代码仍分开从两个原始变量构建mask，并由loss检查子集关系，防止未来数据版本变化被静默忽略。

#### `M_dbz = regression_mask`：该点是否参与dBZ数值回归

\[
M_{dbz}=core\_mask\;\land\;dpr\_value
       =M_{support}\;\land\;(y_{support}=1).
\]

`M_dbz=1`必须同时满足：

1. 该点在非重叠输出核心的真实网格中；
2. DPR确实有物理dBZ，即`y_support=1`；
3. 当前数据下它必然也在可信`pre_dpr`分类域，即`M_support=1`。

`M_dbz=0`时，`target_dbz`中的0只是标准化空间占位，绝不参与Smooth L1。最重要的是，`M_dbz`不包含“模型预测支持域为1”这一条件；即使支持域头暂时漏报，dBZ头仍可以从所有真实DPR目标接收梯度。

### 19.2 四种典型体素实际如何进loss

| 体素状态 | `M_support` | `y_support` | `M_dbz` | 学习任务 |
|---|---:|---:|---:|---|
| 核心内、`pre_dpr`合法、DPR有物理dBZ | 1 | 1 | 1 | 同时学“这里应该有DPR回波”和具体dBZ |
| 核心内、`pre_dpr=0`、DPR无物理dBZ | 1 | 0 | 0 | 只学支持域负例，不把`target_dbz=0`当成0 dBZ回归 |
| 核心内、`pre_dpr`无效/缺测 | 0 | 不使用 | 0 | 两个任务都不监督 |
| halo、轨道外位置或`ray 49:64`padding | 0 | 不使用 | 0 | 可作上下文或占位，不进任何loss |

GR是否有物理dBZ不决定上表的loss资格。因此`Q01`（GR无值、DPR有值）仍是完整的正例+回归目标，这正是Stage 2学习空间填充的核心。

### 19.3 正式首训损失与每轮归约

`configs/stage2_unet3d.yaml`的正式首训配置为：

```text
support loss:       masked BCE-with-logits
support pos_weight: 5.0
lambda_support:     1.0
dBZ loss:           masked Smooth L1 in standardized dBZ
Smooth L1 beta:     0.2
lambda_dbz:         1.0
training threshold: 0.5（只用于每轮诊断指标，不进loss）
```

每个batch内，BCE按`M_support=1`的体素数归约，dBZ Smooth L1按`M_dbz=1`的体素数归约。每轮日志重新用全部batch的各自体素数加权汇总，不会把“只有很少DPR目标的batch”与“数万个DPR目标的batch”等权平均。DDP下两个分母也会跨rank求和。

当某个背景Patch没有任何`M_dbz=1`体素时，dBZ分项返回与计算图连接的0；支持域分项仍然正常学习负例，DDP static graph也不会因此改变。

`configs/stage2_unet3d_4ch_distance_intensity_w1p5.yaml`是当前E3严格单因素配置。它与D保持相同的四通道输入`(B,4,64,64,60)`、split、seed、Sampler、模型宽度、支持域损失、优化器、调度器和早停策略，并从头训练；唯一改动是上述`1.00/1.25/1.50`的dBZ回归权重。

E3的加权验证loss适合在E3训练轨迹内选择checkpoint，但不能与D的未加权`val_loss`直接比大小。跨实验结论必须来自相同完整val整轨上的未加权物理dBZ指标、支持域/FSS指标、强度分箱和逐轨配对结果。

### 19.4 `best.pt`、早停和阈值的先后关系

训练时用不依赖支持域阈值的验证组合损失选模：

```text
val_loss = 1.0 * global masked BCE + 1.0 * global masked dBZ Smooth L1
```

- `best.pt`只在`val_loss`改善时覆盖；
- 早停默认patience=15、min_delta=1e-4，也只监控`val_loss`；
- 每轮日志中0.5阈值的Precision/Recall/CSI是诊断量，不决定`best.pt`；
- 训练完成后才加载`best.pt`，用**完整val整轨**选最终支持域阈值；
- test只读取已保存的val阈值文件，不得在test上重新调阈值。代码会拒绝`selected_on_split != "val"`的阈值文件。

当前阈值候选是0.05到0.95，步长0.05，默认最大化全val体素汇总CSI。如CSI并列，先取更靠近0.5的阈值，再取较小者。选择过程流式累积TP/FP/FN/TN，不需要把38条val轨道同时保存在内存。

### 19.5 从Patch双输出到完整轨道

```text
单Patch模型输出:       (1,1,64,64,60)
裁掉左右halo:            (1,1,core_length,64,60)
裁掉ray高端padding:       (1,1,core_length,49,60)
去掉batch/channel轴:          (core_length,49,60)
按core_start放回整轨:   (nscan,49,60)
```

支持域logits和标准化dBZ使用两个独立重建器，每条scan只允许写入一次；重叠、越界或未完整覆盖都会报错。重建后：

```text
support_logits:             (nscan,49,60)
support_probability:        (nscan,49,60) = sigmoid(logits)
reflectivity_standardized:  (nscan,49,60)
reflectivity_dbz:           (nscan,49,60)
predicted_support:          (nscan,49,60) = probability >= val_threshold
```

用真实test轨道已验证：原形状`(97,49,60)`能从多个`(1,4,64,64,60)`窗口精确还原为`(97,49,60)`，不保留halo或64-ray padding。

### 19.6 模型评价必须同时看的两套dBZ指标

1. `reflectivity_on_target_support`：在所有`M_dbz=1`点上评价未门控的dBZ头。它与支持域阈值无关，回答“只要DPR真有目标，连续头能不能给准dBZ”。
2. `reflectivity_on_common_support`：只在`M_dbz=1 & predicted_support=1`上评价，回答“最终通过支持域门控而对外输出的dBZ有多准”。它可能因模型只命中容易点而显得更好，必须与Recall一起看。

支持域还输出Precision、Recall/POD、CSI、F1、FAR、Specificity，以及完整轨道上的水平FSS-r1/r2/r4。每轮默认不计算Patch FSS，因为Patch边界邻域不完整且计算昂贵；训练后只在完整整轨上计算正式FSS。

`evaluate_stage2_unet3d.py`同时保存：

```text
metrics.json             # 全split汇总
per_file.csv             # 逐轨
per_height.csv           # 60个绝对高度
per_region.csv           # Q11/Q01/Q10/Q00、gap/outside、CFB、dBZ分箱
threshold_curve.csv      # 仅val选阈值时生成
support_threshold.json   # 包含val来源声明的最终阈值
orbits/*.npz             # 可选保存的整轨数组
```

### 19.7 训练完成后的自动调用顺序

rank 0在所有DDP进程结束且释放训练模型后顺序执行：

```text
best.pt
  → 完整val整轨候选阈值搜索
  → 用最优val阈值生成val指标
  → 冻结该阈值，生成test指标
  → 固定seed选取test轨道并绘图
```

每张预测图包含：GR原始物理值支持域、DPR支持域、支持域概率和阈值结果、目标/预测dBZ平面、dBZ误差、散点相关、自动选择ray的A-B垂直剖面、逐高度Recall/CSI/MAE和正负样本支持域概率分布。

### 19.8 真实数据与端到端联调

已完成三层联调：

1. 真实train Patch `(1,4,64,64,60)`通过新训练引擎完成一次AdamW更新和验证；`M_support=92,202`，其中`y_support=1`与`M_dbz=1`均为7,818，参数和损失全部有限。
2. 真实test轨道从Patch输出完整重建回`(97,49,60)`，支持域概率和物理dBZ全部有限。
3. 使用临时随机checkpoint实际跑通“1条val轨道选阈值→1条test轨道固定阈值评价→1条测试轨道生成PNG”。随机模型的指标仅用来确认管线健康，不代表模型性能。

当前Stage 2定向测试85项全部通过；项目完整回归测试共236项全部通过。回归范围同时覆盖既有Stage 1数据、模型、训练与评价代码，说明E3加权扩展没有破坏第一阶段或已有Stage 2管线。

## 20. Stage 2 E2结论与E3-W1.5数据契约

### 20.1 E2不取代当前D主模型

下表都来自相同完整val的38条轨道，支持域阈值均只在val上选择：

| 检查点 | CSI | Recall | FAR | FSS-r2 | MAE (dBZ) | RMSE (dBZ) | Bias (dBZ) | Pearson r | CCC |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| D `best_joint` | **0.5771** | **0.6893** | 0.2200 | 0.8582 | 3.8408 | **5.3521** | **-0.2594** | 0.6966 | 0.6749 |
| E2 `best_dbz` | 0.5727 | 0.6592 | **0.1865** | 0.8552 | **3.7971** | 5.3659 | -0.5608 | **0.6982** | **0.6756** |
| E2 `best_support` | 0.5719 | 0.6733 | 0.2084 | **0.8603** | 3.9093 | 5.5954 | -1.3867 | 0.6830 | 0.6380 |

E2 `best_dbz`相对D把MAE改善了0.0437 dBZ、FAR改善了0.0335，但Recall下降了0.0301，CSI下降了0.0044，RMSE也轻微增加。逐轨结果并不支持将这一小幅pooled MAE改善解读为稳定全面提升。E2 `best_support`只小幅恢复Recall，却使MAE、RMSE、Bias和相关性全部明显退化。

因此：

1. D `best_joint`继续作为Stage 2当前平衡主模型；
2. E2 `best_dbz`只作为低MAE/低虚警的Pareto诊断候选；
3. E2实验结束，不继续组合“距离+插值”六通道；
4. 下一步回到D，仅检验强回波尾部权重。

### 20.2 E3从原始物理值到batch权重的完整流程

```text
原始 dbz_dpr:                    (nscan,49,60), 物理dBZ
  -> classify_reflectivity_storage
  -> 只在dpr_value=True处依物理dBZ分箱
       <25          -> 1.00
       [25,35)      -> 1.25
       >=35         -> 1.50
  -> 截取当前64-scan核心+上下文: (64,49,60)
  -> ray高端padding到64:             (64,64,60)
  -> 增加channel轴:                    (1,64,64,60)
  -> M_dbz=0处填0.0
  -> DataLoader堆叠:                    (B,1,64,64,60)
                                       = regression_weights
```

这一流程与`target_dbz`的数值流程不同：`target_dbz`还要做逐高度标准化，而`regression_weights`在物理dBZ分箱后不再标准化。两者形状相同，但前者是回归标签，后者只决定每个`M_dbz=1`体素在Smooth-L1分子和分母中的贡献。

E3最终进入模型的输入仍是：

```text
inputs:              (B,4,64,64,60)
  [sparse_dbz_std, gr_value_mask, nearest_distance_scaled, height_scaled]
support_logits:      (B,1,64,64,60)
predicted_dpr_dbz:   (B,1,64,64,60)
```

`regression_weights`不会进入上述四通道张量，也不改变模型输出。它仅在训练/验证时与`target_dbz`、`M_dbz`一起传入损失；推理和整轨重建时不需要它。

## 21. Stage 2→Stage 1串联接口与Stage 3冻结实验契约

### 21.1 当前F0全冻结数据流

当前Stage 3起点使用W1.25 Stage 2和T3D Stage 1：

```text
Stage 2 GR输入:
x_gr:                         (B,4,64,64,60)
  [0] sparse_dbz_standardized
  [1] gr_value_mask
  [2] nearest_gr_distance_scaled
  [3] height_scaled

Stage 2输出:
support_logits:               (B,1,64,64,60)
support_probability:          (B,1,64,64,60)
predicted_dpr_std_s2:         (B,1,64,64,60)

串联标准化bridge:
predicted_dpr_physical_dbz:   (B,1,64,64,60)
predicted_dpr_std_s1:         (B,1,64,64,60)

Stage 1输入:
x_stage1:                     (B,3,64,64,60)
  [0] predicted_dpr_std_s1
  [1] dpr_support_channel
  [2] height_scaled

Stage 1输出:
predicted_log_rain:           (B,1,64,64,60)
predicted_rain_mm_h:          (B,1,64,64,60)
type_logits（结构保留时）:    (B,3,64,64)
```

Stage 2与Stage 1使用不同元数据契约。正确转换为：

\[
\widehat Z_{phys}(z)
=\widehat Z'_{S2}(z)\sigma_{S2}(z)+\mu_{S2}(z),
\]

\[
\widehat Z'_{S1}(z)
=\frac{\widehat Z_{phys}(z)-\mu_{S1}(z)}{\sigma_{S1}(z)}.
\]

两组`mu/sigma`都是长度60的逐高度统计，广播后参与`(B,1,64,64,60)`张量运算。不得将Stage 2标准化值直接送入Stage 1。

### 21.2 串联中四种关键mask的精确含义

#### `true_dpr_support`

对一个体素，只有原始`dbz_dpr`被解析为有限、非fill、物理dBZ时才为1。它是DPR反射率是否存在的标签，不表示降水率一定大于0。Stage 3的`O`版使用该mask做科学诊断，但部署时不可获得。

#### `support_probability`

```text
support_probability = sigmoid(support_logits)
```

它表示Stage 2对“此体素存在DPR物理dBZ”的连续置信度，取值在`[0,1]`。它只由GR输入推断，因此可供部署使用。

#### `predicted_support_hard`

```text
predicted_support_hard = support_probability >= 0.8
```

`0.8`是当前只在val选定的W1.25阈值。它可用于F0和硬P版推理对照，但比较运算不可导，不得出现在“冻结Stage 1、用rain loss训练Stage 2”的反向路径中。

#### `rain_label_mask`

降水loss与metric mask必须由原始`pre_dpr`、DPR反射率QC、CFB和核心几何独立构造。不得执行：

```text
rain_label_mask &= predicted_support_hard
```

否则Stage 2漏掉的真实降水体素会从损失和评价中消失。当前主口径的`reliable_positive_mask`只评价可靠正降水强度；若后续允许模型在support外预测降水，还必须新增包含有效零降水的`qc_label_domain_including_zero`训练与评价口径。

### 21.3 O版从Stage 2输出到Stage 1输入的数值处理

```text
predicted_dpr_std_s2:         (B,1,64,64,60)
  -> 反标准化到物理dBZ
predicted_dpr_physical_dbz:   (B,1,64,64,60)
  -> 按Stage 1统计标准化
predicted_dpr_std_s1:         (B,1,64,64,60)
  -> true_dpr_support=0处填0.0
oracle_dbz_channel:           (B,1,64,64,60)
  -> 拼接true_dpr_support与height
x_stage1_oracle:              (B,3,64,64,60)
```

这里`oracle_dbz_channel=0.0`的含义是“在Stage 1标准化空间使用该高度train均值作占位”，不是0 dBZ。Stage 1必须联合第二通道`true_dpr_support=0`才能理解它是占位值。

### 21.4 `S3-C1-O-S1Adapt`的梯度路径

```text
Stage 2:
  eval()
  requires_grad=False
  forward使用no_grad/inference_mode
        |
        v
fixed predicted_dpr_std_s2
        |
        v
Stage 1: trainable
        |
        v
rain loss = L_I + 0.02 L_G
```

形状不变：

```text
x_gr                 (B,4,64,64,60)
x_stage1_oracle      (B,3,64,64,60)
predicted_log_rain   (B,1,64,64,60)
```

首轮C1-O只训练rain路径，暂时关闭T3D类型损失，避免额外多任务优化干扰“Stage 1能否适应Stage 2值场”的单因素判断。

### 21.5 `S3-C2-O-S2TaskAware`的梯度路径

```text
Stage 2: selected blocks trainable
        |
        v
predicted_dpr_std_s2
        |
        v
differentiable Torch normalization bridge
        |
        v
Stage 1:
  eval()
  requires_grad=False
  但forward不能使用no_grad/inference_mode
        |
        v
rain loss
        |
        `---- 梯度穿过冻结Stage 1返回Stage 2
```

该路径中禁止：

- 将中间张量转成NumPy；
- 对Stage 2 dBZ调用`.detach()`；
- 在逐高度标准化bridge中使用会不必要截断有效梯度的硬裁剪；
- 在可导路径上做`support_probability >= threshold`；
- 用预测support修改标签loss mask；
- 将整个Stage 1前向包在`torch.no_grad()`中。

C2-O损失为：

\[
L_{C2}
=L_{dbz}^{W1.25}
+L_{support}
+\lambda_R(L_I+0.02L_G).
\]

`L_dbz`和`L_support`是中间物理锚定，用来防止Stage 2产生只能迎合冻结Stage 1、但不再符合DPR dBZ语义的隐编码。

当前实现进一步固定了以下数据和优化契约。

#### C2完整Patch与标签侧张量

C1为了只适配rain映射而使用正降水Patch；C2不能继续这样做，否则背景体素和support负样本会大量消失。`Stage3C2PatchDataset`因此使用完整Stage 2索引：train为2076个Patch、val为437个Patch，并恢复W1.25原本的四类采样配额。

```text
Stage 2可部署输入：                  (B,4,64,64,60)
  [dbz_gr_sparse_standardized,
   gr_value_mask,
   gr_nearest_distance_scaled,
   height_scaled]

oracle诊断附加通道：                (B,2,64,64,60)
  [true_dpr_support_oracle,
   stage1_height_scaled_copy]

DataLoader packed_inputs：           (B,6,64,64,60)

Stage 2标签侧（不进入模型）：
  stage2_target_support              (B,1,64,64,60), float 0/1
  stage2_support_loss_mask           (B,1,64,64,60), bool
  stage2_target_dbz                  (B,1,64,64,60), 标准化dBZ
  stage2_regression_mask             (B,1,64,64,60), bool
  stage2_regression_weights          (B,1,64,64,60), float

Stage 1标签侧（不进入模型）：
  target                             (B,1,64,64,60), log1p(mm/h)
  loss_mask/reliable_loss_mask       (B,1,64,64,60), bool
  loss_weights                       (B,1,64,64,60), float
  height_km                          (B,1,1,1,60), km
```

这些mask为1的具体条件保持原有定义：

- `stage2_support_loss_mask=1`：该体素位于非重叠输出核心、真实轨道几何和可信GR–DPR占用域内；这里同时监督`target_support=0`和`1`；
- `stage2_regression_mask=1`：除满足核心/几何条件外，该处还存在一个可读取的物理DPR dBZ值，即`M_dbz=1`；它必然是`stage2_support_loss_mask`和正support标签的子集；
- `stage2_regression_weights>0`：只在`stage2_regression_mask=1`处，根据真实物理DPR dBZ分为`<25/25–35/>=35 dBZ`，权重为`1.00/1.10/1.25`；
- `loss_mask=1`：Stage 1原有可靠正降水监督（以及配置存在时的显式弱监督）；当前封版配置不加入新的CFB弱监督；
- `reliable_loss_mask=1`：位于非重叠核心，DPR反射率有效、CFB可靠且`pre_dpr>0`，用于最终rain指标和G的连续高度pair。

#### C2实际分支输入、解冻范围与梯度

```text
packed_inputs[:,0:4]
  -> Stage 2
  -> support_logits                  (B,1,64,64,60)
  -> predicted_dpr_std_s2            (B,1,64,64,60)

predicted_dpr_std_s2
  -> S2逐高度反标准化到物理dBZ
  -> S1逐高度重新标准化
  -> true_dpr_support=0处填S1标准化0.0
  -> 拼接packed_inputs[:,4:5]和[:,5:6]

x_stage1_oracle                      (B,3,64,64,60)
  [predicted_dpr_std_s1,
   true_dpr_support_oracle,
   height_scaled]
  -> 冻结Stage 1
  -> predicted_log_rain              (B,1,64,64,60)
```

原始Stage 1 T3D epoch 22的全部参数均`requires_grad=False`并保持`eval()`，但前向不能使用`no_grad`。Stage 2从W1.25 epoch 27初始化，首轮只解冻：

```text
stage2_model.decoder[-1]             # 最高水平分辨率解码块
stage2_model.reflectivity_head       # dBZ输出头
合计可训练参数：7025
双网络总参数：4,008,886
```

Stage 2其余模块保持`eval()`，因此冻结bottleneck中的dropout不会产生随机漂移。`support_head`本身冻结，但`L_support`仍能经固定head权重把梯度传到共享`decoder[-1]`。

#### `lambda_R`只由训练batch决定

正式训练前，每个DDP rank从Stage 2分层训练Sampler中读取4个具有rain监督的batch，分别计算相对于上述7025个可训练参数的梯度范数：

```text
g_anchor = ||grad(L_dbz^W1.25 + L_support)||_2
g_rain   = ||grad(I + 0.02G)||_2

lambda_R_unclamped
  = 0.25 * geometric_mean(g_anchor / g_rain)

lambda_R
  = clip(lambda_R_unclamped, 1e-4, 1.0)
```

三卡正式运行会合并最多12个有效训练batch的对数比值；val/test完全不参与权重选择。所选权重、原始范数、是否触发截断和源检查点哈希写入`gradient_scale_audit.json`及每个checkpoint。真实CPU单batch冒烟得到`g_anchor≈0.649`、`g_rain≈0.145`、未截断权重约1.121，安全上限后`lambda_R=1.0`；该数值仅证明实现可运行，正式三卡训练以自动审计的全局结果为准。

#### C2训练、选模与存储

C2固定训练20个epoch，学习率从`1e-5`余弦下降到`1e-6`。当前配置关闭早停，避免像C1一样在尚未进入低学习率阶段时结束；最终仍从完整轨迹独立保存：

- `best.pt`与`best_rain.pt`：验证Patch最终rain RMSE最低；
- `best_joint.pt`：复合目标最低；
- `best_dbz.pt`：Stage 2标准化dBZ锚点损失最低；
- `best_support.pt`：support锚点损失最低；
- `last.pt`：最后一轮；
- `epoch_0009.pt/epoch_0019.pt`：每10轮周期检查点，不逐轮保存。

正式训练后先对`best.pt`的support在完整val重新选择阈值，再执行Stage 2整轨物理评价和Stage 2→冻结Stage 1的完整38轨2×2串联评价。oracle-support的最终rain是C2方向的主判定项；predicted-support只用于同时审计部署接口，不能反过来改变oracle训练loss mask。

### 21.6 P版与soft-support是两个不同实验

硬P版使用：

```text
predicted_support_hard:       (B,1,64,64,60), bool
predicted_dpr_std_s1在mask外填0
x_stage1_hard:                (B,3,64,64,60)
```

soft-support版使用：

```text
z_soft = support_probability * predicted_dpr_std_s1
x_stage1_soft:                (B,3,64,64,60)
  [z_soft, support_probability, height_scaled]
```

soft-support会改变输入数值语义和梯度路径，不得与硬P版混成同一个实验。若soft版不再对最终rain做硬support门控，任务就已从“已知可靠正降水上的强度反演”扩展为“降水发生+强度”，必须加入有效零雨监督和降水发生指标。

### 21.7 局部联合与直接多头模型的张量

`S3-C3-PartialJoint`保留与C2相同的张量链，但同时解冻Stage 1的接口附近模块和Stage 2的末级解码/输出头。只有C1/C2中至少一个单边实验有效时才能进入C3。

`S3-D0-DirectMultiHead`不构造Stage 1三通道中间输入，而是从GR共享特征直接产生三个头：

```text
x_gr:                   (B,4,64,64,60)
  -> shared_3d_features
  -> rain_log:           (B,1,64,64,60)  # 主任务
  -> predicted_dpr_std:  (B,1,64,64,60)  # 辅助dBZ
  -> support_logits:     (B,1,64,64,60)  # 辅助support
```

该路线保留dBZ/support物理监督，但最终降水不再必须穿过单个硬dBZ+mask瓶颈。

#### D0实际Dataset：卫星变量只在标签侧

`Stage3D0PatchDataset`仍用Stage 1和Stage 2索引做逐文件、逐核心严格对齐，但会丢弃Stage 1的三个输入通道。送入模型的只有Stage 2 W1.25四个可部署GR通道：

```text
原NC dbz_gr_sparse:              (nscan,49,60)
  -> 物理值/缺测三态解析
  -> train-only逐高度标准化；缺测位置填标准化0.0
  -> Patch上下文+水平padding

一个Dataset输入inputs:           (4,64,64,60)
  channel 0 dbz_gr_sparse_standardized
  channel 1 gr_value_mask
  channel 2 gr_nearest_distance_scaled
  channel 3 height_scaled

DataLoader batch inputs:          (B,4,64,64,60)
```

四通道的缺测/padding数值规则与Stage 2距离模型完全相同：标准化dBZ缺测填`0.0`（该高度训练均值占位）、值mask填`0.0`、最近观测距离填`1.0`、高度通道按对应物理层广播；轨道外scan halo和`49→64`的ray padding不伪造观测。

以下张量由对齐的DPR/`pre_dpr`产生，只服务于loss和metric，绝不拼接进`inputs`：

```text
stage2_target_support             (B,1,64,64,60)
stage2_support_loss_mask          (B,1,64,64,60), bool
stage2_target_dbz                 (B,1,64,64,60), 标准化DPR dBZ
stage2_regression_mask            (B,1,64,64,60), bool
stage2_regression_weights         (B,1,64,64,60), W1.25
target                            (B,1,64,64,60), log1p(pre_dpr)
loss_mask/reliable_loss_mask      (B,1,64,64,60), bool
loss_weights                      (B,1,64,64,60), Stage 1强度权重
height_km                         (B,1,1,1,60)
```

因此D0训练仍保留完整train 2076个Patch和val 437个Patch，也继续使用W1.25四类采样比例`0.20/0.10/0.30/0.40`；它不是只抽取正降水Patch的条件回归DataLoader。

#### D0共享网络与三头形状

模型从W1.25 `best_dbz`加载stem、encoder、decoder、support head和dBZ head；只有rain head是新增参数：

```text
inputs                             (B,4,64,64,60)
stem                               (B,16,64,64,60)
encoder level 1                    (B,32,32,32,60)
encoder level 2                    (B,64,16,16,60)
encoder level 3                    (B,128,8,8,60)
encoder level 4                    (B,256,4,4,60)
decoder + skip connections         (B,16,64,64,60)
  |- support_head 1x1x1            (B,1,64,64,60), logits
  |- reflectivity_head 1x1x1       (B,1,64,64,60), 标准化DPR dBZ
  `- rain_head 1x1x1               (B,1,64,64,60), log1p(mm/h)
```

所有下采样/上采样仍只有`(scan,ray)`方向，60层高度从头到尾不压缩、不padding。rain输出不在网络内部乘support，避免hard threshold切断梯度。

#### D0-H、旧D0-D与D0-D-RainPrimary的可训练参数和损失

```text
D0-H rain_head_only:
  stem/encoder/decoder/support_head/reflectivity_head 冻结且eval
  只训练rain_head.weight+bias，共17参数
  5 epoch, AdamW 1e-4 -> 1e-5, lambda_R=1

D0-D decoder_and_all_heads:
  stem+encoder冻结且eval
  完整decoder、support_head、reflectivity_head、rain_head可训练
  20 epoch, AdamW 1e-5 -> 1e-6
  共589011个可训练参数
```

两个范围的数学目标相同：

\[
L_{D0}=L_{support}+L_{dbz}^{W1.25}+\lambda_R(L_I+0.02L_G).
\]

D0-H中两个物理loss仍被计算和记录，但对应参数全冻结，实际只对rain head反传；这样可确认W1.25共享特征本身是否可读。D0-D中三个任务共同更新decoder。梯度审计明确只在共享decoder上比较`anchor`与`rain`范数，不把互不接收对方梯度的三个专属head混入比值。

旧`D0-D-AnchorPrimary`保留上述公式用于复现，但不作为下一实验启动。原因是它让rain成为相对较弱的辅助梯度，与直接反演模型的目标相反。

当前`S3-D0-D-RainPrimary`从D0-H `best_rain.pt` epoch 2恢复包括rain head在内的完整模型，再解冻完整decoder和三个head。它严格反转任务主次：

\[
L_{D0\text{-}RP}=(L_I+0.02L_G)+\lambda_{phys}
  (L_{support}+L_{dbz}^{W1.25}).
\]

权重选择只读取train batch。在共享decoder参数上分别计算未加权物理梯度`g_phys`和rain梯度`g_rain`，随后选择：

```text
lambda_phys_unclamped
  = 0.25 * geometric_mean(g_rain / g_phys)
lambda_phys
  = clip(lambda_phys_unclamped, 1e-4, 1.0)

加权后的目标：
  |lambda_phys * g_phys| / |g_rain| = 0.25
rain_weight始终固定为1.0
```

真实CPU单batch烟雾审计得到`g_phys≈0.9055`、`g_rain≈0.3884`、`lambda_phys≈0.1072`且未截断；该值只证明实现和方向正确，正式三卡运行会合并最多12个有效训练batch并把最终值写入`gradient_scale_audit.json`和检查点metadata。

#### D0整轨重建与support口径

滑窗输入仍为重叠上下文，输出只保留每个非重叠核心，三头分别拼回`(nscan,49,60)`。rain log先做`expm1(max(log,0))`得到非负物理`mm/h`，之后才形成：

- `d0_oracle_support`：真实DPR support外填0，仅诊断rain强度头；
- `d0_predicted_support`：D0 support probability在完整val选择阈值后，预测support外填0，代表可部署输出。

两种模式使用同一个未门控rain轨道，完整val上的降水标签和评价mask也完全相同。评价脚本同时保存support probability、辅助dBZ、未门控rain、两个门控rain及统一轨道可视化输入，方便把强度错误与支持域错误分开。

#### C2结束结论与D0启动条件

C2正式train-only审计得到`lambda_R=0.6791856`，20轮训练后的最佳rain仍出现在早期；完整38轨oracle-support为`RMSE=3.9195 mm/h、r=0.4715`，匹配F0为`3.9180/0.4713`，没有达到`RMSE改善3%`或`r提高0.01`的继续门槛。predicted-support为`RMSE=3.9848、r=0.4545`，故C2-P和C3暂停。

D0-H已完成5轮正式训练和38轨val，但oracle-support仅为`RMSE=4.1538 mm/h、r=0.3728`，弱于匹配F0的`3.9180/0.4713`；predicted-support为`4.1997/0.3528`。因此冻结特征的17参数线性rain head被否决。RainPrimary现已通过单元测试、真实2076/437 Patch初始化契约、train-only梯度审计和CPU两训练/两验证batch端到端烟雾；下一步是正式三卡训练20轮。

### 21.8 Stage 3验证、测试和检查点纪律

- Stage 3的C0/C1/C2/P/D0/C3开发全部只使用完整38条val轨道；
- 不自动访问test，不在test上选择冻结方向、support表示、损失权重或解冻范围；
- 只在最终锁定1至2个候选后做一次test评价；
- 始终保存`best.pt`和`last.pt`；
- 周期检查点只每10个epoch保存：`epoch_0009.pt`、`epoch_0019.pt`、`epoch_0029.pt`等；
- C1/C2检查点只保存可训练部分，并记录冻结检查点路径、哈希与配置；
- D0保存完整单网络（共享骨干、三个head），但周期点仍只写`0009/0019/...`；5轮D0-H不产生周期pt；
- DDP下只允许rank 0写检查点、整轨指标和可视化。

### 21.9 `S3-C0-2x2`接口审计的实际数据流

对每个Stage 2模型，现有评价脚本在完整轨道上构造同一`dBZ值场 × support`的四个单元。所有物理轨道数组均为`(nscan,nray,60)`，切窗进入Stage 1后均为`(B,3,64,64,60)`：

```text
TT: true_dbz + true_support
PT: predicted_dbz + true_support
TP: true_dbz_counterfactual + predicted_support
PP: predicted_dbz + predicted_support
```

其中TP的逐体素处理是：

```text
true_support=1:
  true_dbz_counterfactual = 原始物理dbz_dpr

true_support=0:
  true_dbz_counterfactual = Stage 1训练集该高度mean_dbz[z]

随后按Stage 1统计标准化：
  true_support=0处的占位值 -> 0.0

Stage 1输入：
  channel 0 = 上述标准化dBZ，形状(B,1,64,64,60)
  channel 1 = predicted_support，形状(B,1,64,64,60)
  channel 2 = height_scaled，形状(B,1,64,64,60)
```

这样处理是因为`predicted_support=1 AND true_support=0`的假阳性位置没有可读取的真实DPR dBZ。填逐高度训练均值只定义中性占位，不伪造真实回波，也不偷用Stage 2预测值。审计将该限制写入`factorial_2x2.json`；`factorial_2x2.csv`逐指标保存TT/PT/TP/PP值、两个条件下的值场效应、两个条件下的support效应及交互项。

新轨道缓存还保存：

```text
true_dpr_support:                       (nscan,nray,60), bool
stage2_support_probability__<run>:      (nscan,nray,60), float32
stage2_predicted_support__<run>:        (nscan,nray,60), bool
stage2_reflectivity_dbz__<run>:         (nscan,nray,60), float32
rain__<mode>:                           (nscan,nray,60), float32
input_support__<mode>:                  (nscan,nray,60), bool
output_support__<mode>:                 (nscan,nray,60), bool
```

可视化先用`any(qc_label_mask, axis=z)`形成`(nscan,nray)`公共QC footprint，再只由该footprint和有限经纬度计算一次地理范围。所有方法的水平切片共享该范围、同一高度和同一色标；浅灰点为公共QC底图，青色虚线为真实DPR support，橙色实线为当前方法送入Stage 1的input support。预测降水本身仍只在`output_support=1`处着色，因此输入support与CFB等输出门控不会被混为同一个mask。

更完整的冻结顺序、通过门槛和停止规则见[第三阶段串联适配、联合优化与直接反演实验路线](./第三阶段串联适配、联合优化与直接反演实验路线.md)。

### 21.10 C1-O当前代码的DataLoader打包与实际分支输入

`Stage3C1PatchDataset`复用同一条轨道、同一`file_id/core_start/core_length`下的Stage 1和Stage 2索引。若Stage 1启用`positive_only`，它先取得原始`patch_index`，再用该原始编号读取未过滤的Stage 2 Patch，避免逻辑下标错配。构造时会一次性校验两套索引的文件路径、Patch数量、core起点/长度、水平几何和60层高度坐标。

DataLoader内部打包：

```text
Stage 2 GR输入：                 (B,4,64,64,60)
  [sparse_dbz_std_gr, gr_value_mask,
   nearest_gr_distance_scaled, height_scaled]

Stage 1标签侧只取：
  true_dpr_support_oracle：      (B,1,64,64,60)
  stage1_height_scaled_copy：    (B,1,64,64,60)

packed_inputs（传给串联封装器）：(B,6,64,64,60)
```

`packed_inputs`只是一次DataLoader传输容器，不是把6个通道同时送进某一个U-Net。封装器内部严格切分：

```text
packed[:,0:4] -> 冻结Stage 2 -> predicted_dpr_std_s2 (B,1,64,64,60)
packed[:,4:5] -> true_dpr_support_oracle             (B,1,64,64,60)
packed[:,5:6] -> Stage 1 height                      (B,1,64,64,60)

predicted_dpr_std_s2
 -> S2反标准化为物理dBZ
 -> S1逐高度重新标准化
 -> true_support=0处填标准化0.0
 -> 与true_support、height拼接

x_stage1_oracle：                (B,3,64,64,60)
Stage 1 rain输出：               (B,1,64,64,60)
```

原始真实`dbz_dpr`只参与`true_dpr_support`判定、rain标签QC及诊断，真实dBZ数值通道在Dataset中被显式丢弃。单元测试会只改变NC中的真实DPR dBZ数值而保持support不变，并确认整个`packed_inputs`逐值不变，从而防止标签泄漏。

训练状态为：

```text
Stage 2全部1,986,498参数：requires_grad=False，始终eval/no_grad
Stage 1 T3D类型头：        保留在state_dict，但requires_grad=False且不前向
Stage 1共享干路+rain头：   1,986,321个可训练参数
loss：                     I + 0.02G，不含type CE
```

C1检查点中的`model`只保存Stage 1完整state_dict（包括未改动的类型头），因此仍可被现有Stage 1/串联评价器加载；冻结Stage 2检查点、threshold、两套normalization的绝对路径和SHA-256保存在`stage3_sources`。检查点只写`best.pt`、`last.pt`以及每10轮一次的`epoch_0009.pt/epoch_0019.pt/...`。

### 21.11 `S2-R1-O-DPRSparseValue`非部署空间补全上限

该实验只隔离“现有稀疏几何能否补全DPR数值场”，不能部署。原始轨道中各变量均为`(nscan,49,60)`：

```text
gr_value_mask：GR原始存储是有限物理dBZ时为1
dpr_valid：    DPR原始存储是有限物理dBZ时为1
anchor_mask = gr_value_mask AND dpr_valid
```

也就是说，只有同一体素既被GR直接观测、又有DPR真实反射率，`anchor_mask`才为1。数值流程为：

```text
物理dbz_dpr：(nscan,49,60)
  -> 使用训练集DPR逐高度mean[z]/std[z]标准化
  -> dpr_standardized：(nscan,49,60)
  -> anchor_mask=1：保留dpr_standardized
  -> anchor_mask=0：填0.0（标准化中性占位，不代表0 dBZ）
  -> dbz_dpr_sparse_anchor_standardized：(nscan,49,60)
```

距离通道不是Patch内计算：先在完整轨道上对每个高度分别寻找最近`anchor_mask=1`的水平体素，使用scan/ray Chebyshev距离，截断后缩放到`[0,1]`。因此Patch边界不会伪造新的“远距离”；轨道外halo和水平padding的距离填1.0。其他填充值如下：

| 通道 | 有效含义 | 缺锚点/轨道外/水平padding填充值 |
|---|---|---:|
| `dbz_dpr_sparse_anchor_standardized` | 锚点真实DPR标准化dBZ | `0.0` |
| `dpr_sparse_anchor_mask` | 是同体素GR-DPR锚点时为1 | `0.0` |
| `dpr_sparse_anchor_distance_scaled` | 到同高度最近锚点的截断缩放距离 | `1.0` |
| `height_scaled` | 60层物理高度线性缩放到`[-1,1]` | `0.0` |

切出`core=32 + 左右halo各16`后，ray从49补到64，高度不补：

```text
单样本inputs： (4,64,64,60)
DataLoader batch：(B,4,64,64,60)
单头U-Net输出： (B,1,64,64,60)，标准化DPR dBZ
```

真实DPR support没有拼进输入，而只用于构造损失mask：

```text
M_dbz = core_mask AND dpr_valid
形状：(B,1,64,64,60)，bool

M_dbz=1：该体素属于唯一输出core，且DPR有真实物理反射率，参与损失
M_dbz=0：halo、padding或DPR缺测，不参与损失
```

物理dBZ标签先按`<25/25–35/>=35`生成`1.0/1.10/1.25`的监督权重，再计算标准化空间Smooth-L1（`beta=0.2`）。权重仅是loss元数据，不是输入。模型不预测support，也没有support损失或support阈值。

因此训练采样也不再抽取`dpr_count=0`的纯背景Patch：背景/普通目标/fill-dominant/强目标权重为`0/0.125/0.375/0.50`，即删除原背景配额后保持W1.25三个目标层之间的相对比例；完整validation仍逐一覆盖所有Patch。

整轨推理只保留每个Patch不重叠的32-scan核心，依次拼回`(nscan,49,60)`；反标准化得到物理dBZ。评价和冻结Stage 1串联始终使用真实DPR support，因此support、回波顶和回波底不是本实验学到的结果。需要重点比较的是锚点/非锚点、Q11/Q01、gap/outside、强反射率尾部、CFAD、质心、逐高度dBZ以及最终降水和`dR/dz`。
