# GR–DPR 三维降水反演数据集说明

本文档用于解释本项目数据集的结构、变量含义、物理意义、数据来源和配准方式。内容以以下文件为例：

```text
2A.GPM.DPR.V9-20211125.20170501-S071945-E085219.018026.V07A.nc
```

当前数据目录为：

```text
/storage/GR_DPR_3D/GRToDPRRes_V07_Pct_V1.2.1_sw_260412
```

这不是单一雷达的原始观测文件，而是一次 GPM 卫星过境期间，将卫星 DPR、多个地基雷达和再分析气象场配准到同一个三维网格后形成的样本。

## 1. 项目任务概述

本项目要解决的是一个带有空间缺测掩码的三维“稀疏到稠密”降水反演问题：

```text
多个地基雷达原始扫描
  → 质量控制与订正
  → 三维栅格化
  → 多个地基雷达合并
  → DPR 网格上的稀疏 GR 反射率
  → 降水反演模型
  → DPR 网格上的三维降水率
                         ↑
                  DPR 反演产品作为标签
```

可以将模型要学习的映射概括为：

\[
(\text{GR 稀疏反射率及辅助气象场},\ \text{观测掩码})
\longrightarrow \text{DPR 三维降水率}
\]

其中：

- GR（Ground Radar）表示地基雷达；
- DPR（Dual-frequency Precipitation Radar）表示 GPM 卫星搭载的双频降水雷达；
- 地基雷达数据是模型的主要输入；
- DPR 反演的三维降水率 `pre_dpr` 是核心监督标签。

DPR 降水率本身也是遥感反演产品，并非完全无误差的物理真值。因此，更严谨的叫法是“参考标签”或“伪真值”。

## 2. 文件名含义

示例文件名可以大致拆解为：

```text
2A.GPM.DPR.V9-20211125.20170501-S071945-E085219.018026.V07A.nc
```

- `2A`：GPM 二级反演产品；
- `GPM.DPR`：GPM 卫星双频降水雷达产品；
- `20170501`：观测日期为 2017 年 5 月 1 日；
- `S071945-E085219`：原始轨道段开始、结束时间，约为 07:19:45–08:52:19；
- `018026`：卫星轨道号；
- `V07A`：DPR 产品版本；
- `.nc`：经过实验室进一步加工后保存为 NetCDF/HDF5 文件。

该示例文件大小约为 155 MB，描述一段卫星扫描条带，而不是某个时刻的一张普通二维天气图。

## 3. 数据的三维网格

示例文件的主要三维变量形状为：

```text
(nscan, nray, z) = (596, 49, 60)
```

三个维度分别表示：

```text
                     z：垂直高度方向，共 60 层
                     ↑
                     │
nscan：卫星前进方向 ──┼── nray：卫星横向扫描方向
      共 596 行       │        每行 49 个波束
```

### 3.1 `nscan=596`

`nscan` 表示沿卫星飞行方向的扫描线数量。卫星向前飞行时不断进行横向扫描，每完成一次横向扫描，就形成一条 scan line。

### 3.2 `nray=49`

每条扫描线包含 49 个横向波束，可以理解为卫星从条带一侧扫描到另一侧形成的 49 个像元。

因此，该文件包含的水平廓线数量为：

\[
596\times49=29204
\]

### 3.3 `z=60`

每个水平位置对应一根包含 60 层的垂直廓线：

```text
z[0]  = 0.125 km
z[1]  = 0.375 km
z[2]  = 0.625 km
...
z[7]  = 1.875 km
...
z[59] = 14.875 km
```

垂直间隔为 0.25 km，所以一个完整三维变量包含：

\[
596\times49\times60=1,752,240
\]

个三维格点。

需要注意，`596×49` 不是规则的经纬度矩形网格，而是沿卫星轨道弯曲的条带网格。每个像元的真实经纬度需要通过 `lat` 和 `lon` 查询。

## 4. 如何理解一个具体数据点

例如：

```python
pre_dpr[100, 20, 7]
```

表示：

- 第 100 条卫星扫描线；
- 该扫描线的第 20 个横向波束；
- 高度约为 1.875 km；
- DPR 反演得到的三维降水率。

这个格点的位置和时间可以通过以下变量确定：

```python
lat[100, 20]
lon[100, 20]
time[100]
z[7]
```

同一索引下的：

```python
dbz_gr_sparse[100, 20, 7]
dbz_gr_interp[100, 20, 7]
dbz_dpr[100, 20, 7]
pre_dpr[100, 20, 7]
```

理论上描述同一个时空格点，只是物理量和数据来源不同。

## 5. 坐标和辅助定位变量

| 变量 | 形状 | 含义 |
|---|---:|---|
| `z` | `(60,)` | 高度，单位 km |
| `lat` | `(596, 49)` | 每个水平像元的纬度 |
| `lon` | `(596, 49)` | 每个水平像元的经度 |
| `time` | `(596,)` | 每条扫描线的时间 |
| `scan_id` | `(596,)` | DPR 扫描线编号 |

`lat` 和 `lon` 没有 `z` 维，因为同一根垂直廓线的 60 层共享一个水平位置。

`time` 只有 `nscan` 维，因为一条横向扫描线上的 49 个波束可以近似认为在同一时刻观测。

示例文件的时间单位是：

```text
seconds since 2017-05-01 07:47:30
```

实际读取时应由 NetCDF4 或 xarray 自动解码，不能直接把原始整数当作 Unix 时间戳。

## 6. 地基雷达 GR 数据

### 6.1 雷达反射率是什么

雷达发射电磁波，雨滴、雪、冰雹等水凝物会将一部分能量散射回来。反射率描述雷达接收到的回波强度。

通常：

- dBZ 越大，水凝物越多或粒子越大；
- 较强反射率一般对应较强降水；
- 但反射率和降水率并不是一一对应关系；
- 相同 dBZ 在层云、对流、雨、雪、冰雹条件下可能对应不同降水率。

dBZ 是对数单位。线性雷达反射率因子 \(Z\) 与 dBZ 的关系为：

\[
\mathrm{dBZ}=10\log_{10}Z
\]

因此，40 dBZ 和 20 dBZ 的物理差异不是简单的两倍。

### 6.2 `dbz_gr_sparse`

```text
形状：(596, 49, 60)
单位：dBZ
```

这是最核心的地基雷达输入，表示配准到 DPR 三维网格后的稀疏地基雷达平均反射率。

地基雷达原本按照方位角、仰角和距离发射波束。转换到统一三维网格后，只有雷达波束实际穿过的位置有直接观测，其他位置会形成空洞，因此称为 `sparse`。

### 6.3 `dbz_gr_sparse_min` 和 `dbz_gr_sparse_max`

```text
形状：(596, 49, 60)
单位：dBZ
```

一个公共三维格点中可能落入多个地基雷达采样点，所以数据集保存了：

- `dbz_gr_sparse`：平均反射率；
- `dbz_gr_sparse_min`：最小反射率；
- `dbz_gr_sparse_max`：最大反射率。

三者可以描述一个公共格点内部的变化程度。例如：

```text
样本 A：mean=25，min=24，max=26
样本 B：mean=25，min=10，max=45
```

虽然平均值相同，但样本 B 的内部不均匀程度明显更强，可能包含局地强对流核心。只保留平均值会丢失这类信息。

当前说明文档没有指出平均值是在 dBZ 空间直接计算，还是先转换为线性 \(Z\) 后计算。这会影响物理含义，后续需要从数据生成代码或数据生产者处确认。

### 6.4 `dbz_gr_interp`

```text
形状：(596, 49, 60)
单位：dBZ
```

这是由稀疏地基雷达反射率经过空间插值得到的连续反射率场：

```text
dbz_gr_sparse：直接观测，存在空间空洞
dbz_gr_interp：插值填补后更加连续
```

它不是新的雷达观测，而是根据周围已有观测推算得到的值。

优点：

- 空间上更连续；
- 容易输入传统卷积神经网络；
- 可作为传统基线。

缺点：

- 容易削弱局地极值；
- 容易平滑强对流结构；
- 无法直接区分真实弱回波和插值出来的弱回波；
- 插值前损失的信息通常很难由后续模型恢复。

因此，本项目可能更适合直接使用 `dbz_gr_sparse` 和对应的有效位置掩码，把 `dbz_gr_interp` 作为基线或辅助变量。

### 6.5 `sw_gr_sparse*`

包括：

- `sw_gr_sparse`；
- `sw_gr_sparse_min`；
- `sw_gr_sparse_max`。

```text
形状：(596, 49, 60)
单位：m/s
```

数据说明将其描述为地基雷达的速度标准差或谱宽。谱宽反映一个雷达采样体积中粒子径向速度的离散程度，可能受到以下因素影响：

- 湍流；
- 风切变；
- 不同粒子的下落速度；
- 雷达波束内速度不均匀；
- 非气象回波。

它不是降水率，但可以提供降水系统动力结构和粒子分布方面的辅助信息。

## 7. 星载卫星 DPR 数据

GPM 是全球降水测量卫星，DPR 是其搭载的双频降水雷达。DPR 从高空向下观测，而地基雷达从地面向上、向四周扫描，两者观测几何明显不同。

### 7.1 `dbz_dpr`

```text
形状：(596, 49, 60)
单位：dBZ
```

这是 DPR 的 Ku 波段三维反射率。数据说明指出它经过了衰减订正。

它和 `dbz_gr_sparse` 都是反射率，但不能认为二者应该完全相等，因为两类雷达存在：

- 雷达频率不同；
- 观测方向不同；
- 空间分辨率和波束体积不同；
- 衰减、杂波和遮挡影响不同；
- 仪器校准误差；
- 卫星与地基雷达之间的观测时间差；
- 地基雷达远距离波束展宽；
- 波束未被降水目标完全填满等问题。

`dbz_dpr` 可以用于 GR–DPR 差异分析、质量控制或辅助监督。但如果模型部署时只有地基雷达，就不能把它作为推理阶段的输入。

### 7.2 `pre_dpr`

```text
形状：(596, 49, 60)
单位：mm/h
```

这是本项目最核心的三维标签。

它不是卫星直接测量的“雨量”，而是 DPR 处理算法根据雷达反射率、双频信息、衰减订正和微物理假设反演得到的三维降水率。

可以这样理解不同数值：

```text
pre_dpr = 0       有效观测且没有降水
pre_dpr = 1       每小时约 1 mm 的降水率
pre_dpr = 20      很强的降水
pre_dpr = NaN     没有有效观测，或者该格点不可信
```

必须特别注意：

```text
0 不等于缺测
```

- `0` 是有效的无降水样本；
- NaN 或 mask 表示未知，不能直接当成无降水参与训练。

所谓“密集标签”是相对于稀疏 GR 而言。`pre_dpr` 在 DPR 条带和有效高度范围内较为连续，但并不是整个数组中的所有位置都可靠。

### 7.3 `nsrr_dpr`

```text
形状：(596, 49)
单位：mm/h
```

表示 DPR 的近地面降水率，英文为 Near Surface Rain Rate。它通常取地表杂波影响区上方、距离地面较近且仍然可信的位置，因此不一定是严格意义上的地表值。

### 7.4 `srr_dpr`

```text
形状：(596, 49)
单位：mm/h
```

表示 DPR 的地表降水率，英文为 Surface Rain Rate，是将观测和反演结果进一步推算到地表后得到的二维产品。

三类 DPR 降水率的区别可以概括为：

```text
pre_dpr     整根三维降水廓线
nsrr_dpr    接近地面的一个可信降水率
srr_dpr     最终推算到地表的降水率
```

## 8. 地表和杂波相关变量

### 8.1 `cfb`

```text
形状：(596, 49)
类型：原始 DPR 距离仓索引
```

`cfb` 是 Clutter Free Bottom 的缩写，表示每根廓线中没有明显地表杂波影响的最低高度层。

星载雷达向下观测时，接近地面的位置会受到强烈地表回波影响。师兄的数据加载代码使用类似下面的逻辑：

```python
cfb_z = z[cfb]
dpr_clutter = z < cfb_z
```

也就是低于 `cfb` 的高度层会被视为杂波影响区域，不能用于计算训练损失。

### 8.2 `binRealSurface`

```text
形状：(596, 49)
类型：整数高度层索引
```

变量属性将它描述为地表位置索引，但实际样本中的取值约为 `160–176`，超过当前 60 层 `z` 网格的索引范围 `0–59`。因此，它不是当前 `z` 数组的直接索引，而应当是原始 DPR 距离仓（range bin）中的地表索引。

它和 `cfb` 的区别是：

- `binRealSurface`：地表位于原始 DPR 的哪个距离仓；
- `cfb`：降水观测最低可信到哪一层。

通常 `cfb` 位于真实地表上方，因为靠近地面的若干层已经受到地表杂波污染。

不能直接执行 `z[binRealSurface]`。若要把 `binRealSurface` 转换为当前数据集的海拔高度，需要额外获得原始 DPR 距离仓到高度的映射关系。

## 9. 降水类型和质量标志

### 9.1 `typePrecip`

```text
形状：(596, 49)
```

取值含义：

- `1`：层云性降水（Stratiform）；
- `2`：对流性降水（Convective）；
- `3`：其他降水；
- `-1111`：无降水；
- `-9999`：缺测。

层云和对流的物理结构差异较大：

- 层云降水通常范围大、较均匀、强度偏弱；
- 对流降水通常范围较小、变化剧烈，容易出现强反射率和强降水。

该变量适合用于分组评价，例如分别统计层云和对流降水的模型效果。但是它来源于 DPR，实际部署时未必可用，因此不能不加考虑地作为模型输入。

### 9.2 `flagPrecip`

```text
形状：(596, 49)
```

这是 DPR 降水检测方式和置信度的编码。例如：

- `0`：无降水；
- `1/2`：Ku 单频检测，可能/确定；
- `11/12`：Ka 相关检测，可能/确定；
- `21/22`：双频检测，可能/确定。

例如 `22` 表示双频确定检测到降水，可信度相对较高。

## 10. 再分析气象背景场

这些变量既不是地基雷达直接测得的，也不是卫星 DPR 直接测得的，而是来自再分析资料，之后被插值到 DPR 三维格点。

### 10.1 `p`

```text
形状：(596, 49, 60)
单位：hPa
```

表示气压。气压通常随高度上升而降低。

### 10.2 `t`

```text
形状：(596, 49, 60)
单位：K
```

表示温度。开尔文温度换算为摄氏度：

\[
T_{\degree C}=T_K-273.15
\]

温度会影响水凝物是液态雨滴、雪还是冰，因此对“反射率到降水率”的映射很重要。

### 10.3 `q`

```text
形状：(596, 49, 60)
单位：kg/kg
```

表示比湿，即湿空气中水汽质量所占的比例。它描述环境中的水汽条件。

`p/t/q` 可以作为模型的环境背景输入，帮助模型区分不同温度、湿度和高度条件下的降水微物理过程。

## 11. 变量来源汇总

| 数据来源 | 主要变量 | 物理含义 | 在项目中的角色 |
|---|---|---|---|
| 地基雷达 GR | `dbz_gr_sparse*` | 稀疏三维反射率 | 主要输入 |
| 地基雷达 GR | `sw_gr_sparse*` | 多普勒速度谱宽 | 辅助输入候选 |
| GR 插值产品 | `dbz_gr_interp` | 插值后的连续反射率 | 传统基线或辅助输入 |
| 卫星 DPR | `dbz_dpr` | Ku 波段三维反射率 | 对比分析或辅助监督 |
| 卫星 DPR | `pre_dpr` | 三维降水率 | 核心标签 |
| 卫星 DPR | `nsrr_dpr`、`srr_dpr` | 近地面、地表降水率 | 二维评价或辅助标签 |
| 卫星 DPR | `cfb`、`binRealSurface` | 杂波底和地表位置 | 构造有效标签掩码 |
| 卫星 DPR | `typePrecip`、`flagPrecip` | 降水类型和检测质量 | 样本筛选、分组评价 |
| 再分析资料 | `p`、`t`、`q` | 气压、温度、比湿 | 环境辅助输入 |
| 坐标变量 | `lat`、`lon`、`z`、`time` | 时空位置 | 数据定位和可视化 |

## 12. GR 与 DPR 的主要差异

| 特征 | 地基雷达 GR | 星载雷达 DPR |
|---|---|---|
| 平台 | 固定在地面 | 搭载在 GPM 卫星上 |
| 观测方向 | 从地面向上、向四周扫描 | 从高空向下扫描 |
| 原始坐标 | 方位角、仰角、距离 | 沿轨扫描、横轨波束、垂直距离仓 |
| 空间覆盖 | 雷达站周围，受地形和距离限制 | 沿卫星轨道形成条带 |
| 时间覆盖 | 可以高频连续扫描 | 只有卫星过境时才能观测 |
| 近地面问题 | 远处低层可能被地球曲率和地形遮挡 | 近地面容易受地表杂波影响 |
| 主要项目变量 | `dbz_gr_sparse` | `dbz_dpr`、`pre_dpr` |
| 项目角色 | 模型输入 | 参考标签和质量信息 |

即使二者被放到同一个三维数组中，它们也不会天然完全一致。频率、观测几何、波束体积、时间差、衰减和仪器偏差仍然存在，这些也是模型需要处理的困难。

## 13. 不同来源的数据如何统一到同一个网格

原始地基雷达通常使用：

```text
雷达站点 + 方位角 + 仰角 + 距离
```

卫星 DPR 使用：

```text
卫星扫描线 + 横向波束 + 垂直距离仓
```

数据集预处理大致执行了以下步骤：

1. 对地基雷达进行质量控制和订正；
2. 将地基雷达极坐标转换为经度、纬度和海拔高度；
3. 根据卫星过境时间匹配附近时刻的地基雷达扫描；
4. 将多个地基雷达观测组合起来；
5. 将 GR 观测栅格化到 DPR 水平像元和统一的 60 个高度层；
6. 对落入同一个公共格点的 GR 数据计算平均值、最小值和最大值；
7. 将 DPR 反射率和降水率重采样到同一组高度层；
8. 将再分析的 `p/t/q` 插值到同一三维格点；
9. 使用 `cfb` 等变量标记不可信区域。

完成配准后，相同数组索引 `(i, j, k)` 就表示大致相同的经度、纬度、高度和观测时刻。

但“索引统一”不等于“物理观测完全等价”。GR 与 DPR 的系统偏差仍然存在。

当前资料没有给出以下数据生产细节：

- GR 与 DPR 匹配时允许的最大时间差；
- 水平和垂直配准的具体算法；
- 多雷达重叠区域的优先级或加权方式；
- 格点内平均是在 dBZ 空间还是线性反射率空间完成；
- 插值算法的搜索半径和边界处理方法。

这些细节可能影响后续实验结论，建议从数据生成代码或数据生产者处进一步确认。

## 14. 缺测、无降水和低反射率的区别

这是使用数据时最重要的区别之一。

### 14.1 缺测或无效

通常表示为：

```text
NaN、masked value、-9999 等特殊编码
```

含义可能是：

- 雷达没有覆盖；
- 受地表杂波影响；
- 数据质量不合格；
- 配准后该位置没有观测。

缺测表示“不知道”，不能直接当成 0。

### 14.2 有效的无降水

通常表现为有效的 `pre_dpr=0` 或降水检测标志为 0，表示传感器对该位置进行了有效判断，但没有检测到降水。

### 14.3 弱反射率或噪声

低 dBZ 可能表示很弱的水凝物回波，也可能接近雷达噪声。师兄现有加载代码会把低于约 9 dBZ 的 GR 反射率填为 0，但这属于模型预处理规则，不应与原始缺测混为一谈。

## 15. Z–R 关系是什么

传统降水反演经常通过经验关系将反射率 \(Z\) 转换成降水率 \(R\)：

\[
Z=aR^b
\]

项目中的高度分层 Z–R 模型写成：

\[
R=\exp(\mathrm{dBZ}\cdot w_z+b_z)
\]

其中每个高度层都有独立的 `weight` 和 `bias`。

这种方法简单、物理意义明确，可以作为重要基线。但同一反射率可能对应不同的粒径分布、降水类型和相态，因此单一 Z–R 关系很难覆盖所有天气条件。

## 16. 对后续建模最重要的认识

### 16.1 输入不只是数值，还包括掩码

模型必须知道哪些 GR 格点是真实观测，哪些是缺测。否则无法区分：

```text
没有观测到数据
```

和：

```text
确实观测到了弱回波或无降水
```

### 16.2 标签也不是所有位置都有效

训练时必须利用 `pre_dpr` 的 mask 和 `cfb` 排除缺测、地表杂波等不可信区域。

### 16.3 不能把任务简单当作普通图片回归

该数据同时具有：

- 弯曲的卫星条带坐标；
- 三维垂直结构；
- 不规则的稀疏观测；
- 多传感器系统偏差；
- 严重长尾和大量无降水样本；
- 有效区、缺测区和杂波区掩码。

### 16.4 防止使用部署时不可获得的信息

如果最终目标是在只有地基雷达时反演降水，那么以下 DPR 变量原则上只能作为标签、质量控制或评价信息：

- `dbz_dpr`；
- `pre_dpr`；
- `nsrr_dpr`；
- `srr_dpr`；
- `typePrecip`；
- `flagPrecip`。

训练输入应优先来自实际推理时能够获得的 GR 和气象背景场。

## 17. 推荐的数据理解顺序

在研究网络前，建议按以下顺序熟悉数据：

1. 读取一个 `.nc` 文件并打印所有变量、形状和属性；
2. 选择某个高度层，例如 `z=1.875 km`，绘制二维条带图；
3. 选择一条扫描线，绘制距离–高度垂直剖面；
4. 对比 `dbz_gr_sparse` 和 `dbz_gr_interp`，观察插值填补了什么；
5. 对比 `dbz_gr_sparse` 和 `dbz_dpr`，观察两类雷达的系统差异；
6. 对比 GR 通过 Z–R 得到的降水率和 `pre_dpr`；
7. 检查 `cfb` 以下的杂波区域；
8. 分别查看层云、对流和无降水样本；
9. 统计 `pre_dpr` 的零值比例、分位数和强降水尾部；
10. 最后再确定模型输入、标签、掩码和损失函数。

## 18. 相关文件

- 数据集原始变量说明：`variables_schema-段晨阳.docx`
- 项目任务介绍：`20260408雷达降水廓线反演-20260818.pptx`
- Z–R 批量转换脚本：`zrh_nc_to_rain.py`
- GR/DPR 对比绘图脚本：`plot_2km_zrh_4files.py`
- 数据目录附带的加载代码：`/storage/GR_DPR_3D/DatasetNew.py`
